# Backy page and backend completeness audit

Date: 2026-05-07

This audit tracks whether each Backy admin/public surface has the necessary backend and editor behavior expected from a WordPress/Wix/Canva-like product. It is intentionally strict: a page is not complete until it persists through backend APIs, exposes the right public/admin contract, handles loading/empty/error states, and supports the workflows users expect from a production CMS/editor.

## Completion legend

- **Ready**: backend-backed, validated, user-facing behavior is complete for the current product scope.
- **Partial**: usable path exists, but important backend/product behavior is missing.
- **Prototype**: mostly local/mock UI or incomplete workflow.
- **Missing**: route or workflow is absent.

## Product-level gates

Backy should not be called complete until these gates are satisfied:

- **Durable backend**: sites, pages, posts, users, settings, media, forms, comments, events, and publish state are stored in the same durable backend, not split between seed data, localStorage, and local JSON files.
- **Admin APIs**: every admin page has authenticated CRUD endpoints with validation, pagination, RBAC, audit trails, and consistent `{ success, requestId, data/error }` envelopes.
- **Public render contract**: custom frontends can fetch exact site/page/post data, theme tokens, assets, actions, bindings, SEO, and editable maps without relying on admin implementation details.
- **Editor parity**: page/post editing covers Canva/Wix expectations: drag/resize, selection, layers, snapping/alignment, grouping, undo/redo, responsive breakpoints, media/font insertion, template sections, preview, publish, and revision history.
- **WordPress parity**: pages/posts/media/users/settings support drafts, publish/schedule, slugs/permalinks, SEO, revisions, categories/tags where relevant, comments/moderation, forms, roles, and export/import hooks.
- **Operational readiness**: auth, permissions, rate limits, storage quotas, telemetry, backups, webhook retries, tests, and deployment configuration are documented and verified.

## Admin page matrix

| Surface | Status | Current evidence | Required to complete |
| --- | --- | --- | --- |
| `/login` | Prototype | `apps/admin/src/stores/authStore.ts` uses mock users and localStorage. | Replace mock auth with backend sessions/tokens, password reset, invite acceptance, MFA-ready flow, role enforcement on routes, and server-side authorization. |
| `/` dashboard | Partial | `apps/admin/src/routes/index.tsx` shows stats from local store and recent activity. | Backend aggregate metrics, real storage/database status, failed workflow alerts, publishing health, onboarding state, and links to unfinished work. |
| `/sites` | Partial | `apps/admin/src/routes/sites.tsx` now loads and deletes through `GET/DELETE /api/admin/sites`, with local fallback state. | Add filters, ownership/RBAC, domain/publish status, duplicate/archive flows, and stronger destructive-action confirmation UX. |
| `/sites/new` | Partial | `apps/admin/src/routes/sites.new.tsx` now creates through `POST /api/admin/sites`, with local fallback state. | Add template/site type selection, domain validation, default pages, publish target configuration, and richer failure recovery. |
| `/sites/$siteId` | Partial | `apps/admin/src/routes/sites.$siteId.tsx` now saves/deletes site settings through `PATCH/DELETE /api/admin/sites/:siteId`; forms/comments moderation sections are backend-connected. | Add domain verification, SEO/theme/publish settings, webhook configuration, form builder management, comment policy settings, and audit logs. |
| `/pages` | Partial | `apps/admin/src/routes/pages.tsx` now loads and deletes through `GET/DELETE /api/admin/sites/:siteId/pages`, with local fallback state. | Add site selector, hierarchy/navigation management, published/draft/scheduled filters, route conflict checks, bulk actions, revisions, and public preview links. |
| `/pages/new` | Partial | `apps/admin/src/routes/pages.new.tsx` now creates through `POST /api/admin/sites/:siteId/pages`, with local fallback state and draft/published/scheduled status controls including `scheduledAt`. | Apply real templates, add site-aware navigation placement, richer SEO defaults, and immediate transition into editor. |
| `/pages/$pageId/edit` | Partial | `apps/admin/src/routes/pages.$pageId.edit.tsx` now loads/saves through `GET/PATCH /api/admin/sites/:siteId/pages/:pageId`, exposes backend-backed publish/archive/revision rollback/preview controls, and hydrates site media so uploaded fonts are available in editor font controls. Backend preview tokens allow draft hosted page, render, and page API access. Editor layers can now select, root-reorder, hide/show, lock/unlock, duplicate, and delete canvas elements through persisted element fields. Canvas drag/resize now uses ref-backed transient transforms with one undo history commit on release, selected elements support arrow-key nudging, the design surface has scale-aware zoom/fit controls, dragged elements avoid accidental text selection, and smart guide lines snap dragged elements to canvas/element edges and centers. Page settings persist scheduled publishing dates. | Add stronger autosave status, responsive breakpoints, grouping, full nested layer reordering, multi-select transforms, robust undo/redo QA, and complete component properties. |
| `/blog` | Partial | `apps/admin/src/routes/blog.tsx` now loads and deletes through `GET/DELETE /api/admin/sites/:siteId/blog`, with local fallback state. Blog taxonomy and author resources now exist at admin/public endpoints, post list APIs support taxonomy/author filters, and the admin blog list exposes category/tag/author filters plus taxonomy columns. | Add full category/tag management screens, SEO metadata, bulk actions, comments status, and preview/published URLs. |
| `/blog/new` | Partial | `apps/admin/src/routes/blog.new.tsx` now creates through `POST /api/admin/sites/:siteId/blog`, with local fallback state, backend-loaded author/category/tag assignment controls, and scheduled publishing input. | Add featured image, excerpt/SEO validation, autosave/revisions, preview, and block/editor defaults suited to long-form writing. |
| `/blog/$postId` | Partial | `apps/admin/src/routes/blog.$postId.tsx` now loads/saves/deletes through `GET/PATCH/DELETE /api/admin/sites/:siteId/blog/:postId`, persists author/category/tag/scheduled publishing assignments, and exposes backend-backed publish/archive/revision rollback/preview controls. Backend preview tokens allow draft hosted post and post API access. | Add featured media, canonical/SEO controls, and comments/moderation status. |
| `/media` | Partial | `apps/admin/src/routes/media.tsx` now loads/uploads/updates/deletes through backend media endpoints, with metadata editing, search/type/visibility filters, folder create/delete/filter/assignment UI, and usage links to referenced pages/posts. Page/post save, rollback, and delete flows sync media usage references. Public media catalog hides private items. | Add authenticated/signed private media file delivery, image transforms, storage provider abstraction, quota checks, full font registration workflow, and broader bulk/reference management. |
| Editor media modal | Partial | `apps/admin/src/components/editor/MediaLibraryModal.tsx` now uses backend list/upload and scopes assets to page/post/global contexts; `CanvasEditor` hydrates site media so uploaded fonts appear in font controls. | Same media gaps as `/media`, plus insert sizing presets, upload progress, replace asset, crop/focal point, and full font registration workflow. |
| `/users` | Partial | `apps/admin/src/routes/users.tsx` now loads/deletes through `GET/DELETE /api/admin/users`, with local fallback state. | Auth/RBAC enforcement, invitations, permissions matrix, team/workspace membership, activity logs, deactivate/reactivate, protected self-role changes, filters, and pagination. |
| `/users/new` | Partial | `apps/admin/src/routes/users.new.tsx` now creates through `POST /api/admin/users`, with duplicate email and role validation at the API layer plus local fallback state. | Real invite email/token delivery, workspace scoping, resend/expire invitation controls, and audit events. |
| `/users/$userId` | Partial | `apps/admin/src/routes/users.$userId.tsx` now loads/saves/deletes through `GET/PATCH/DELETE /api/admin/users/:userId`, with local fallback state. | Permissions matrix, session revocation, activity timeline, safeguards for last admin, password reset token endpoint, and audit events. |
| `/settings` | Partial | `apps/admin/src/routes/settings.tsx` now loads/saves delivery mode and regenerates API keys through `GET/PATCH/POST /api/admin/settings`, with local fallback state. Settings persist to `data/backy/admin-content.json`, and read/update/regenerate smoke passed against the running public dev server. | Auth/RBAC enforcement, real key rotation/revocation history, webhook/integration settings, site defaults, SEO/social settings, billing/storage limits, audit events, and environment validation. |

## Public/API matrix

| Surface | Status | Current evidence | Required to complete |
| --- | --- | --- | --- |
| `GET /api/sites` | Partial | `apps/public/src/app/api/sites/route.ts` now returns `{ success, requestId, data }` for public site list/detail bootstrap, keeps legacy top-level `site/sites` keys for compatibility, resolves by id/slug/custom domain, and hides draft sites. `npm run test:admin-contract --workspace @backy/public` covers draft-hidden and published-resolved behavior. | Auth/rate limits where needed, richer pagination, locale/domain canonicalization, and durable data source. |
| `GET /api/sites/:siteId/pages` | Partial | Public page retrieval exists from `backyStore`; draft access requires a matching preview token instead of raw `includeUnpublished`, and scheduled pages are hidden until their `scheduledAt` time passes. `GET /api/sites/:siteId/navigation` now exposes publishable page navigation and `GET /api/sites/:siteId/resolve?path=/...` resolves page/blog paths for external frontends. `npm run test:admin-contract --workspace @backy/public` covers future-hidden/past-visible scheduled pages, navigation readback, and page/post route resolution. | Route conflict handling, cache headers, editable navigation tree, nested hierarchy, custom links, redirects/410, complete SEO/meta payload, token auditing, and broader contract tests. |
| `GET /api/sites/:siteId/render` | Partial | `apps/public/src/app/api/sites/[siteId]/render/route.ts` returns the new AI/custom-frontend render payload for pages and blog posts and supports token-gated draft page preview. Render payloads now include a `navigation.primary` manifest derived from publishable pages, plus normalized collection dataset manifests and collection-record editable map entries when canvas elements declare collection `dataBindings`. `npm run test:admin-contract --workspace @backy/public` validates page, blog, and collection-bound page render responses against `specs/ai-frontend-contract/content-payload.schema.json`, covers public post render payloads, and checks scheduled post render guards; `npm run test:preview-contract --workspace @backy/public` covers draft blocking and tokenized page render/page/hosted preview. | Version negotiation, broader dynamic route coverage, collection record value resolution, cache strategy, stable SDK examples, CI wiring, and token auditing. |
| `GET /api/sites/:siteId/media` | Partial | Public media list exists and only returns public catalog entries. | Signed/private file delivery, transformations, responsive variants, metadata edits, reference browsing, and CDN/storage integration. |
| `GET/POST/PATCH/DELETE /api/admin/sites/:siteId/media` | Partial | Admin media upload/list/update/delete/folder endpoints store files under `public/uploads` and metadata under `data/backy/media-library.json`; list filters support type, visibility, search, tag, and folder; content saves sync page/post usage references. | Auth/RBAC, durable database, object storage, virus/type scanning, quotas, background transforms, and audit events. |
| Collection APIs | Partial | `GET/POST/PATCH/DELETE /api/admin/sites/:siteId/collections` and nested `/records` endpoints now persist collection schemas and records to `data/backy/admin-content.json`. Public endpoints under `GET /api/sites/:siteId/collections` expose only published/public-readable collections and publishable records. Collection-bound canvas elements now surface dataset and binding metadata through `/render`. `npm run test:admin-contract --workspace @backy/public` covers collection CRUD, required/unique field validation, public visibility guards, public record reads, partial record updates, collection-bound render payloads, and cleanup. | Admin collection builder UI, DB-backed indexes/querying, CSV import/export, dataset resolver UI/value hydration, dynamic list/item pages, visitor-write policies, backups, permissions/RBAC, and audit events. |
| Sites/pages admin APIs | Partial | `GET/POST/PATCH/DELETE /api/admin/sites` and `/api/admin/sites/:siteId/pages` now persist to `data/backy/admin-content.json`. Page publish/archive/rollback/revisions, `scheduledAt`, and preview token endpoints exist. The public API proxy now allows the local admin dev origin so the Vite admin can call the Next backend without CORS failures. `npm run test:admin-contract --workspace @backy/public` covers temporary site CRUD, page create/list/detail/update/delete, scheduled page visibility, and the admin-origin CORS preflight/read path. | Auth/RBAC, durable database, cache invalidation, audit events, production origin policy, and broader contract tests. |
| Blog admin APIs | Partial | `GET/POST/PATCH/DELETE /api/admin/sites/:siteId/blog` now persists posts to `data/backy/admin-content.json`. Post publish/archive/rollback/revisions, `scheduledAt`, and preview token endpoints exist. Category and tag CRUD endpoints exist under `/api/admin/sites/:siteId/blog/categories` and `/api/admin/sites/:siteId/blog/tags`; author list endpoints exist under `/api/admin/sites/:siteId/blog/authors`; public taxonomy/author list endpoints and post filters also exist. `npm run test:admin-contract --workspace @backy/public` covers temporary blog category/tag CRUD, author resources, post assignment/filtering, and scheduled post visibility. | Auth/RBAC, durable database, cache invalidation, audit events, and broader contract tests. |
| Users admin APIs | Partial | `GET/POST/PATCH/DELETE /api/admin/users` now persists team users to `data/backy/admin-content.json`; `npm run test:admin-contract --workspace @backy/public` covers list/create/duplicate-email/update/delete. | Auth/RBAC, workspace scoping, invite email/token flow, session management, permission resources, last-admin safeguards, audit events, pagination, and broader contract tests. |
| Settings admin APIs | Partial | `GET/PATCH/POST /api/admin/settings` now persists delivery mode and API keys to `data/backy/admin-content.json`; `npm run test:admin-contract --workspace @backy/public` covers read/update validation/key regeneration. | Auth/RBAC, key rotation/revocation history, secret hashing/storage policy, workspace/site scoping, audit events, webhook/integration settings, and broader contract tests. |
| Forms APIs | Partial | Form definitions, submissions, contacts, spam checks, CSV-like admin exports, and audit events exist in `apps/public/src/lib/backyStore.ts`. | Builder/admin UI, durable persistence, email/webhook delivery, retry dashboard, field-level validation schema, consent exports, and analytics. |
| Comments APIs | Partial | Page/blog comments, reports, moderation filters, and report reasons exist. | Admin policy settings, durable persistence, moderation queues in dashboard, notifications, author blocking, anti-spam provider hooks, and analytics. |
| Blog APIs | Partial | Public blog routes exist; draft detail access requires a matching preview token instead of raw draft status access, and scheduled posts are hidden until their `scheduledAt` time passes. Public category/tag/author list endpoints and `categoryId/categorySlug/tagId/tagSlug/authorId/authorSlug` blog filters now exist. `GET /api/sites/:siteId/render?path=/blog/:slug` returns a post-shaped AI/custom-frontend render payload with post comment interactions. `npm run test:preview-contract --workspace @backy/public` covers draft post blocking and tokenized post API preview; `npm run test:admin-contract --workspace @backy/public` covers public taxonomy/author list/filter smoke plus future-hidden/past-visible scheduled posts and post render payloads. | RSS/sitemap, archive/search endpoints, token auditing, and broader contract tests. |
| Public hosted pages | Partial | `apps/public/src/app/sites/[subdomain]/[[...path]]/page.tsx` renders page content and supports token-gated draft page previews; `apps/public/src/app/sites/[subdomain]/blog/[slug]/page.tsx` renders published posts and token-gated draft post previews with preview metadata marked noindex. `GET /api/sites/:siteId/resolve` exposes the same page/blog publish guards for external frontends. `npm run test:preview-contract --workspace @backy/public` covers hosted page/post preview smoke. Public rendering now skips elements marked `visible: false` by the editor layer controls. | Full hosted-route reuse of the resolver, redirects/410, sitemap/robots, theme inheritance, responsive rendering guarantees, deployment/cache policy, and richer blog archive templates. |

## Immediate implementation order

1. **Real admin persistence API**: add authenticated admin endpoints for sites/pages/posts/users/settings and move admin store writes behind API helpers.
2. **Editor completion pass**: layers panel, snapping/alignment, undo/redo hardening, responsive canvas modes, preview/publish, and revision autosave.
3. **Media management completion**: delete/update metadata, search/tags/folders, alt/caption editor, font registration, object storage abstraction.
4. **Publishing model**: add cache invalidation, preview token audit events, and broaden hosted preview tests into the main CI path.
5. **Backend validation/test suite**: broaden endpoint contract tests (`test:preview-contract`, `test:admin-contract`), editor serialization tests, render payload schema validation, and smoke tests for every admin page.

## Current conclusion

Backy has meaningful scaffolding for a CMS/editor and now has stronger media and render-contract foundations, but most admin pages are still **Prototype** or **Partial**. The next milestone should be replacing local admin CRUD with real backend APIs before adding more visual features, otherwise new Wix/Canva-style UI controls will keep saving into a non-production data model.

# Backy page and backend completeness audit

Date: 2026-05-10

This audit tracks whether each Backy admin/public surface has the necessary backend and editor behavior expected from a WordPress/Wix/Canva-like product. It is intentionally strict: a page is not complete until it persists through backend APIs, exposes the right public/admin contract, handles loading/empty/error states, and supports the workflows users expect from a production CMS/editor.

## Completion legend

- **Ready**: backend-backed, validated, user-facing behavior is complete for the current product scope.
- **Partial**: usable path exists, but important backend/product behavior is missing.
- **Prototype**: mostly local/mock UI or incomplete workflow.
- **Missing**: route or workflow is absent.

## Product-level gates

Backy should not be called complete until these gates are satisfied:

- **Durable backend**: sites, pages, posts, users, settings, media, forms, comments, events, and publish state are stored in the same durable backend, not split between seed data, localStorage, and local JSON files.
- **Admin APIs**: every admin page has authenticated CRUD endpoints with validation, pagination, RBAC, audit trails, and consistent `{ success, requestId, data/error }` envelopes.
- **Public render contract**: custom frontends can fetch exact site/page/post data, theme tokens, assets, actions, bindings, SEO, and editable maps without relying on admin implementation details.
- **Editor parity**: page/post editing covers Canva/Wix expectations: drag/resize, selection, layers, snapping/alignment, grouping, undo/redo, responsive breakpoints, media/font insertion, template sections, preview, publish, and revision history.
- **WordPress parity**: pages/posts/media/users/settings support drafts, publish/schedule, slugs/permalinks, SEO, revisions, categories/tags where relevant, comments/moderation, forms, roles, and export/import hooks.
- **Operational readiness**: auth, permissions, rate limits, storage quotas, telemetry, backups, webhook retries, tests, and deployment configuration are documented and verified.

## Admin page matrix

| Surface | Status | Current evidence | Required to complete |
| --- | --- | --- | --- |
| `/login` | Partial | `apps/public/src/app/api/admin/auth/login/route.ts`, `/session`, `/logout`, `/password-recovery`, `/accept-invite`, and `/reset-password` now provide backend-backed local admin authentication. `apps/admin/src/stores/authStore.ts` signs in through the admin auth API, accepts invite tokens, resets local-demo passwords, persists the returned session token/user, can validate an existing session, and revokes the session on logout. The login page now requests password recovery through the backend auth API instead of checking local hard-coded users in the browser, `/accept-invite?token=...` is a public route that activates pending invited users and starts a session, and `/reset-password?token=...` lets admins set a new local-demo password from a generated reset token. `npm run test:login --workspace @backy-cms/admin` covers invalid credentials, API-backed local recovery, successful sign-in, persisted session token, and dashboard redirect. `npm run test:users --workspace @backy-cms/admin` now covers generated invite-link acceptance, user activation, returned session, generated reset-link acceptance, new-password login, and `user.invite.accept`/`user.password_reset.accept` audit logging. | Replace local demo auth with production Supabase Auth or equivalent, email reset delivery, MFA-ready flow, route/API RBAC enforcement beyond Users, refresh/rotation/revocation history, session cookies or httpOnly token storage, audit-event UI, and server-side authorization for every admin mutation. |
| `/` dashboard | Partial | `apps/admin/src/routes/index.tsx` now loads sites, pages, blog posts, users, media, runtime storage/database/Supabase/Vercel settings, site readiness, and recent admin audit logs from backend APIs, with local fallback state if the admin API fails and user-directory access degrades independently if that protected endpoint is unavailable. It shows refresh/loading/fallback states, backend health, publishing blockers/warnings, quick creation paths, launch-ready workflow links, module map, API handoff, custom frontend setup links, request-id-backed activity, and a dashboard-run infrastructure diagnostic panel that reuses the Settings validator for database, media storage, Supabase, and Vercel readiness. The dashboard design now follows `DESIGN.md` with semantic status tokens and a denser control-room layout. `npm run test:dashboard --workspace @backy-cms/admin` now covers API-backed temporary site creation, rendered command center/site selector/stats/readiness/infrastructure diagnostics/workflows/attention/activity/API/module regions, active API site switching with scoped endpoint text, critical builder/navigation links, infrastructure check execution, refresh behavior, layout checks, screenshot capture, and cleanup. | Add real aggregate analytics, deployment execution/history, products/commerce signals, form/comment moderation queues, workflow failure alerts, onboarding state, per-workspace RBAC filtering, and rendered visual regression coverage. |
| `/sites` | Partial | `apps/admin/src/routes/sites.tsx` now loads and deletes through `GET/DELETE /api/admin/sites`, with local fallback state. The multi-site workspace includes command/readiness/API/feature-contract/library regions, search plus status/domain/page-coverage filters, CSV/frontend-contract export, per-site publish status editing, duplicate-as-draft, quick archive, preview/manage links, and typed delete confirmation. `npm run test:sites --workspace @backy-cms/admin` creates a temporary site, verifies the rendered multi-site command/API/feature-contract/library surfaces, publishes it through the status control, exercises search/domain/page/status filters, duplicates it as a draft without carrying the custom domain, archives the original, verifies each mutation through the admin API, confirms typed deletion, checks layout overflow, captures a screenshot, and cleans up. | Add ownership/RBAC, domain verification, Vercel deploy orchestration, per-site billing/quotas, audit-event UI, and DB-backed production persistence. |
| `/sites/new` | Partial | `apps/admin/src/routes/sites.new.tsx` now creates through `POST /api/admin/sites`, supports draft/published/archived status selection, slug/domain validation, blank/business/storefront/publication blueprints, default starter page seeding, public address/API handoff previews, download/copy handoff controls, and recovery links if starter page seeding fails. `npm run test:sites --workspace @backy-cms/admin` now drives the rendered `/sites/new` form, creates a published storefront site through the UI, verifies Home/Shop/Contact starter pages through the admin pages API, confirms homepage/status inheritance, then continues through `/sites` filters, status changes, duplication, archiving, typed deletion, screenshot capture, and cleanup. | Add real domain verification/DNS state, Vercel deploy target orchestration, workspace/team ownership, plan limits, billing quotas, template marketplace/imports, audit-event UI, and DB-backed production persistence. |
| `/sites/$siteId` | Partial | `apps/admin/src/routes/sites.$siteId.tsx` now saves/deletes site settings through `PATCH/DELETE /api/admin/sites/:siteId`; forms/comments moderation sections are backend-connected. Form submissions that generated collection draft records now show a content shortcut into the Collections admin surface. The page now loads site pages and stored navigation through admin APIs, then lets admins add page/internal/external primary/footer menu items, nest children, reorder root links, toggle visibility, and save through `PATCH /api/admin/sites/:siteId/navigation`. It also loads/saves exact redirect and 410 gone rules through `GET/PATCH /api/admin/sites/:siteId/redirects`, can preview unsaved redirect route warnings through `POST /api/admin/sites/:siteId/redirects`, and displays source-shadowing or missing-target diagnostics before/after save. Site SEO defaults, JSON-LD defaults, sitemap settings, and robots settings persist through `GET/PATCH /api/admin/sites/:siteId/seo`. `npm run test:site-detail --workspace @backy-cms/admin` now creates a temporary site, opens its workspace in authenticated headless Chrome, verifies command/readiness/navigation/redirect/SEO/settings/automation/handoff regions, adds primary and footer navigation through the rendered editors, persists a header search toggle, previews and saves a redirect rule, saves SEO defaults, confirms all three backend APIs read back the UI changes, captures a screenshot, and cleans up the site. | Add domain verification, theme/publish settings, webhook configuration, form builder management, comment policy settings, audit logs, richer drag-and-drop menu ordering, and advanced route SEO controls. |
| `/pages` | Partial | `apps/admin/src/routes/pages.tsx` now loads and deletes through `GET/DELETE /api/admin/sites/:siteId/pages`, with local fallback state. The list now has an active-site selector, status filters for all/published/draft/scheduled/archived pages, readiness health pulled from site readiness, first readiness issue text for route/SEO/layout diagnostics, a blocked-health metric filter, refresh control, preview action, published-page link action, search-aware row selection, and bulk publish/archive/delete actions through backend APIs. Empty states distinguish an empty site from filtered results, every create entry point routes through link-backed `/pages/new` navigation with the active `siteId`, and the empty-site action now uses the same generic `New Page` flow instead of a separate homepage starter. The table, CSV export, and handoff JSON now expose route diagnostics, delivery diagnostics, page hierarchy, and navigation placement context: page rows show route availability/conflict state, duplicate/reserved/invalid routes and collection list/item route shadows block publish and bulk publish, delivery rows show published/preview-only/scheduled/archived/blocked state with public/render/resolve/preview endpoint context plus public delivery health probes for published pages, top-level pages show child counts, child pages show their parent title/route, and page rows include navigation placement/label metadata for custom frontend handoff. Single-page and bulk publish actions now open review dialogs before mutating backend state, showing route, readiness, delivery, public/render/resolve, and preview endpoint context while still rechecking readiness before publish. Page rows also load revision summaries from `/api/admin/sites/:siteId/pages/:pageId/revisions`, showing revision count, latest note/date/status, and a direct history link into the editor; CSV and handoff JSON include route, delivery, delivery-health, delivery-health history, and revision summary fields. Published-page delivery health can be refreshed from the command center or directly from each published row, and each row now shows a recent-probes trail with timestamp, status, and public/render/resolve HTTP results. Browser QA covers empty-site controls, blocked filter activation, clear-filter behavior, empty-state/new-page navigation, a temporary published row with health plus preview/edit/delete/published actions, and temporary search-filtered rows through bulk select-visible, publish, archive, and delete. `npm run test:pages-list --workspace @backy-cms/admin` specifically verifies the empty-site `New Page` action renders a usable page creation command center with active site and enabled `Create Page` submit control; it also verifies the command-center registration shortcut, temporary parent/child hierarchy rendering, a temporary backend revision surfaced in the list, row route availability diagnostics, row delivery diagnostics, published-page delivery health display, refresh control plus recent-probe history, the single-page publish review modal, the bulk-publish review modal, the confirmed bulk-publish mutation path by publishing a temporary draft page, verifying the success notice/table row state, clearing selected bulk UI state, and reading the published status back from the admin page detail API, plus named visual screenshot artifacts and layout/overflow checks for empty, delivery-row, bulk-modal, and post-publish table states. | Current page-list scope is complete; broader platform gaps remain around auth/RBAC, durable DB, visual baseline comparison thresholds, and cross-browser CI. |
| `/pages/new` | Partial | `apps/admin/src/routes/pages.new.tsx` now creates through `POST /api/admin/sites/:siteId/pages`, with local fallback state, draft/published/scheduled status controls including `scheduledAt`, real starter canvas templates for landing/storefront/blog-index/about/contact/registration pages, editable header/navigation/footer chrome for non-blank templates, route conflict checks against the selected site's page library, a creation handoff manifest, and immediate transition into the visual editor after persistence. The create route includes site-aware navigation placement controls for no menu, primary menu, or footer menu, persists those choices in route search/meta preview, and patches site navigation after the page record is created. It also supports parent page placement: selected parents are saved as `parentId` plus `meta.parentPageId`/`meta.parentPageTitle`, homepage routes stay top-level, and primary/footer navigation nests the created page under the chosen parent. It has local autosave/recovery for unsaved title, slug, target site, template, status/schedule, homepage flag, parent page, SEO description, search title, canonical path, Open Graph image URL, robots flags, and navigation placement/label, with restore/discard actions and autosave status in the command center; successful create clears the local draft. The page now includes SEO/social controls and persists `meta.title`, `meta.description`, `meta.canonical`, `meta.keywords`, `meta.jsonLd`, `meta.ogImage`, `meta.noIndex`, and `meta.noFollow` so public render payloads and custom frontends receive route metadata immediately after creation. Keywords and JSON-LD can be supplied directly or generated from the selected template defaults. Template selection now includes reusable visual wireframe previews for every starter type plus a selected-template canvas summary for chrome/section expectations before create. The editor route retries freshly-created backend page loads before falling back to local state, so a successful create opens the backend-backed editor without a misleading local-copy warning. `npm run test:page-create --workspace @backy-cms/admin` covers rendering the create workspace from search state, autosave localStorage persistence, desktop and mobile template-preview visual assertions with screenshot artifacts, switching contact/storefront/blog-index/blank/registration templates and verifying selected previews, navigation defaults, form seeds, dynamic-data seeds, site-chrome payload state, template preview rendering/selection state, recovery restore, creating a temporary parent page and child backend page through the UI, clearing the local draft after create, navigating into the editor without backend fallback, verifying the selected primary navigation parent/child nesting through the admin navigation API, verifying saved parent/SEO/keywords/JSON-LD/social/robots meta through the page detail API, verifying the created About template persisted editable header/navigation/footer chrome, title/story/value canvas elements, nested value-card children, canvas sizing, and content-document metadata, and creating landing/storefront/blog-index/contact/registration/blank starter pages through the UI to verify backend-persisted chrome, form blocks, data bindings, canvas sizing, content-document metadata, editor-canvas DOM rendering, non-collapsed required canvas elements, no local fallback, no empty canvas state, no horizontal overflow, and per-template editor screenshot artifacts before cleanup. | Add screenshot comparison thresholds for created editor canvas states once a visual baseline runner exists. |
| `/pages/$pageId/edit` | Partial | `apps/admin/src/routes/pages.$pageId.edit.tsx` now loads/saves through `GET/PATCH /api/admin/sites/:siteId/pages/:pageId`, exposes backend-backed publish/archive/revision rollback/preview controls, and hydrates site media so uploaded fonts are available in editor font controls. Text styling controls can now open the media picker to upload/select font files for the selected element. Backend preview tokens allow draft hosted page, render, and page API access. Editor layers can now select, root-reorder, move up/down in sibling order, move nested layers out to the parent while preserving visual position, move selected layers into child-accepting layers, hide/show, lock/unlock, duplicate, and delete canvas elements through persisted element fields. Canvas drag/resize now uses ref-backed transient transforms with one undo history commit on release, selected elements support arrow-key nudging, multi-layer drag, proportional multi-layer resize/scale from corner handles, toolbar align-left/center/right/top/middle/bottom commands, and multi-layer horizontal/vertical spacing distribution, the design surface has scale-aware zoom/fit controls, dragged elements avoid accidental text selection, and smart guide lines snap dragged elements to canvas/element edges and centers. Canvas elements also support shift/command/control-click multi-select from the design surface for grouping and layer operations. The component library now supports composed nested presets and includes hero, feature-grid, and lead-capture form blocks that remain normal editable canvas trees after drop. The editor library also loads active API-backed reusable sections for the active site, can save the selected element tree as a reusable section, can rename/delete saved sections, can insert saved sections as synced canvas instances with source metadata, can refresh a selected synced instance from the saved source, can detach it into an independent editable copy, and still stores concrete canvas trees for public rendering. The shared canvas editor now supports desktop/tablet/mobile breakpoint editing by merging active breakpoint layout/content/style and layer visibility/lock changes into `element.responsive` overrides while keeping desktop base state intact; the inspector exposes an active breakpoint override card with reset-all plus group-level layout/layer/content/style reset controls that show which groups inherit desktop, and the public renderer applies mobile/tablet/desktop overrides from the rendered container width. `npm run test:editor-drag --workspace @backy-cms/admin` now creates a temporary backend page and smoke-tests authenticated editor load, heading/image/container/nested-child/form dragging, image/form resizing, Shift+Arrow keyboard nudging, Ctrl+Z/Ctrl+Shift+Z drag undo/redo plus non-drag undo/redo for inspector layout edits, keyboard nudges, and layer visibility toggles, selected text inspector font-media picker presence, `Ctrl/Cmd+A` sibling selection, three-layer spacing distribution, multi-layer resize/scale with persisted reload hydration, `Ctrl/Cmd+G` grouping, `Ctrl/Cmd+Shift+G` ungrouping with position-preserving roundtrip, layer-tree nest/outdent controls with position-preserving coordinate conversion, synced reusable-section insertion/source refresh/detach, invalid numeric input warning guards, backend save persistence, mobile and tablet breakpoint layout plus layer visibility/lock override persistence, group-level reset/inheritance controls, desktop/mobile/tablet switch hydration, and fresh editor reload rehydration in headless Chrome. The property panel now loads site collections, can attach normalized collection field bindings to selected elements, can configure dataset query controls for search, field filters, sort direction, limit, and offset, can configure collection-backed repeater elements with dataset ids, field mapping, query filters, sorting, limits, offsets, columns, gaps, and empty states, can configure form-to-collection draft record writes with field mapping, can configure image source, alt, title, object-fit, object-position, loading, decoding, and referrer-policy behavior, can configure icon symbol, size, color, title, and accessibility label behavior, can configure list type, marker style, indent, and item behavior, can configure divider color, thickness, style, and margin behavior, can configure column count and gap behavior, can configure navigation items, direction, gap, and accessibility label behavior, can configure spacer size and background color behavior, can configure quote citation and border behavior, can configure input field label, submission name, required state, placeholder, help text, input type, validation pattern, length limits, and default value behavior, can configure textarea label, submission name, required state, placeholder, help text, rows, length limits, and default value behavior, can configure select field label, submission name, required state, placeholder, help text, option list, and default selected value behavior, can configure checkbox/radio field labels, submission names, required states, placeholders, help text, option lists, and selected values, and can configure link text, href target, rel, title, ARIA label, and underline behavior, can configure button href target, rel, title, ARIA label, and button type behavior, and can configure video source, poster, object-fit, controls, autoplay, loop, mute, and inline-playback behavior plus embed iframe title, allowed feature, sandbox, loading, referrer-policy, and fullscreen behavior plus map address, zoom, title, loading, referrer-policy, and fullscreen behavior that persist into public rendering. Public PageRenderer now renders Slate-backed heading, paragraph, and quote text for custom frontend output instead of dropping array content. Public render payload normalization now preserves binding query and pagination metadata and resolves collection-bound values through search/filter/sort-aware dataset queries. The shared canvas editor now exposes visible dirty, autosave queued, autosaving, manual saving, saved, and failed-save status states, including pending-change count, last save mode/time, and last error metadata while its silent backend autosave runs. Page settings persist scheduled publishing dates. `npm run test:editor-drag --workspace @backy-cms/admin` now also creates a temporary collection, binds a canvas element through the rendered Data inspector query controls, verifies queued autosave metadata after a desktop edit, saves the page, and verifies manual saved metadata plus persisted image behavior, icon behavior, list behavior, divider behavior, columns behavior, nav behavior, spacer behavior, quote behavior, input field behavior, textarea behavior, select field behavior, checkbox/radio behavior, link behavior, button link behavior, video behavior, embed iframe behavior, map iframe behavior, query/pagination binding, rich-text public renderer output, and repeater dataset/layout props through the backend page API. | Add visual regression thresholds and complete remaining component properties. |
| `/blog` | Ready | `apps/admin/src/routes/blog.tsx` now loads and deletes through `GET/DELETE /api/admin/sites/:siteId/blog`, with local fallback state. Blog taxonomy and author resources now exist at admin/public endpoints, post list APIs support taxonomy/author filters, and the admin blog list exposes category/tag/author filters plus taxonomy columns. The list workspace has a command center, readiness map, API/frontend handoff snippets, public route snippets, CSV/JSON exports, row preview/edit/delete actions, bulk publish/archive/delete controls, a rendered taxonomy manager for creating/editing/describing/coloring/deleting categories and tags through the backend taxonomy APIs, per-row SEO/comment controls that show SEO title/description readiness, toggle `noIndex`/`noFollow` through the post update API, and summarize total/pending/flagged post comments from the comments API, plus surfaced public search and year/month archive feed URLs for custom frontend blog index pages. `npm run test:blog-list --workspace @backy-cms/admin` creates temporary taxonomy and draft post data through the admin API, submits a temporary blog comment, verifies the rendered command/API/filter/bulk/table/taxonomy/SEO/comment/search-feed/archive-feed regions, runs a visual-state regression guard for visible command/API/table/taxonomy regions, no framework overlay, no horizontal overflow, SEO/comment controls, and search/archive snippets with screenshot artifacts, toggles row `noIndex` with backend readback, creates/edits/deletes a category and tag through the rendered taxonomy manager with backend readback, exercises search/category/tag/author filters, opens a tokenized preview URL, bulk-publishes through the UI, verifies public slug, taxonomy, search, and archive feed delivery, captures a screenshot, and cleans up. | Current blog-list scope is complete; broader platform gaps remain around durable database rollout, audit-event UI, and broader contract tests tracked under Blog admin APIs. |
| `/blog/new` | Ready | `apps/admin/src/routes/blog.new.tsx` now creates through `POST /api/admin/sites/:siteId/blog`, with local fallback state, backend-loaded author/category/tag assignment controls, scheduled publishing input, shared `Panel`/`Notice`/`Button` controls, and the reusable `EditorWorkspaceFrame` around the same page editor canvas used for pages. The creation workspace has been reorganized into an editor-first layout with a wider canvas, editorial draft panel, sticky publishing rail, site-aware taxonomy/author controls, segmented draft/published/scheduled actions, readiness checks for title/slug/summary/SEO/featured-media/design/schedule, and taxonomy chips instead of cramped checkbox stacks. It now supports post-scoped featured image selection through the shared media library, persists `featuredImageId`, includes selected media in the create payload and frontend handoff manifest, and uses the selected image as the Open Graph fallback. It also includes route-level SEO creation controls for search title, canonical path, search description, Open Graph image URL, noindex, and nofollow, and persists those values into post meta. The create surface now has a real `Save draft and preview` workflow: it persists a draft, requests the existing backend preview-token endpoint, opens the hosted/API preview URL, and transitions into the blog edit page for revision/publish work. It also has local autosave/recovery for unsaved title, slug, excerpt, publishing status, SEO fields, featured media, taxonomy, author, canvas elements, and canvas size, with restore/discard actions and autosave status in the command center. The route now supports a canvas focus mode that hides draft/SEO/taxonomy/publish panels, preserves `focus=canvas` in search state, expands the editor frame for page-design work, and keeps compact save-draft-preview, publish, and show-panels controls available above the canvas. A writing-structure panel now adds editable article sections and pull quotes into the canvas while showing word-count and reading-time planning metrics, and those inserted long-form blocks persist through autosave and created post content. Browser QA covers rendering the workspace, the scheduled date control, expanded canvas height, readiness enabling save, and creating then cleaning up a draft post through the UI/API. `npm run test:blog-create --workspace @backy-cms/admin` covers the new create workspace render, desktop and focus-mode visual-state guards for visible command/workspace/canvas/editor side panels, no framework overlay, no horizontal overflow, breakpoint/grouping controls, frontend template handoff, focus-mode panel hiding, and screenshot artifacts, mobile breakpoint authoring for the frontend-template heading with desktop-layout preservation plus autosave/final-post mobile override persistence, writing-structure controls with section/quote insertion plus autosave/final-post persistence, canvas focus/restore behavior, featured media picker, SEO controls, autosave localStorage persistence, recovery restore, and save-draft-and-preview flow against the live admin/public dev servers. The embedded editor toolbar is now grouped into stable control clusters, scrolls instead of clipping in constrained widths, and supports `Cmd/Ctrl+G` plus `Shift+Cmd/Ctrl+G` for grouping/ungrouping selected layers. `npm run typecheck --workspace @backy-cms/admin`, `git diff --check`, and `npm run build --workspace @backy-cms/admin` passed for the featured media, SEO, create-preview, autosave-recovery, browser-smoke, and canvas-focus slices. | Current blog-create scope is complete; broader editor/product gates remain tracked under `/pages/$pageId/edit` and product-level gates. |
| `/blog/$postId` | Ready | `apps/admin/src/routes/blog.$postId.tsx` now loads/saves/deletes through `GET/PATCH/DELETE /api/admin/sites/:siteId/blog/:postId`, persists author/category/tag/scheduled publishing assignments, and exposes backend-backed publish/archive/revision rollback/preview controls. Backend preview tokens allow draft hosted post and post API access. The edit workspace verifies slug availability before save/publish, persists route-level SEO metadata including canonical path, Open Graph image, robots flags, and search title/description, supports featured image selection through the shared post-scoped media library, includes the image and SEO contract in frontend handoff JSON, and surfaces post-specific comment moderation state with pending/approved/flagged counts plus quick approve/reject actions against the comments API. `npm run typecheck --workspace @backy-cms/admin`, `git diff --check`, and `npm run build --workspace @backy-cms/admin` passed for the route, SEO, featured media, and comment moderation slices. | Current page scope is complete; next improvements belong to broader editor/product gates: autosave, responsive breakpoint authoring, richer long-form writing ergonomics, and visual regression coverage shared with the page editor. |
| `/collections` | Partial | `apps/admin/src/routes/collections.tsx` now provides a backend-backed collection builder for the active site. It lists collections, creates/updates/deletes collection schemas, edits field keys/labels/types/required/unique flags, edits select/tag options and reference targets, toggles public-read and visitor-create permissions, creates/updates/deletes records, filters/searches/sorts paginated record lists through the admin API, renders backend validation details, runs selected-record bulk status/delete actions, imports CSV rows through backend schema validation/upsert, exports the filtered record set as backend-generated CSV, and links published records to generated `/:collectionSlug/:recordSlug` hosted routes. `npm run test:collections --workspace @backy-cms/admin` now creates a temporary public-readable dynamic collection plus draft record, loads `/collections` in authenticated headless Chrome, verifies the command center, templates, API contract, frontend contract, schema builder, records table, field controls, route links, and record selection controls, publishes the selected record through the rendered bulk action, confirms admin and public record APIs expose the published record, captures a screenshot, and cleans up the temporary schema. | Add richer dataset binding UI inside the canvas editor, custom dynamic list/item templates, RBAC/audit events, and durable DB-backed querying. |
| `/products` | Partial | `apps/admin/src/routes/products.tsx` now exposes a backend-backed commerce catalog workspace for the active site: product schema setup/sync, public-read catalog permissions, readiness checks, public storefront/API handoff manifest, CSV export/import/template controls, filters, product CRUD, variants, pricing, inventory policy, delivery URL, checkout URL, gallery/media fields, category/tags/vendor merchandising, SEO title, publish status, and product editor state. Collection record CSV import now preserves typed number, boolean, tags/multi-reference, and JSON field values so imported catalog data is usable by storefront APIs in demo and database-backed modes. Public catalog responses now include a non-secret commerce storefront contract derived from Settings so frontends can read catalog/order-intake availability, default currency, payment-provider handoff mode, provider test/live mode, checkout redirect paths, tax/shipping/discount flags, inventory reservation settings, webhook endpoint readiness, webhook event allowlist, and reconciliation mode/window without using admin APIs or leaking webhook secret references. `npm run test:commerce --workspace @backy-cms/admin` opens the real Products/Orders UI in headless Chrome, forces full product/order schema sync during the test window, creates a published product through the Product editor, imports a typed product CSV row through the admin import API, verifies the public `/api/sites/:siteId/commerce/catalog` response, posts a public checkout order to `/commerce/orders`, verifies inventory reservation, checks Products layout overflow/regions/import controls, deletes the smoke records, captures a screenshot, and restores the original demo collection schemas. `npm run test:settings --workspace @backy-cms/admin` now authenticates with the admin auth API, verifies Settings hydration, saves commerce provider/reconciliation metadata through the rendered UI, restores the original settings, and a focused manifest verification covers public commerce contract exposure. | Add executable payment-provider session creation/reconciliation workers, tax/shipping calculation, discounts/coupons, refunds/returns, subscriptions, product option matrices, storefront product-list/page templates, richer order/customer linkage, analytics, and provider webhook processing. |
| `/orders` | Partial | `apps/admin/src/routes/orders.tsx` now exposes a private commerce operations workspace for the active site: order schema setup/sync, private order permissions, public checkout-intake contract visibility, readiness checks, admin API handoff, CSV export/import/template controls, filters, order CRUD, line items, payment state, fulfillment state, tracking, refunds, addresses, customer/support notes, and workflow actions. `npm run test:commerce --workspace @backy-cms/admin` verifies the orders schema can be temporarily synced to the full 29-field order contract and that public checkout intake writes a private order record while keeping direct public order collection access disabled. `npm run test:orders --workspace @backy-cms/admin` snapshots/restores the order schema, opens the real Orders UI, verifies command/API/metrics/queue/editor regions and layout/import controls, imports a typed order CSV row through the admin import API, creates a manual order with line items, tax/shipping/discount, payment/customer/tracking/address/note data, exercises search/payment/fulfillment/source filters, marks the order paid, fulfills it, edits refund and support notes, verifies each mutation through the admin records API, captures a screenshot, deletes the order through the confirmation modal, and restores the original schema. | Add real payment session/provider reconciliation, fulfillment integrations, shipment labels, tax/shipping calculation, refund automation, customer profiles, fraud/risk controls, notifications, and order analytics. |
| `/forms` | Partial | `apps/admin/src/routes/forms.tsx` now provides a backend-backed forms command center for the active site with form list/detail, frontend API snippets, template cards, submission inbox, approval/rejection/spam moderation, contact-share context, webhook delivery history, email notification delivery history, consent export summary/export, and rendered failed-delivery retry actions for both webhooks and email notifications. Template cards can create standalone backend forms directly instead of only copying schema or starting a page, and standalone forms now expose an inline form-builder panel for title/name/description, active/audience/moderation/success/spam/consent-retention/contact-share controls, field add/remove/reorder, field key/label/type/required/placeholder/help/options editing, per-field validation rule editing for min/max length, pattern, and numeric min/max, collection-write target selection, slug source selection, per-field collection mapping, dirty/reset state, and save through `PATCH /api/admin/sites/:siteId/forms/:formId`. Page/blog canvas-derived forms remain visible but are marked canvas-owned so admins edit them from the source canvas. Captcha-enabled public forms now verify external provider tokens before validation/persistence/delivery, accepting Turnstile, hCaptcha, reCAPTCHA, and generated frontend token aliases while keeping captcha tokens out of stored submission values. Public form notification email delivery now supports local-outbox, generic HTTP handoff, Resend, and SMTP provider modes with delivery events for queued/succeeded/failed outcomes. Selected forms can now be saved as backend reusable embed blocks through `POST /api/admin/sites/:siteId/forms/:formId/embed-block`, which creates a normal reusable section with a form canvas tree, public definition/submit URLs, and captured frontend design tokens/chrome/binding metadata so connected frontends can retain design context while dropping the form into future pages or blogs. Forms analytics are now loaded from `GET /api/admin/sites/:siteId/forms/analytics`, aggregating all form submissions across demo and repository modes into site totals, status counts, collection-route counts, approval/spam rates, 14-day trend data, and per-form breakdowns rendered on the Forms page. Form create/update/delete, submission review, consent retention, and form embed-block creation now emit request-id-backed admin audit logs, and `/forms` renders a Forms activity panel from `/api/admin/audit-logs`. Forms admin APIs now enforce first-class `forms.view`, `forms.create`, `forms.edit`, `forms.manage`, and `forms.delete` permissions instead of relying on broad collection permissions. The database repository now persists form spam/consent policy objects into the durable `settings` JSON, and `supabase/migrations/002_forms_contacts_persistence.sql` creates durable `form_definitions`, `form_submissions`, and `form_contacts` tables with RLS policies and indexes. `npm run test:forms --workspace @backy-cms/admin` verifies a denied `forms.create` permission override blocks form creation with `FORBIDDEN_PERMISSION`, verifies captcha-protected public forms reject missing tokens and accept a valid provider token without storing it as field data, then opens the real Forms UI, creates a temporary public-create collection, creates a temporary Registration form from the template, edits it through the builder into a six-field public definition with an updated title, placeholder, Company field, Company minLength validation rule, collection field mapping, notification email, notification webhook, spam rules, and consent retention/export policy, saves the edited form as a reusable embed block and verifies the reusable-section content/metadata, verifies the analytics API and rendered analytics panel/trend/top-form regions, verifies the public definition API exposes the rule and mapping, verifies an invalid public registration is rejected with the configured validation message, submits a valid public registration against the edited schema, verifies the submission creates a draft collection record with mapped values and source submission provenance, verifies configured email delivery events, retries a failed email delivery from the rendered delivery panel, verifies the consent export/retention policy panel, export action, and due-consent anonymization plus site-wide retention automation for consent fields, verifies failed webhook delivery events render in the Forms UI, retries the failed webhook from the rendered delivery panel, verifies retry queued/succeeded events, refreshes the inbox, approves the submission through the UI, verifies form/submission/embed audit-log request IDs and the rendered Forms activity panel, verifies the admin API readback, checks layout overflow/regions, captures a screenshot artifact, and deletes the temporary form/reusable block/collection. `npm run test:repositories --workspace @backy/db` verifies form definition/submission/contact repository persistence, including spam/consent settings merging. | Add deeper segmentation/list analytics and a real database-mode end-to-end smoke against a configured Supabase/Postgres service. |
| `/contacts` | Partial | `apps/admin/src/routes/contacts.tsx` now provides a backend-backed lead pipeline for contacts generated by form contact-share mappings. It loads source forms and contacts through admin APIs, aggregates all source forms or a selected form, exposes readiness checks, lifecycle metrics, permission contract/audit activity panels, private contacts/list/create/update/import/promote API snippets, handoff JSON, CSV export/import/template controls, manual contact creation with email upsert, source-value search, form/status/quality/duplicate filters, duplicate-email readiness, internal notes editing, individual lifecycle actions for new/contacted/qualified/archived, selected-contact state, select-visible controls, bulk lifecycle updates, and selected duplicate-email merge actions that consolidate identity/source data into one primary contact while archiving duplicates with trace notes. `GET /api/admin/sites/:siteId/forms/contact-segments` now provides backend contact segment analytics for all forms or a selected form, including lifecycle counts, missing identity segments, source-value retention, ready-to-promote leads, duplicate-email records, contact IDs, and source form IDs across demo and repository modes. `GET/POST/DELETE /api/admin/sites/:siteId/forms/contact-lists` persists saved contact filter lists under site settings in demo and repository modes, returns matched contact counts/IDs, and records create/update/delete audit events. `POST /api/admin/sites/:siteId/forms/:formId/contacts/:contactId/promote` promotes qualified contacts into invited or active Backy users, reuses existing users by email, generates invite links for invited accounts, writes promotion metadata back to the contact, and records contact/user audit events. `POST /api/admin/sites/:siteId/forms/:formId/contacts/:contactId/promote-customer` promotes qualified contacts into private customer collection records, creates or extends the `customers` collection when needed, upserts by email, writes customer promotion metadata back to the contact, and records contact/customer audit events. The contact handoff manifest includes create/import/segment/saved-list/promote-user/promote-customer/sync/consent-retention endpoints, backend segment summaries, saved list summaries, duplicate group metadata, forms/activity permission requirements, recent contact audit actions, and current selection/merge/retention metadata for custom admin frontends. `npm run test:contacts --workspace @backy-cms/admin` creates a temporary contact-share form, creates a contact through the direct admin contact API, imports a contact from CSV, creates duplicate-email leads, verifies the contact-segments API, creates and later deletes a saved qualified-leads list through the contact-lists API, submits a public lead, opens the real Contacts UI on that form, verifies create/import/bulk/merge/segment/saved-list/promote-user/promote-customer/sync/retention/access-audit regions render, promotes an imported contact to a Backy user and private customer record through the UI, syncs that selected contact to a webhook receiver, verifies contact-sync delivery events, exports contact consent evidence, applies due contact consent retention through the UI, verifies permission keys plus promotion/sync/retention audit activity in the Contacts access panel, merges the duplicate leads through the rendered UI, saves internal notes, marks the submitted contact qualified through the UI, selects the rendered contact, archives it through the bulk lifecycle panel, verifies the admin contact API readback, checks layout overflow/regions, captures a screenshot artifact, and deletes the temporary form/contact/customer data. `npm run test:repositories --workspace @backy/db` now verifies the repository-backed contact workflow used by the Contacts surface, including form/status/request/search filters, exact contact ID read/update behavior, duplicate merge metadata, contact/user/customer promotion metadata, consent-retention anonymization, and contact audit-log filters. | No Contacts-specific backend blocker remains; the broader configured Supabase/Postgres integration smoke is tracked under Forms/Contacts APIs. |
| `/comments` | Partial | `apps/admin/src/routes/comments.tsx` now provides a backend-backed moderation command center for page and blog comments. It loads site-wide comments, pages, posts, site comment policy, author blocklist entries, private comment analytics, and comment delivery/activity events through API helpers, shows readiness checks, moderation metrics, workflow links, private list/analytics/bulk/single update/blocklist/event API snippets, handoff JSON, CSV export, search/status/target/triage/thread/sort filters, visible selection, moderation reasons, author blocklist search/type filters, and per-comment approve/reject/spam/block/report-resolution actions. The site policy panel persists default public comment rules through `PATCH /api/admin/sites/:siteId`, including open/closed state, manual vs auto-approve, guest/name/email/reply/report toggles, blocked terms, closed message, and default sort. The admin surface now derives a thread map from `commentThreadId`/`parentId`, exposes thread summary/reply/report/pending counts, lets reviewers filter the queue to a single discussion, and displays parent/reply context on comment cards. Comment cards now include an official reply composer that posts admin-authored replies through the page/blog comment API, attaches them to the selected parent/thread, and lets reviewers publish immediately or send the reply to the manual queue. Reply cards now include a same-target parent selector so reviewers can move a reply under another top-level parent/thread through the single-comment update API. A rendered analytics panel consumes `GET /api/sites/:siteId/comments/analytics`, exposing 30-day totals, report reasons, target hot spots, and threaded queue signals for custom admin dashboards. A rendered delivery activity panel consumes `GET /api/sites/:siteId/events?kind=all`, filters comment events, and shows submission, report, moderation, and thread-change handoffs. Reported comments can now be resolved from the queue or selected bulk controls, clearing `reportCount`/`reportReasons` while preserving review metadata through the moderation API. Blocking a comment now refreshes the author blocklist panel, and stored email/IP blocklist entries can be reviewed and removed from the Comments UI. `npm run test:comments --workspace @backy-cms/admin` creates a temporary published page, submits public pending page comments plus a parent/reply thread and second top-level move target, reports one comment through the public report API, verifies the analytics API reports seeded pending/reported/threaded/reason counts, verifies comment-submitted/comment-reported event API readback, opens the real Comments UI, verifies the rendered analytics panel, delivery activity panel, thread map, and parent/reply card context, creates an official admin reply through the rendered composer and verifies API readback with parent/thread linkage plus approved status, moves an existing reply to another rendered parent and verifies parent/thread API readback, saves a stricter comment policy through rendered controls, verifies policy API readback, verifies a public blocked-term comment is rejected, approves one comment, rejects another, resolves the reported comment through rendered controls, blocks one comment with a persisted reason, verifies the author blocklist entry is created, removes it through the rendered blocklist panel, verifies API readback including reviewer/rejection reason/cleared report flags/blocklist cleanup, checks layout overflow/regions, captures a screenshot artifact, deletes the temporary page, restores default policy, and verifies attached fallback comments are cleaned up. | Add executable outbound comment notifications/webhooks, anti-spam provider hooks, RBAC/audit-event UI, and durable DB-backed moderation persistence coverage. |
| `/media` | Partial | `apps/admin/src/routes/media.tsx` now loads/uploads/updates/deletes through backend media endpoints, with metadata editing, search/type/visibility filters, folder create/delete/filter/assignment UI, selectable media cards, bulk visibility/folder/delete actions, usage links to referenced pages/posts, explicit page/post bind-unbind controls, and font registration fields for uploaded font family/weight/style/fallback/display. The page now groups uploaded font assets by family, variants, fallback stack, display mode, and delivery visibility. The page exposes usage analytics for the loaded library: storage/type mix, referenced vs unused counts, public/private delivery counts, replacement/version counts, largest assets, and actionable All/Unused/Referenced/Replaced grid focus controls. The media storage panel now shows the active runtime provider, quota, Supabase project/bucket readiness, missing environment fields, a direct Infrastructure settings link, and a Media-page "Run check" action that reuses the Settings infrastructure validator for storage/Supabase diagnostics. The same storage panel can edit and save non-secret provider metadata from Media itself: provider, bucket, public base URL, path prefix, private file delivery, image transforms, Supabase project URL/ref, and Supabase storage enablement, while keeping secrets in environment variables. The media detail dialog exposes delivery controls for custom frontends: public file endpoint URLs, time-limited signed URL generation with expiry/disposition controls, public image transform URL generation for optimized frontend image delivery, prepared responsive image `srcset` manifests with generated WebP byte/provider metadata, Backy-served delivery analytics, static upload safety scan status, stable-ID file replacement, replacement history, and a per-asset activity trail from admin audit logs. Page/post save, rollback, and delete flows sync media usage references, and `POST /api/admin/sites/:siteId/media/:mediaId/bind` can explicitly bind/unbind media to page/post contexts. Public media catalog hides private items, while public font media is exposed through `/render` `assets.fonts` with `weights`, `styles`, `fallbackStack`, `display`, and `cssFamily`; hosted pages/posts inject matching `@font-face` rules. Admin integrations can now mint temporary signed media file URLs through `POST /api/admin/sites/:siteId/media/:mediaId/signed-url`, and `/api/sites/:siteId/media/:mediaId/file` blocks unsigned private files while serving valid signed private bytes from the storage adapter and recording Backy-served request/byte analytics. Public image assets expose a `responsive` manifest in list/detail responses, `POST /api/admin/sites/:siteId/media/:mediaId/transforms` now generates stored WebP variants through the active storage adapter while accounting generated bytes against quota, and `/api/sites/:siteId/media/:mediaId/transform?width=...&quality=...` remains a validated on-demand optimizer fallback that records transform request analytics, with manifest/OpenAPI/SDK exposure and admin smoke coverage. Uploads and replacements enforce a per-site media quota from `BACKY_SITE_MEDIA_QUOTA_BYTES` with a 500 MB default, run static safety checks before storage, reject active-content SVG payloads, return quota usage on upload/list/replace responses, and count retained replacement versions plus generated transform bytes against storage usage. `npm run test:media --workspace @backy-cms/admin` now covers API-backed folder creation, public image and private file upload, rendered media command center/upload/API/storage/folder/bulk/library regions, Media-page storage metadata editing with API readback and restoration, Media-page storage/Supabase infrastructure check rendering, detail-dialog stable-ID file replacement with retained replacement history, responsive variant preparation with generated transform metadata, metadata editing with visibility changes and API readback, temporary signed URL generation from the details dialog, delete confirmation, layout checks, screenshot capture, and cleanup. `npm run test:media-quota --workspace @backy/public` verifies low-quota rejection against a configured server. `@backy/storage` provides local/S3/R2/Supabase adapter contracts for upload/read/delete/sign/list/stat, with local path traversal coverage in `npm run test:smoke --workspace @backy/storage`; public runtime media storage now selects local, S3/R2-compatible, or Supabase storage from environment variables and exposes a non-secret runtime summary through admin settings. Upload/replacement metadata stores the provider storage path and safety scan result so private file reads and deletes do not depend on reverse-parsing remote public URLs. Media create/update/delete, replace, transform preparation, and explicit bind/unbind now emit queryable admin audit logs with request-id correlation. | Add executable provider credential setup beyond non-secret metadata, first-class DB-backed media version tables, full antivirus/provider scanning, and provider/CDN-level traffic/conversion analytics beyond Backy-served endpoint counts. |
| Editor media modal | Partial | `apps/admin/src/components/editor/MediaLibraryModal.tsx` now uses backend list/upload and scopes assets to page/post/global contexts; it accepts image/video/file/font uploads, detects font files by MIME or extension, stores default font registration metadata on font uploads, and shows font thumbnails in the picker. `CanvasEditor` hydrates site media so uploaded and registered font files appear in font controls, and the property panel exposes an upload/select font action for text elements. | Same media gaps as `/media`, plus insert sizing presets, upload progress, replace asset, crop/focal point, and direct font fallback/display editing from the picker. |
| `/users` | Partial | `apps/admin/src/routes/users.tsx` now provides a backend-backed users command center. It loads users through authenticated `GET /api/admin/users`, exposes access-readiness checks, role/status metrics, private users API snippets, handoff JSON, CSV export/import/template controls with explicit duplicate-handling policy and dry-run preview, search/role/status/access-review filters, table pagination/sorting, inline role/status updates through authenticated `PATCH /api/admin/users/:userId`, selected-row bulk status/delete actions through `POST /api/admin/users/bulk`, delete confirmation through authenticated `DELETE /api/admin/users/:userId`, role permission summaries, signed-in account self-protection locks for role/status/removal/bulk selection, membership-registration handoff links into Pages, Forms, Contacts, and Settings, and a user access activity panel backed by admin audit logs. Users APIs now enforce the saved permission matrix for `users.view`, `users.create`, `users.manage`, and `users.delete`, including explicit per-user deny overrides. `npm run test:users --workspace @backy-cms/admin` verifies the users API rejects missing auth, verifies a denied `users.create` override blocks user creation with `FORBIDDEN_PERMISSION`, creates temporary collaborators through an authenticated users API session, opens the real Users UI, verifies the invite and directory surfaces, previews a CSV import without creating users, imports one collaborator from CSV while skipping a duplicate email, imports again in update-existing mode, verifies import audit metadata for create and upsert, verifies the signed-in admin row self-protection locks, changes role and status from the directory, runs a rendered bulk status update, verifies bulk API delete and bulk audit-log readback, checks API readback, verifies the user update audit log through `/api/admin/audit-logs`, verifies the rendered Access activity panel, verifies layout/regions, captures a screenshot artifact, removes the user through the rendered confirmation modal, and verifies cleanup. | Broader auth/RBAC enforcement across every non-user admin resource, transactional invitation email delivery, workspace/team scoping, persisted granular permission resources beyond the Users surface, and richer destructive import rollback. |
| `/users/new` | Partial | `apps/admin/src/routes/users.new.tsx` now creates through `POST /api/admin/users`, with duplicate email and role validation at the API layer plus local fallback state. It includes an invite command center, readiness checks, role/status selection, access preview, API payload preview, copy/download handoff controls, and return-to-users routing with site context. Generated invite links now target `/accept-invite?token=...`, where local-demo invited accounts can accept the token, activate the user, receive an admin session, and record `user.invite.accept`. `test:users` renders the invite page, verifies the command-center/form surface, generates and accepts a detail invite token with API/user/audit readback, then covers the persisted user workflow through the API and Users directory. | Real invite email delivery, workspace scoping, and more robust browser automation coverage for the controlled form submit path. |
| `/users/$userId` | Partial | `apps/admin/src/routes/users.$userId.tsx` now loads/saves/deletes through authenticated `GET/PATCH/DELETE /api/admin/users/:userId`, with local fallback state. It exposes a user access command center, readiness checks, role/status editing, signed-in account self-protection locks, backend-loaded permission matrix from authenticated `GET /api/admin/users/:userId/permissions`, persisted per-user inherit/allow/deny overrides through authenticated `PATCH /api/admin/users/:userId/permissions`, API update handoff, lifecycle actions for active/invited/inactive/suspended, reset-email helper, invite-link generation through `POST /api/admin/users/:userId/invite-link` with admin-selected expiry, password reset token generation through `POST /api/admin/users/:userId/password-reset` with admin-selected expiry, per-user audit activity from `GET /api/admin/audit-logs?entity=user&entityId=...`, active local-demo admin session history/revocation controls from `GET/DELETE /api/admin/auth/sessions`, and guarded removal. `test:users` opens the real signed-in admin detail page and verifies self-protection locks plus protected current-session rendering, then opens a temporary user detail page, verifies the rendered permission matrix, saves a granular permission override with API readback and audit-log verification, selects invite/reset expiry options, generates and accepts an invite link with activation/session/audit readback, generates and accepts a reset link with local credential update/new-password-login/audit readback, verifies selected expiry values in audit metadata, suspends the user through the lifecycle action UI, verifies API readback, verifies the rendered detail activity timeline, and returns to the directory removal flow. | Broader route/API permission enforcement outside Users, ownership transfer flows, transactional invite/reset email delivery, and deeper audit-event filtering/detail views. |
| `/settings` | Partial | `apps/admin/src/routes/settings.tsx` now loads/saves delivery mode and regenerates API keys through `GET/PATCH/POST /api/admin/settings`, with local fallback state. Settings persist to `data/backy/admin-content.json`, admin reads include active runtime media storage, database, Supabase, and Vercel diagnostics. `POST /api/admin/settings` with `action: "validate-infrastructure"` returns non-mutating database/storage/Supabase/Vercel readiness diagnostics against draft metadata and runtime env. The Delivery tab surfaces API contracts and storage runtime state, the Infrastructure tab persists non-secret Supabase/Vercel connection metadata, shows detected environment readiness, and exposes a rendered infrastructure check result panel, and the Commerce tab now persists storefront mode, currency, payment-provider handoff metadata, checkout redirect paths, guest checkout, taxes, shipping, discounts, inventory reservation, and commerce webhook intent. This keeps the content/settings/API control plane Backy-owned while treating Supabase, Vercel, and payment providers as configurable external connectors with secrets kept outside settings. The page has moved further onto shared Backy-owned UI primitives (`Button`, `Panel`, `Notice`, `SegmentedTabs`) and includes a settings command center, frontend API capability map, downloadable/copyable handoff manifest, global design/SEO defaults, commerce handoff, notification routing, auth policy controls, API-key rotation, and settings audit history. `npm run test:settings --workspace @backy-cms/admin` opens the real admin Settings UI in headless Chrome, verifies tab navigation, changes delivery/general/appearance/SEO/infrastructure/commerce/notification/security controls, runs the infrastructure check button, verifies Supabase/Vercel provider diagnostics, saves through the backend API, confirms persisted Supabase/Vercel/storage/commerce/auth/notification metadata, checks no horizontal overflow, captures a screenshot artifact, and restores the original settings payload. Settings changes and API key regeneration emit queryable admin audit logs with redacted API-key snapshots, and the Security tab shows recent settings audit events with refresh/loading/empty/error states. | Auth/RBAC enforcement on every admin mutation, real key rotation/revocation history, billing/storage limits, secret hashing/storage policy, workspace/site scoping, actual Supabase/Vercel/payment-provider connection execution, webhook delivery/retry execution, domain/deployment history, and broader environment validation. |

## Public/API matrix

| Surface | Status | Current evidence | Required to complete |
| --- | --- | --- | --- |
| `GET /api/sites` | Partial | `apps/public/src/app/api/sites/route.ts` now returns `{ success, requestId, data }` for public site list/detail bootstrap, keeps legacy top-level `site/sites` keys for compatibility, resolves by id/slug/custom domain, and hides draft sites. `npm run test:admin-contract --workspace @backy/public` covers draft-hidden and published-resolved behavior. | Auth/rate limits where needed, richer pagination, locale/domain canonicalization, and durable data source. |
| `GET /api/sites/:siteId/pages` | Partial | Public page retrieval exists from `backyStore`; draft access requires a matching preview token instead of raw `includeUnpublished`, scheduled pages are hidden until their `scheduledAt` time passes, and page list/detail responses now return `{ success, requestId, data }` envelopes while preserving legacy top-level page keys. Published page reads now emit public contract/cache headers, ETags, conditional 304 responses, and database-mode `content` cache revisions, while preview-token reads are no-store. `GET /api/sites/:siteId/navigation` now exposes a backend-owned primary/footer navigation tree with page, internal route, external URL, and nested child items while falling back to publishable pages when no custom primary menu exists, and public navigation reads emit contract/cache headers, ETags, conditional 304 responses, and database-mode site-level cache revisions. `GET/PATCH /api/admin/sites/:siteId/navigation` now reads/writes the stored menu and validates page references before saving, and `/sites/$siteId` now provides a connected navigation-management UI. `GET/POST/PATCH /api/admin/sites/:siteId/redirects` now reads/writes stored redirect/410 rules, validates invalid rules, and previews source-route conflicts or missing internal targets without requiring a save; `/sites/$siteId` exposes connected redirect management and conflict diagnostics. `GET /api/sites/:siteId/resolve?path=/...` resolves page/blog/dynamic collection item paths plus site-level redirect and gone rules for external frontends with public contract/cache headers, ETags, conditional 304 responses, and database-mode site-level cache revisions for published reads. `npm run test:admin-contract --workspace @backy/public` covers future-hidden/past-visible scheduled pages, page/post content feed cache headers/304 behavior, configured nested navigation and URL links plus navigation cache headers/304 behavior, invalid navigation references, invalid redirect rules, redirect conflict preview diagnostics, page/post route resolution cache headers/304 behavior, redirect/410 route resolution, and collection dynamic item route resolution. | Complete SEO/meta payload, token auditing, and broader contract tests. |
| `GET /api/sites/:siteId/render` | Partial | `apps/public/src/app/api/sites/[siteId]/render/route.ts` returns the new AI/custom-frontend render payload for pages, blog posts, and generated collection dynamic list/item pages, and supports token-gated draft page preview. Render payloads now include a `navigation.primary` manifest derived from publishable pages, public media, uploaded public font assets, plus normalized collection dataset manifests, resolved public collection fields/records, server-resolved element prop values, and collection-record editable map entries when canvas elements declare collection `dataBindings`, hydrated `repeater` element datasets/records, or generated dynamic list/item payloads. Published render responses include short public cache headers, ETag/304 revalidation, and Backy contract/schema/request/site metadata headers, while preview/error responses are no-store. `npm run test:admin-contract --workspace @backy/public` validates page, blog, collection-bound page, repeater hydration, and dynamic list/item render responses against `specs/ai-frontend-contract/content-payload.schema.json`, checks uploaded font asset manifests, checks bound collection values resolve into element props, covers public post render payloads, checks scheduled post render guards, and verifies render cache/contract headers plus conditional 304 responses; `npm run test:preview-contract --workspace @backy/public` covers draft blocking and tokenized page render/page/hosted preview. | Version negotiation, custom dynamic template routes, cache invalidation events, stable SDK examples, CI wiring, and token auditing. |
| `GET /api/sites/:siteId/seo` | Partial | Public SEO discovery now lists publishable page, blog post, and dynamic collection item routes with canonical paths, absolute `canonicalUrl` values, title/description, robots flags, Open Graph basics, route-specific JSON-LD objects, keywords, sitemap priority/change frequency, and updated timestamps. Site SEO defaults now persist via `GET/PATCH /api/admin/sites/:siteId/seo`, are editable from `/sites/$siteId`, and are applied to route titles, fallback descriptions, Open Graph images, site-level JSON-LD defaults, discovery defaults, sitemap enablement/default priority/default frequency/dynamic-route inclusion, and robots index/follow/custom directives. Admin SEO responses and the site UI now show supported title variables plus sample dynamic collection list/item route previews using real collection and record values. Page settings can edit route-specific JSON-LD arrays, and page/post render payload SEO merges site-level and route-level JSON-LD for custom/default frontend head rendering. `format=sitemap` emits XML from indexable routes according to stored sitemap settings and `format=robots` emits robots text from stored robots settings; custom-domain sites expose `https://custom-domain/...` canonical URLs and custom-domain sitemap/robots public URLs while keeping path canonicals for API compatibility. Cacheable public contract JSON responses emit `x-backy-cache-revision`, SEO sitemap/robots responses carry the same SEO revision, and SEO JSON supports ETag/304 revalidation against that revision seed. `@backy/db` now includes a durable `cache_invalidation_events` table and repository/factory contract for scoped invalidation revisions, and database-mode admin SEO, navigation, redirect, page, blog post, collection, collection record, media, and media folder mutations record scoped invalidation events. Hosted Backy sites expose crawlable `/sites/:siteSlug/sitemap.xml` and `/sites/:siteSlug/robots.txt` from the same route index/settings. Site readiness now flags duplicate page/post canonical paths as publish-blocking SEO errors. Site settings now persist redirect/410 route rules consumed by public route resolution; admin redirect read/save/preview responses include route-shadowing and missing-target warnings for safer SEO routing. Manifest/OpenAPI/SDK surfaces advertise and consume the endpoint, and `test:admin-contract` verifies JSON, API sitemap/robots, hosted sitemap/robots, SEO/manifest/OpenAPI/render cache revision and 304 behavior, manifest, OpenAPI, SEO defaults, dynamic SEO previews, custom-domain canonical URLs, JSON-LD defaults, route JSON-LD, sitemap/robots settings, canonical conflict readiness errors, redirect conflict previews, and redirect/410 resolver coverage; `test:repositories` verifies the invalidation repository contract. | Propagate revision headers to non-contract public feeds. |
| `GET /api/sites/:siteId/media` | Partial | Public media list exists, only returns public catalog entries, supports type/scope/page/post/search/tag/folder pagination filters for custom frontends, and now returns a `{ success, requestId, data }` envelope while preserving legacy top-level `media/pagination`. Public image media includes responsive `srcset` manifests that prefer stored WebP variants generated by the admin transform-prep endpoint. `GET /api/sites/:siteId/media/:mediaId/file` serves public files directly and private files only with a valid temporary signed token from the admin signing endpoint while recording Backy-served file request/byte analytics; `GET /api/sites/:siteId/media/:mediaId/transform` validates public image transforms, records transform request analytics, and redirects to the optimizer as an on-demand fallback; `test:admin-contract` covers unsigned private blocking, signed private delivery, generated image variants, image transform redirects, delivery analytics, and unsupported transform errors. | Editable metadata/reference browsing from custom frontend workflows where needed, provider/CDN traffic analytics, and provider configuration UI. |
| `GET/POST/PATCH/DELETE /api/admin/sites/:siteId/media` | Partial | Admin media upload/list/update/delete/folder endpoints store files through the environment-selected `@backy/storage` adapter (`local`, `s3`, or `supabase`) and metadata under `data/backy/media-library.json`; list filters support type, visibility, search, tag, and folder; font uploads/register edits persist family/weight/style/fallback/display metadata; `POST /api/admin/sites/:siteId/media/:mediaId` replaces an asset file while keeping the media id stable and retaining prior file metadata in `metadata.replacementVersions`; `POST /api/admin/sites/:siteId/media/:mediaId/transforms` generates stored WebP responsive image variants and records byte/provider metadata; content saves sync page/post usage references; explicit media bind/unbind route updates page/post references and binding metadata; uploads/replacements/transform generation enforce per-site quota, run static upload safety checks, reject active-content SVG payloads, and return quota usage metadata; media create/update/delete/replace, transform preparation, folder create/update/delete, and bind/unbind emit admin audit logs covered by `test:admin-contract`. | Auth/RBAC, durable database, provider configuration UI, full antivirus/provider scanning, and first-class media version tables. |
| Collection APIs | Partial | `GET/POST/PATCH/DELETE /api/admin/sites/:siteId/collections` and nested `/records` endpoints now persist collection schemas and records to `data/backy/admin-content.json`. Public endpoints under `GET /api/sites/:siteId/collections` expose only published/public-readable collections and publishable records, include select/tag option metadata plus reference targets in field schemas, return `{ success, requestId, data }` envelopes while preserving legacy top-level collection/record keys, emit public contract/cache headers with ETags, support conditional 304 responses, and attach database-mode `content` cache revisions from invalidation events. Public/admin record lists support basic search, field-value filtering, sorting, limit, offset, and pagination metadata. Public `POST /api/sites/:siteId/collections/:collectionId/records` respects `permissions.publicCreate`, validates submitted values, stores visitor-created records as draft, and returns private/no-store contract responses. The admin record list endpoint also serves the same filtered result set as CSV via `format=csv`, `/records/bulk` handles selected-record status updates and deletes, and `/records/import` accepts CSV for schema-validated create/upsert, including select/tag option validation. Collection-bound canvas elements, repeater elements, and generated collection list/item pages now surface dataset, field, field option/reference, record, and binding metadata through `/render`; public `/:collectionSlug` and `/:collectionSlug/:recordSlug` routes resolve after normal page/blog routes, and admin writes reject page/collection route conflicts before ambiguous URLs are saved. The admin `/collections` page now exposes schema CRUD, public read/create controls, field option/reference editors, record CRUD, record search/filter/sort/pagination/bulk controls, and CSV import/export against these endpoints, and the editor property panel can attach collection field bindings to selected canvas elements. `npm run test:admin-contract --workspace @backy/public` covers collection CRUD, required/unique/option field validation, public collection feed cache headers/304 behavior, public visibility guards, public record reads/search/filter/sort, public create disabled/allowed behavior, draft moderation for visitor-created records, admin CSV export/import, admin record pagination metadata, bulk record status/delete actions, partial record updates, collection-bound render payload hydration, repeater dataset hydration, dynamic list/item route/render payloads, hosted collection route rendering, and cleanup. | DB-backed indexes/querying, richer dataset query/sort/repeater UI, custom dynamic list/item templates, broader visitor-write policies, backups, permissions/RBAC, and audit events. |
| Reusable section APIs | Partial | `GET/POST/PATCH/DELETE /api/admin/sites/:siteId/reusable-sections` now persists reusable canvas sections to `data/backy/admin-content.json`, and public `GET /api/sites/:siteId/reusable-sections` plus detail reads expose active saved sections for published sites with public contract/cache headers, ETags, conditional 304 responses, and database-mode `content` cache revisions. Database-mode reusable-section mutations record scoped invalidation events. Saved sections keep normal `content.elements`/`canvasSize`, support category/tag/search/status filters where appropriate, validate required non-empty content, reject duplicate slugs, support optimistic conflict guards and bounded version history, restore older versions, export/import portable JSON with upsert history preservation, structured library metadata editing, and discover/bulk-refresh synced page/blog instances while preserving instance placement. Admin reusable-section list/detail/version/export/instance reads enforce `pages.view`, create/update/import/restore/metadata/instance-refresh mutations enforce `pages.edit`, and deletes enforce `pages.delete`. Reusable-section create/update/metadata-update/delete/restore/import/instance-propagation emits queryable admin audit logs covered by `test:admin-contract`. The editor library lists active saved sections, saves the selected element tree through `POST`, renames/deletes saved entries, refreshes the saved-section list, inserts saved sections as synced canvas instances with source metadata, refreshes a selected synced instance from its saved source, and detaches instances into independent editable copies while still storing concrete canvas trees for public rendering. Manifest/OpenAPI/SDK surfaces advertise and consume reusable section reads. `npm run test:admin-contract --workspace @backy/public` covers missing-auth rejection, create/list/detail/update/archive/delete plus duplicate and invalid-content errors, structured metadata read/update, version conflict/history/restore, export/import/upsert, synced instance discovery/refresh, reusable-section audit logs, public list/detail cache headers/304 behavior, and manifest/OpenAPI exposure; SDK smoke covers public reusable-section reads; editor drag smoke now covers synced reusable-section insert/source-refresh/detach. | Add full template registry contracts, move persistence to DB, and extend RBAC enforcement to the rest of the admin surface. |
| Sites/pages admin APIs | Partial | `GET/POST/PATCH/DELETE /api/admin/sites` and `/api/admin/sites/:siteId/pages` now persist to `data/backy/admin-content.json`. Page publish/archive/rollback/revisions, `scheduledAt`, and preview token endpoints exist. The public API proxy now allows the local admin dev origin so the Vite admin can call the Next backend without CORS failures and stamps admin API responses with `Cache-Control: no-store`, `x-backy-cache-scope: admin`, request ids, and `x-backy-admin-contract-version: backy.admin.v1`. `npm run test:admin-contract --workspace @backy/public` covers temporary site CRUD, page create/list/detail/update/delete, scheduled page visibility, the admin-origin CORS preflight/read path, and global admin response headers for every admin JSON call exercised by the smoke. `npm run test:sites --workspace @backy-cms/admin` now covers the site creation command surface, API-backed temporary site creation, rendered multi-site command center/API/feature-contract regions, UI status publishing with API readback, delete confirmation, layout checks, screenshot capture, and cleanup. | Full session auth/RBAC, durable database, cache invalidation, audit events, production origin policy, and broader contract tests. |
| Blog admin APIs | Partial | `GET/POST/PATCH/DELETE /api/admin/sites/:siteId/blog` now persists posts to `data/backy/admin-content.json`. Post publish/archive/rollback/revisions, `scheduledAt`, and preview token endpoints exist. Category and tag CRUD endpoints exist under `/api/admin/sites/:siteId/blog/categories` and `/api/admin/sites/:siteId/blog/tags`; database-mode category/tag mutations record scoped invalidation events. Author list endpoints exist under `/api/admin/sites/:siteId/blog/authors`; public taxonomy/author list endpoints and post filters also exist, public search/archive filters accept `q`/`search` plus `year`/`month`, and public category/tag/author/search/archive feeds emit contract/cache headers with ETags and conditional 304 responses. Admin blog list/detail/readiness/revision/author/taxonomy reads enforce `pages.view`, create/update/archive/rollback enforce `pages.edit`, publish/preview-token creation enforce `pages.publish`, and deletes enforce `pages.delete`. `npm run test:admin-contract --workspace @backy/public` covers missing-auth rejection, temporary blog category/tag CRUD, taxonomy feed cache headers/304 behavior, author resources, post assignment/filtering, and scheduled post visibility; `test:blog-list` covers public search/archive feed delivery. | Durable database, audit event UI, and broader contract tests. |
| Users admin APIs | Partial | `GET/POST/PATCH/DELETE /api/admin/users` now persists team users to `data/backy/admin-content.json`; `POST /api/admin/users/import` imports CSV rows with full-name/email/role/status validation, duplicate-email skip or explicit upsert mode, dry-run preview counts with no writes/audit mutation, `users.create` plus `users.manage` permission enforcement for upserts, current-user protection, last-active owner/admin safeguards, and queryable create/upsert import audit events. `POST /api/admin/users/bulk` supports selected-user status updates and deletes with current-user protection, last active `owner`/`admin` safeguards, `users.manage`/`users.delete` permission enforcement, and queryable bulk audit events. User role/status normalization now preserves `owner` and `suspended`, the detail endpoint prevents deleting, demoting, or deactivating the last active `owner`/`admin`, and create/update/delete mutations emit queryable admin audit logs with before/after snapshots, request IDs, role/status/email metadata, and changed-field hints. Local-demo admin sessions can now be listed and non-current sessions can be revoked through `GET/DELETE /api/admin/auth/sessions` with bearer-session authorization and current-session protection. Local-demo invite tokens can be generated through `POST /api/admin/users/:userId/invite-link` with validated expiry minutes, accepted through public `POST /api/admin/auth/accept-invite`, consumed on success, and audited with selected expiry metadata. Local-demo password reset tokens can be generated through `POST /api/admin/users/:userId/password-reset` with validated expiry minutes and accepted through public `POST /api/admin/auth/reset-password`, which consumes the token, updates a hashed local credential, starts a session, and records `user.password_reset.accept`. `npm run test:admin-contract --workspace @backy/public` covers list/create/duplicate-email/update/delete plus the last-admin safeguard. `npm run test:users --workspace @backy-cms/admin` now covers API user creation, CSV import dry-run preview, duplicate skips, update-existing mode, permission-denial enforcement, invite generation and acceptance, reset generation and acceptance, selected invite/reset expiry audit metadata, new-password login, UI self-protection locks, active session rendering, UI directory role/status updates, bulk status updates, bulk API deletion, user update/import/bulk audit-log API readback, rendered activity panel, detail lifecycle status updates, delete confirmation, API readback, layout checks, and cleanup. | Full session auth/RBAC on every admin resource, workspace scoping, real invite/reset email delivery, pagination, destructive import rollback, and broader contract tests. |
| Settings admin APIs | Partial | `GET/PATCH/POST /api/admin/settings` now persists delivery mode, API keys, auth policy, global design/SEO/notification settings, commerce behavior settings, and non-secret Supabase/Vercel/storage integration metadata to `data/backy/admin-content.json`; it returns non-secret `runtimeStorage`, `runtimeDatabase`, `runtimeSupabase`, and `runtimeVercel` diagnostics for the active runtime. Settings update and API-key regeneration emit redacted admin audit logs covered by `test:admin-contract`. `npm run test:admin-contract --workspace @backy/public` covers read/update validation/key regeneration, runtime diagnostics, integration metadata persistence, and no secret exposure in runtime summaries. `npm run test:admin-auth --workspace @backy/public` verifies that a key-enforced server rejects missing/wrong admin API keys and accepts `X-Backy-Admin-Key` plus bearer auth. `npm run test:settings --workspace @backy-cms/admin` covers the admin UI save path and readback for delivery/general/appearance/SEO/infrastructure/commerce/notification/security settings. The broader admin contract smoke can also target a key-enforced server and will send the configured key on admin requests. | Full session auth/RBAC, key rotation/revocation history, secret hashing/storage policy, workspace/site scoping, webhook delivery/retry execution, executable provider configuration beyond non-secret metadata, and broader contract tests. |
| Forms/Contacts APIs | Partial | Form definitions, submissions, contacts, spam checks, CSV-like admin exports, and audit events exist in `apps/public/src/lib/backyStore.ts`. Public form list/detail, definition, submission list/detail/create/review, and contact list/status routes now return `{ success, requestId, data }` envelopes while preserving legacy top-level fields where applicable. Public form list reads and `GET /api/sites/:siteId/forms/:formId/definition` emit contract/cache headers with ETags and conditional 304 responses; form detail, submission list/detail/create/review, and contact list/update routes use contract headers but remain private/no-store because they currently include submission/contact visitor data. Admin forms now support direct `POST /api/admin/sites/:siteId/forms` creation plus `GET/PATCH/DELETE /api/admin/sites/:siteId/forms/:formId` detail/update/deletion in both repository-backed and demo fallback modes, with fallback form definitions persisted into `admin-content.json`; deletion also removes related fallback submissions and contacts. Required checkbox consent fields and authored validation rules validate correctly for public submissions. Canvas-derived forms and standalone admin-created forms can define collection write targets and field maps; accepted public form submissions can create draft collection records after public-create permission and collection schema validation, including normalized source-submission provenance fields when present, and contact-share submissions create/update private contacts. Admin contact list/create/update/import endpoints are exercised from `/contacts`, including direct JSON create, CSV import with email upsert, full contact field updates for merge operations, and rendered duplicate merge UI contracts. `GET /api/admin/sites/:siteId/forms/contact-segments` now returns backend contact segment analytics for all site forms or a selected form across demo and repository modes. `GET/POST/DELETE /api/admin/sites/:siteId/forms/contact-lists` now persists saved contact filter lists under site settings, returns matched contact counts and IDs, and records audit events in demo and repository modes. `POST /api/admin/sites/:siteId/forms/:formId/contacts/:contactId/promote` now promotes qualified contacts into Backy users with optional invite-link generation and persisted contact promotion metadata. `POST /api/admin/sites/:siteId/forms/:formId/contacts/:contactId/promote-customer` now promotes qualified contacts into private customer collection records with persisted contact metadata. `POST /api/admin/sites/:siteId/forms/:formId/contacts/sync` now sends selected contacts to an admin-provided webhook and records `contact-sync` delivery events. `POST /api/admin/sites/:siteId/forms/:formId/contacts/consent-retention` now exports selected contact consent evidence and applies due consent/source-IP anonymization. `test:admin-contract` covers the temporary page/form/collection/contact flow plus form list and form-definition cache headers/304 behavior and private submission/contact cache headers. The frontend manifest/OpenAPI and SDK now expose the cacheable form definition URL and `formDefinitionCached()` helper. Form delivery events can be queried through `GET /api/sites/:siteId/events?kind=form-submission&formId=:formId`; notification email sends support local-outbox, generic HTTP handoff, Resend, and SMTP provider modes with queued/succeeded/failed events. Failed submission webhooks can be replayed through `POST /api/admin/sites/:siteId/forms/:formId/submissions/:submissionId/webhook-retry`, and `/forms` now renders delivery history plus retry controls. `/forms` also detects consent-like checkbox fields and exports a dedicated consent CSV with status, request, timestamp, source, policy label, retention/delete due dates, privacy request email, and optional IP hash/user-agent provenance. `/forms` now persists per-form spam timing, rate-limit, duplicate-window, blocked-term controls, and consent retention/export policy controls; admins can apply due-consent anonymization for consent values plus IP/user-agent provenance, and admin automation can run the same policy site-wide; public submissions enforce spam rules before persistence or delivery. Captcha-enabled public forms verify Turnstile, hCaptcha, reCAPTCHA, and generated frontend token aliases before persistence or delivery. Form admin mutations and review/retention workflows now emit request-id audit events rendered on `/forms`, and form admin routes enforce dedicated `forms.*` permission keys with override coverage. `supabase/migrations/002_forms_contacts_persistence.sql` adds durable Supabase/Postgres tables, policies, and indexes for forms/submissions/contacts, while `npm run test:repositories --workspace @backy/db` covers repository create/update/list/detail/delete behavior for form definitions, submissions, contacts, and spam/consent settings JSON persistence. `npm run test:forms --workspace @backy-cms/admin` covers denied `forms.create` override enforcement, admin template creation, form-builder update persistence, authored validation rule persistence, standalone form collection mapping persistence, per-form spam and consent settings persistence, consent retention anonymization including the site-wide automation endpoint, public blocked-term spam rejection, public definition readback, invalid public registration rejection, valid public registration submit, collection record creation, configured email delivery events, consent export UI, webhook failure/retry event history through the rendered Forms UI, UI moderation, rendered audit activity, API readback, layout checks, and cleanup. `npm run test:contacts --workspace @backy-cms/admin` covers contact direct creation, CSV import, contact-share creation, backend contact segment analytics, saved contact-list API create/render/delete, contact-to-user promotion, contact-to-customer promotion, selected contact webhook sync, contact consent-retention export/anonymization, permission/audit panel rendering, duplicate-email merge, private contact listing, notes/status update through the Contacts UI, API readback, layout checks, and cleanup. | Add a database-mode integration smoke against a configured Supabase/Postgres service. |
| Comments APIs | Partial | Page/blog comments, reports, moderation filters, site policy, analytics, and report reasons exist. Public page/blog comment submit/list/detail/update endpoints, site-wide comment list/detail/update/report routes, comment analytics, event listing, and report-reason discovery now return `{ success, requestId, data }` envelopes while preserving legacy top-level comment/reason/event keys. Report-reason discovery emits public contract/cache headers, ETags, conditional 304 responses, and cache revisions; comment list/detail/moderation/report submission, analytics, and interaction-event routes emit contract headers with `private`/`no-store` cache scope because they can include visitor identity, actor, and moderation state. Page/blog comment submission now respects persisted site comment policy for open/closed state, default moderation, guest/name/email/reply rules, and blocked terms; report submission respects the policy report toggle. The frontend manifest exposes normalized `site.commentPolicy` for custom frontend builders. The site-wide moderation PATCH route now supports `action: "clearReports"` / `clearReports: true` in demo and repository modes so report triage can resolve flags without changing the comment status, and the admin API helper now forwards `commentThreadId` filters for thread-specific moderation handoffs. Admin-authored official replies are submitted through the page/blog comment API with parent/thread IDs, admin request IDs, and reviewer-selected moderation mode. Single-comment PATCH now also supports moving replies by updating `parentId`/`commentThreadId` with same-target parent validation in demo and repository modes. `GET /api/sites/:siteId/comments/analytics` provides authenticated 30-day/all-time totals, status counts, report reasons, thread metrics, top targets, and daily submitted/reviewed/reported trends in demo and repository modes. Comment delivery/activity events are now consumed from `GET /api/sites/:siteId/events?kind=all` by the `/comments` admin panel so admins can see submission, report, moderation, and thread-update handoffs beside the moderation queue. `test:admin-contract` submits an approved page comment and verifies target comments, site-wide comments, detail/update/report routes, report reasons, report-reason cache headers/304 behavior, private comment/event route cache headers, and audit events through the public envelopes. `npm run test:comments --workspace @backy-cms/admin` now covers policy UI persistence, public blocked-term rejection, public pending page comment submission, parent/reply thread creation and UI context, analytics API counts, site-wide comment listing, public report submission, rendered comment delivery activity from the events API, UI-created official admin reply readback, UI reply reparenting readback, UI-driven approve/reject/report-resolution actions, API readback, layout checks, and fallback cleanup after deleting the temporary page. | Durable persistence, executable outbound comment notifications/webhooks, anti-spam provider hooks, RBAC/audit-event UI, and DB-backed moderation cleanup coverage. |
| Blog APIs | Partial | Public blog routes exist; draft detail access requires a matching preview token instead of raw draft status access, scheduled posts are hidden until their `scheduledAt` time passes, and blog post list/detail plus taxonomy/author endpoints now return `{ success, requestId, data }` envelopes while preserving legacy top-level keys. Public category/tag/author list endpoints and `categoryId/categorySlug/tagId/tagSlug/authorId/authorSlug`, `q`/`search`, and `year`/`month` blog filters now exist. `GET /api/sites/:siteId/render?path=/blog/:slug` returns a post-shaped AI/custom-frontend render payload with post comment interactions. `npm run test:preview-contract --workspace @backy/public` covers draft post blocking and tokenized post API preview; `npm run test:admin-contract --workspace @backy/public` covers public taxonomy/author list/filter smoke plus future-hidden/past-visible scheduled posts and post render payloads; `test:blog-list` covers public search/archive feeds. | RSS/sitemap, token auditing, and broader contract tests. |
| Public hosted pages | Partial | `apps/public/src/app/sites/[subdomain]/[[...path]]/page.tsx` renders page content, supports token-gated draft page previews, resolves collection-bound page element values before rendering, renders generated collection item pages for published `/:collectionSlug/:recordSlug` records, and applies site-level redirect/gone settings before normal page resolution. `apps/public/src/app/sites/[subdomain]/blog/[slug]/page.tsx` renders published posts, resolves collection-bound post element values, and supports token-gated draft post previews with preview metadata marked noindex. Hosted sitemap/robots route handlers now publish crawlable SEO files for each published Backy site. `GET /api/sites/:siteId/resolve` exposes page/blog/dynamic item/redirect/gone publish guards for external frontends. `npm run test:preview-contract --workspace @backy/public` covers hosted page/post preview smoke. Public rendering now skips elements marked `visible: false` by the editor layer controls. | Full hosted-route reuse of the resolver, theme inheritance, responsive rendering guarantees, deployment/cache policy, custom dynamic item templates, and richer blog archive templates. |
| Frontend manifest/OpenAPI/SDK APIs | Partial | `GET /api/sites?identifier=:identifier` and `GET /api/sites` provide contract/cache-aware public site discovery with ETags, conditional 304 responses, and site metadata headers for identifier lookups. `GET /api/sites/:siteId/manifest` exposes a schema-validated site discovery document for external frontends, including site identity, theme tokens, schema references, capability flags, route patterns, redirect/gone route metadata, configured navigation, public endpoint URLs, SEO/sitemap/robots URLs, pages, blog taxonomy/author summaries, collection schemas/routes, reusable section summaries, form submit/detail/submission/contact URLs, page/blog comment URL templates, comment report URLs, interaction event URLs, collection targets, media/font counts, and a Settings-derived commerce storefront contract for catalog/order-intake/checkout UI decisions. Site frontend design contracts can be read/saved/captured through `GET/PATCH/POST /api/admin/sites/:siteId/frontend-design` with `sites.view` for reads and `sites.configure` for writes; create-page/create-blog/forms/collections/products/reusable-section workflows can preserve frontend design template provenance, and frontend-design contract saves/captures now emit queryable `frontendDesign.update`/`frontendDesign.capture` audit logs with database-mode settings cache invalidations. `GET /api/sites/:siteId/openapi` exports a site-scoped OpenAPI 3.1 document for discovery, resolve, render, SEO discovery, navigation, media, collection, reusable-section, form detail/submission/contact, page/blog/site comment, comment report, report reason, and interaction event operations, including typed redirect/gone route schemas and `ROUTE_GONE` 410 responses. Published discovery responses include short public cache headers, ETag/304 revalidation, and Backy contract/schema/request/site metadata headers, while hidden/error discovery responses are no-store. `packages/sdk-js` now provides a TypeScript client and exported public contract shapes for render payloads, typed route results including redirect/gone, content documents/elements, navigation items, SEO routes, media/font assets, collection schemas/records, reusable sections, forms/submissions/contacts, comments, events, commerce storefront settings, response metadata, and conditional 304 results. The SDK also exposes `manifestCached`, `openapiCached`, and `renderCached` helpers so generated/custom frontends can use Backy's ETags without raw `fetch`, and `resolve()` returns non-throwing gone envelopes for 410 routes while ordinary errors still throw. `npm run test:smoke --workspace @backy/sdk-js` validates site discovery, manifest, OpenAPI, cached manifest/OpenAPI/render 304 revalidation, resolve, redirect/gone route handling, render, SEO discovery, configured navigation, media, reusable sections, forms, site comments, events, public collection record creation, form submission/review/contact updates, page comment submit/detail/update, comment reports, and report event reads against the local public app. `test:admin-contract` validates the manifest against `frontend-manifest.schema.json` and verifies key OpenAPI paths/schemas, routing metadata, navigation payloads, SEO/reusable-section/commerce discovery, cache/contract headers, conditional 304 responses, plus draft-site visibility guards; `npm run test:frontend-design --workspace @backy-cms/admin` validates missing-auth rejection, frontend-design persistence, manifest exposure, template-seeded page/blog content, capture, and audit readback. | Generate SDK types directly from OpenAPI/schema instead of maintaining handwritten starter types, domain/locale variants, auth-aware capability flags, fuller component schemas, CI wiring for SDK smoke, and DB-backed service data. |

## Immediate implementation order

1. **Real admin persistence API**: add authenticated admin endpoints for sites/pages/posts/users/settings and move admin store writes behind API helpers.
2. **Editor completion pass**: layers panel, snapping/alignment, undo/redo hardening, responsive canvas modes, composed presets, saved reusable sections, preview/publish, and revision autosave.
3. **Media management completion**: public media feeds now expose cache revision headers/ETags and admin asset details expose queryable activity, replacement history, library usage analytics, generated responsive image manifests, Backy-served delivery counts, and static upload safety-scan state; remaining work is editable provider configuration, full malware/provider scanning and quarantine, first-class DB-backed media version tables, and provider/CDN-level traffic/conversion analytics.
4. **Publishing model**: add cache invalidation, preview token audit events, and broaden hosted preview tests into the main CI path.
5. **Backend validation/test suite**: broaden endpoint contract tests (`test:preview-contract`, `test:admin-contract`), editor serialization tests, render payload schema validation, and smoke tests for every admin page.

## Product backlog notes

- **Reusable admin UI system**: continue moving every page onto shared Backy-owned primitives for buttons, panels, pills, tabs, notices, empty states, tables, and form controls before adding page-local styling.
- **Blog/page editor organization**: the blog editor now uses a shared workspace frame and better grouped toolbar controls, and the shared page editor can persist desktop/tablet/mobile responsive overrides, but the canvas, component rail, layers/properties panels, page size controls, autosave, preview, and post settings still need a deeper organization pass so users can freely design complex pages without the surface feeling cramped or scattered.
- **Registration/form backend**: registration templates can now create standalone backend forms, standalone forms can be edited from `/forms`, authored validation rules persist into public definitions, standalone form collection binding is configurable from the admin form surface, per-form notification email/webhook targets, spam timing/rate/duplicate/blocked-term controls, captcha provider verification, consent retention/export policy controls can be configured in the form builder, due consent evidence can be anonymized from the Forms panel, and the same retention policy can run site-wide from admin automation, form notification emails create provider-ready/local-outbox/generic HTTP/Resend/SMTP delivery events, failed form submission webhooks and failed email notifications can be retried from the rendered Forms delivery panel, rendered consent export CSV is available for consent-like fields with policy/retention/deletion metadata, reusable embed blocks preserve frontend design context, request-id audit activity is rendered on `/forms`, form admin APIs enforce first-class `forms.*` permissions, backend contact segment analytics are available for site-wide or form-scoped lead lists, saved/custom lead lists are backed by `/forms/contact-lists`, qualified contacts can be promoted to Backy users and private customer records, and the public registration validation/spam/captcha/submit/email/webhook-delivery/retry/collection-write/moderation loop is covered by `test:forms`; form definitions/submissions/contacts have durable Supabase/Postgres migration coverage and repository persistence smoke coverage. Remaining work is a real database-mode integration smoke against a configured Supabase/Postgres service.
- **Contacts/lead backend**: contact-share submissions now have a dedicated `/contacts` smoke covering private contact list, direct contact creation, CSV import with email upsert, backend contact segment analytics, saved contact-list API create/render/delete, contact-to-user promotion, contact-to-customer promotion, selected contact webhook sync, contact consent-retention export/anonymization, permission/audit panel rendering, duplicate-email merge/dedupe UI, notes, individual lifecycle status, rendered bulk lifecycle actions, API readback, and cleanup; `test:repositories` now covers the repository-backed contact workflow for filtering, exact-ID updates, merge/promotion metadata, retention anonymization, and contact audit logs. No Contacts-specific backend blocker remains; broader Supabase/Postgres service smoke remains tracked under Forms/Contacts APIs.
- **Comments/moderation backend**: `/comments` now has a dedicated smoke covering site policy save/readback, public blocked-term rejection, public pending page comments, parent/reply thread creation and rendered thread context, analytics endpoint/panel counts, public report submission, rendered comment delivery activity from the events API, official admin reply authoring with parent/thread API readback, reply reparenting with parent/thread API readback, UI approval/rejection/report-resolution/blocking, author blocklist creation/removal, API readback, layout checks, default policy restoration, and fallback cleanup; remaining work is executable outbound notifications/webhooks, anti-spam provider hooks, RBAC, and DB persistence.
- **Users/access backend**: `/users`, `/users/new`, and `/users/$userId` now have smoke coverage for missing-auth rejection on the users API, authenticated API creation, CSV import dry-run preview, duplicate skips and update-existing mode, invite/directory/detail rendering, signed-in admin self-protection locks, active local-demo session history, backend-loaded permission matrix rendering, invite-link and password reset token generation with selected expiry and audit-log readback, role/status updates, rendered selected-user bulk status updates, bulk delete API behavior, user/import/bulk audit-log readback, rendered directory and detail access activity, lifecycle suspension, delete confirmation, API readback, layout checks, and cleanup; remaining work is real auth/RBAC across all non-user admin resources, transactional invite/reset email delivery, workspace scoping, persisted granular permission overrides, and destructive import rollback.
- **Sites/multi-site backend**: `/sites` and `/sites/new` now have smoke coverage for the site creation command surface, UI-driven blueprint creation, starter page API readback, UI status publishing, search/status/domain/page-coverage filters, duplicate-as-draft, quick archive, frontend API/feature-contract regions, typed delete confirmation, layout checks, screenshot capture, and cleanup; remaining work is real auth/RBAC, workspace/team ownership, domain verification, Vercel deploy orchestration, per-site billing/quotas, audit events, and DB-backed production persistence.
- **Media/file backend**: `/media` now has smoke coverage for folder organization, public image/private file upload, rendered central upload/API/storage/bulk/library surfaces, stable-ID replacement with retained version history, responsive variant preparation, metadata save, visibility changes, signed URL generation, delete confirmation, API readback, layout checks, and cleanup; remaining work is editable provider setup UI, malware/quarantine workflows, first-class media version tables, CDN/provider analytics, richer crop/focal controls, and RBAC/audit-event UI.
- **Commerce/products**: product and private order primitives, schema sync, public catalog API, checkout-intake API, inventory reservation, admin product/order workspaces, product and order CSV export/import/template controls, typed collection CSV import, smoke coverage, and Settings-level commerce behavior/provider handoff metadata now exist. Settings now persists payment provider test/live mode, webhook endpoint/secret-reference metadata, webhook event allowlists, and reconciliation mode/window, while public contracts expose only non-secret readiness and policy fields. Remaining parity work is executable payment-provider session creation/reconciliation workers, taxes, discounts, shipping, refunds/returns, subscriptions, richer product templates/listing pages, customer profiles, notifications, analytics, and actual provider webhook processing.

## Current conclusion

Backy has meaningful scaffolding for a CMS/editor and now has stronger media and render-contract foundations, but most admin pages are still **Prototype** or **Partial**. The next milestone should be replacing local admin CRUD with real backend APIs before adding more visual features, otherwise new Wix/Canva-style UI controls will keep saving into a non-production data model.

# Backy page and backend completeness audit

Date: 2026-05-10

This audit tracks whether each Backy admin/public surface has the necessary backend and editor behavior expected from a WordPress/Wix/Canva-like product. It is intentionally strict: a page is not complete until it persists through backend APIs, exposes the right public/admin contract, handles loading/empty/error states, and supports the workflows users expect from a production CMS/editor.

## Completion legend

- **Ready**: backend-backed, validated, user-facing behavior is complete for the current product scope.
- **Partial**: usable path exists, but important backend/product behavior is missing.
- **Prototype**: mostly local/mock UI or incomplete workflow.
- **Missing**: route or workflow is absent.

## Product-level gates

Backy should not be called complete until these gates are satisfied:

- **Durable backend**: sites, pages, posts, users, settings, media, forms, comments, events, and publish state are stored in the same durable backend, not split between seed data, localStorage, and local JSON files.
- **Admin APIs**: every admin page has authenticated CRUD endpoints with validation, pagination, RBAC, audit trails, and consistent `{ success, requestId, data/error }` envelopes.
- **Public render contract**: custom frontends can fetch exact site/page/post data, theme tokens, assets, actions, bindings, SEO, and editable maps without relying on admin implementation details.
- **Editor parity**: page/post editing covers Canva/Wix expectations: drag/resize, selection, layers, snapping/alignment, grouping, undo/redo, responsive breakpoints, media/font insertion, template sections, preview, publish, and revision history.
- **WordPress parity**: pages/posts/media/users/settings support drafts, publish/schedule, slugs/permalinks, SEO, revisions, categories/tags where relevant, comments/moderation, forms, roles, and export/import hooks.
- **Operational readiness**: auth, permissions, rate limits, storage quotas, telemetry, backups, webhook retries, tests, and deployment configuration are documented and verified.

## Admin page matrix

| Surface | Status | Current evidence | Required to complete |
| --- | --- | --- | --- |
| `/login` | Partial | `apps/public/src/app/api/admin/auth/login/route.ts`, `/session`, `/logout`, `/password-recovery`, `/accept-invite`, and `/reset-password` now provide backend-backed local admin authentication. `apps/admin/src/stores/authStore.ts` signs in through the admin auth API, accepts invite tokens, resets local-demo passwords, persists the returned session token/user, can validate an existing session, and revokes the session on logout. The login page now requests password recovery through the backend auth API instead of checking local hard-coded users in the browser, queues password-reset email through Backy's shared transactional provider stack with provider/status metadata while keeping account lookup responses neutral, `/accept-invite?token=...` is a public route that activates pending invited users and starts a session, and `/reset-password?token=...` lets admins set a new local-demo password from a generated reset token. `npm run test:login --workspace @backy-cms/admin` covers invalid credentials, API-backed local recovery with queued reset delivery metadata, successful sign-in, persisted session token, and dashboard redirect. `npm run test:users --workspace @backy-cms/admin` now covers generated invite-link acceptance, user activation, returned session, generated reset-link acceptance, new-password login, and `user.invite.accept`/`user.password_reset.accept` audit logging. | Replace local demo auth with production Supabase Auth or equivalent, MFA-ready flow, route/API RBAC enforcement beyond Users, refresh/rotation/revocation history, session cookies or httpOnly token storage, audit-event UI, and server-side authorization for every admin mutation. |
| `/` dashboard | Ready | `apps/admin/src/routes/index.tsx` now loads sites, pages, blog posts, users, media, runtime storage/database/Supabase/Vercel settings, site readiness, recent admin audit logs, and commerce product/order record counts from backend APIs, with local fallback state if the admin API fails and user-directory access degrades independently if that protected endpoint is unavailable. It shows refresh/loading/fallback states, backend health, publishing blockers/warnings, quick creation paths, launch-ready workflow links, module map, API handoff, custom frontend setup links, request-id-backed activity, a launch onboarding checklist with setup progress and next-action links, an operations signal board for commerce catalog/orders, comment/form moderation, and workflow failure/readiness alerts, aggregate analytics for publishing mix, recent audit velocity, lead/comment pressure, and commerce totals, a dashboard-run infrastructure diagnostic panel that reuses the Settings validator for database, media storage, Supabase, and Vercel readiness, and a deployment preflight execution/history panel that records session runs with target URL, request id, and ready/warning/blocked counts. Dashboard data loading and rendered actions are filtered by the signed-in admin permission matrix or role defaults, including privileged users/settings/activity/admin-endpoint surfaces and settings-backed infrastructure/deployment checks. The dashboard design now follows `DESIGN.md` with semantic status tokens and a denser control-room layout. `npm run test:dashboard --workspace @backy-cms/admin` now covers API-backed temporary site creation using a real local-demo admin session, rendered command center/site selector/stats/onboarding/RBAC-scope/deployment-history/operations-signal/aggregate-analytics/readiness/infrastructure diagnostics/workflows/attention/activity/API/module regions, active API site switching with scoped endpoint text, critical builder/navigation links, deployment preflight execution/history rendering, infrastructure check execution, refresh behavior, desktop and mobile rendered visual regression checks for region geometry/control sizing/overflow/runtime overlays, and a temporary viewer-session RBAC pass that verifies users/settings/admin endpoints, privileged creation actions, privileged module cards, and settings-backed checks are hidden or disabled. | Current dashboard scope is complete; future global work remains tracked under production auth/RBAC and broader platform rollout rows. |
| `/sites` | Ready | `apps/admin/src/routes/sites.tsx` now loads and deletes through `GET/DELETE /api/admin/sites`, with local fallback state. The multi-site workspace includes command/readiness/RBAC/API/domain-verification/Vercel-deployment/billing-quota/feature-contract/audit/library regions, search plus status/domain/page-coverage filters, CSV/frontend-contract export, per-site publish status editing, selected-site DNS TXT/CNAME verification state, selected-site Vercel preview/production/rollback deployment handoff state, selected-site billing plan and quota usage state, duplicate-as-draft, quick archive, preview/manage links, and typed delete confirmation. The page loads the signed-in user's permission matrix and gates site creation, status/configuration, domain verification, deployment workflow actions, billing/quota changes, duplicate, archive/delete, CSV export, and audit activity visibility by `sites.*`, `settings.billing`, and `activity.export` capabilities. Site create/update/delete APIs now emit queryable `site.created`, `site.updated`, and `site.deleted` admin audit events, and the Sites page renders a request-id-backed Site audit trail panel. `npm run test:sites --workspace @backy-cms/admin` creates a temporary owner and viewer account, creates a temporary site, verifies the rendered multi-site command/RBAC/API/domain-verification/Vercel-deployment/billing-quota/feature-contract/audit/library surfaces, persists pending and verified domain verification state through the admin site API, persists Vercel preview queued, preview ready, and production ready deployment state plus run history through the admin site API, persists billing plan changes and quota usage snapshots through the admin site API, publishes it through the status control, exercises search/domain/page/status filters, duplicates it as a draft without carrying the custom domain, archives the original, verifies each mutation and site audit events through the admin API, verifies the viewer session cannot create/configure/verify domains/run deploy workflow actions/change billing quotas/duplicate/archive/delete or view audit activity from the rendered Sites UI, confirms typed deletion as owner, checks layout overflow, captures a screenshot, and cleans up. `npm run test:repositories --workspace @backy/db` now verifies repository-mode site settings JSON persists and reads back domain verification, Vercel deployment history, and billing quota state through the DB site repository and backing DB row. A fresh `/sites` readiness audit reran both suites successfully after the domain, deploy, billing, RBAC, audit, and DB persistence slices landed. | Current site-list scope is complete; broader workspace/team ownership remains tracked on `/sites/new` and platform rollout rows. |
| `/sites/new` | Ready | `apps/admin/src/routes/sites.new.tsx` now creates through `POST /api/admin/sites`, supports draft/published/archived status selection, slug/domain validation, database team ownership handoff, blank/business/storefront/publication blueprints, default starter page seeding, public address/API handoff previews, download/copy handoff controls, and recovery links if starter page seeding fails. The create workspace now includes launch setup controls for custom-domain DNS verification state, Vercel project/team/production-domain deployment handoff, billing plan/email/quota limits, and starter-marketplace/import/custom-frontend template source metadata. `POST /api/admin/sites` now persists supplied site settings in both demo and DB repository modes, including `siteStatus`, `domainVerification`, `vercelDeployment`, `billingQuota`, and `frontendDesign` setup metadata, while site creation audit rows include the initialized setup status. `npm run test:sites --workspace @backy-cms/admin` now drives the rendered `/sites/new` form, creates a published storefront site through the UI, verifies persisted pending domain verification state, Vercel preview-queued deployment setup, business billing quota setup, and captured marketplace template metadata through the admin sites API, verifies Home/Shop/Contact starter pages through the admin pages API, confirms homepage/status inheritance, then continues through `/sites` filters, status changes, domain verification, Vercel deployment, billing quota workflows, duplication, archiving, typed deletion, screenshot capture, and cleanup. | Current site-create scope is complete; broader production auth/RBAC, real provider execution, and durable DB rollout gaps remain tracked globally. |
| `/sites/$siteId` | Ready | `apps/admin/src/routes/sites.$siteId.tsx` now saves/deletes site settings through `PATCH/DELETE /api/admin/sites/:siteId`; forms/comments moderation sections are backend-connected. Form submissions that generated collection draft records now show a content shortcut into the Collections admin surface. The page now loads site pages and stored navigation through admin APIs, then lets admins add page/internal/external primary/footer menu items, nest children, reorder root and nested sibling links with move controls or drag handles/drop zones, toggle visibility, and save through `PATCH /api/admin/sites/:siteId/navigation`. It also loads/saves exact redirect and 410 gone rules through `GET/PATCH /api/admin/sites/:siteId/redirects`, can preview unsaved redirect route warnings through `POST /api/admin/sites/:siteId/redirects`, and displays source-shadowing or missing-target diagnostics before/after save. Site SEO defaults, JSON-LD defaults, sitemap settings, robots settings, and route-level SEO overrides persist through `GET/PATCH /api/admin/sites/:siteId/seo`; public SEO discovery now applies route overrides for canonical, title, description, Open Graph image, keywords, robots, priority, and change frequency before sitemap/robots output. The site workspace now includes a domain verification panel for saved custom domains, exposes TXT host/value, CNAME target, token, requested/checked/verified timestamps, and persists pending/verified DNS state through the admin site API. The site workspace also includes a theme/publish panel for brand colors, typography, spacing, custom CSS, public token preview, and status delivery state, persisting `theme` plus `settings.siteStatus` through the admin site API. Site-level webhook configuration is now typed under `settings.webhooks`, rendered in the site workspace with enablement, endpoint URL/name, event allowlist, secret reference, and headers controls, persists through the admin site API, and emits `site.webhooks.updated` audit metadata. The workspace now includes a compact standalone form builder that creates site forms, edits identity/delivery/moderation/contact-share/field contracts, reorders fields, persists through the admin forms APIs, and emits form audit rows. Site navigation, redirect, SEO, domain-verification, theme/publish, webhooks, form create/update/delete, comment-policy, frontend-design, create/update/delete mutations now emit site-scoped admin audit logs, and the site workspace renders a `Site activity` audit panel backed by `/api/admin/audit-logs?siteId=...` with request IDs and mutation metadata. `npm run test:site-detail --workspace @backy-cms/admin` now creates a temporary site and SEO target page, opens its workspace in authenticated headless Chrome, verifies command/readiness/domain/theme/webhooks/form-builder/navigation/redirect/SEO/settings/activity/automation/handoff regions, prepares and verifies the rendered domain DNS state, saves rendered theme/publish controls, saves rendered webhook configuration, creates and saves a rendered standalone site form, adds primary/footer navigation plus child routes through the rendered editors, verifies nested drag controls, persists reordered child-route order plus a header search toggle, previews and saves a redirect rule, saves SEO defaults plus a rendered route SEO override, refreshes and verifies the rendered site activity rows, confirms domain/theme/webhooks/forms/navigation/redirect/SEO/frontend-design/comment-policy API readback plus expected audit actions, confirms public SEO discovery exposes the route override, captures a screenshot, and cleans up the site. | Current site-detail workspace scope is complete; broader production auth/RBAC, durable DB, and platform rollout gaps remain tracked globally. |
| `/pages` | Ready | `apps/admin/src/routes/pages.tsx` now loads and deletes through `GET/DELETE /api/admin/sites/:siteId/pages`, with local fallback state. The list now has an active-site selector, status filters for all/published/draft/scheduled/archived pages, readiness health pulled from site readiness, first readiness issue text for route/SEO/layout diagnostics, a blocked-health metric filter, refresh control, preview action, published-page link action, search-aware row selection, and bulk publish/archive/delete actions through backend APIs. Empty states distinguish an empty site from filtered results, every create entry point routes through link-backed `/pages/new` navigation with the active `siteId`, and the empty-site action now uses the same generic `New Page` flow instead of a separate homepage starter. The table, CSV export, and handoff JSON now expose route diagnostics, delivery diagnostics, page hierarchy, and navigation placement context: page rows show route availability/conflict state, duplicate/reserved/invalid routes and collection list/item route shadows block publish and bulk publish, delivery rows show published/preview-only/scheduled/archived/blocked state with public/render/resolve/preview endpoint context plus public delivery health probes for published pages, top-level pages show child counts, child pages show their parent title/route, and page rows include navigation placement/label metadata for custom frontend handoff. Single-page and bulk publish actions now open review dialogs before mutating backend state, showing route, readiness, delivery, public/render/resolve, and preview endpoint context while still rechecking readiness before publish. Page rows also load revision summaries from `/api/admin/sites/:siteId/pages/:pageId/revisions`, showing revision count, latest note/date/status, and a direct history link into the editor; CSV and handoff JSON include route, delivery, delivery-health, delivery-health history, and revision summary fields. Published-page delivery health can be refreshed from the command center or directly from each published row, and each row now shows a recent-probes trail with timestamp, status, and public/render/resolve HTTP results. Browser QA covers empty-site controls, blocked filter activation, clear-filter behavior, empty-state/new-page navigation, a temporary published row with health plus preview/edit/delete/published actions, and temporary search-filtered rows through bulk select-visible, publish, archive, and delete. `npm run test:pages-list --workspace @backy-cms/admin` specifically verifies the empty-site `New Page` action renders a usable page creation command center with active site and enabled `Create Page` submit control; it also verifies the command-center registration shortcut, temporary parent/child hierarchy rendering, a temporary backend revision surfaced in the list, row route availability diagnostics, row delivery diagnostics, published-page delivery health display, refresh control plus recent-probe history, the single-page publish review modal, the bulk-publish review modal, the confirmed bulk-publish mutation path by publishing a temporary draft page with `expectedUpdatedAt`, verifies the success notice/table row state, clears selected bulk UI state, reads the published status back from the admin page detail API, and runs a temporary viewer-session RBAC pass confirming create shortcuts, row selection, edit, preview, publish, archive, delete, and bulk mutation controls are disabled while read-only delivery health refresh remains available, plus named visual screenshot artifacts and layout/overflow checks for empty, delivery-row, bulk-modal, and post-publish table states. | Current page-list scope is complete; broader production auth/RBAC, durable DB rollout, visual baseline comparison thresholds, and cross-browser CI remain tracked globally. |
| `/pages/new` | Ready | `apps/admin/src/routes/pages.new.tsx` now creates through `POST /api/admin/sites/:siteId/pages`, with local fallback state, draft/published/scheduled status controls including `scheduledAt`, real starter canvas templates for landing/storefront/blog-index/about/contact/registration pages, editable header/navigation/footer chrome for non-blank templates, route conflict checks against the selected site's page library, a creation handoff manifest, and immediate transition into the visual editor after persistence. When opened with `collectionId` and `datasetMode`, it loads the selected collection, renders a dataset import panel, includes collection field mappings in the handoff manifest, and seeds collection-backed list pages with a repeater or item pages with bound title/summary elements. The create route includes site-aware navigation placement controls for no menu, primary menu, or footer menu, persists those choices in route search/meta preview, and patches site navigation after the page record is created. It also supports parent page placement: selected parents are saved as `parentId` plus `meta.parentPageId`/`meta.parentPageTitle`, homepage routes stay top-level, and primary/footer navigation nests the created page under the chosen parent. It has local autosave/recovery for unsaved title, slug, target site, template, status/schedule, homepage flag, parent page, SEO description, search title, canonical path, Open Graph image URL, robots flags, and navigation placement/label, with restore/discard actions and autosave status in the command center; successful create clears the local draft. The page now includes SEO/social controls and persists `meta.title`, `meta.description`, `meta.canonical`, `meta.keywords`, `meta.jsonLd`, `meta.ogImage`, `meta.noIndex`, and `meta.noFollow` so public render payloads and custom frontends receive route metadata immediately after creation. Keywords and JSON-LD can be supplied directly or generated from the selected template defaults. Template selection now includes reusable visual wireframe previews for every starter type plus a selected-template canvas summary for chrome/section expectations before create. The editor route retries freshly-created backend page loads before falling back to local state, so a successful create opens the backend-backed editor without a misleading local-copy warning. `npm run test:page-create --workspace @backy-cms/admin` covers rendering the create workspace from search state, autosave localStorage persistence, desktop and mobile template-preview visual assertions with screenshot artifacts, switching contact/storefront/blog-index/blank/registration templates and verifying selected previews, navigation defaults, form seeds, dynamic-data seeds, site-chrome payload state, template preview rendering/selection state, dataset import rendering from a temporary collection, saved collection dataset import metadata, persisted repeater collection/dataset/title/summary/image props, recovery restore, creating a temporary parent page and child backend page through the UI, clearing the local draft after create, navigating into the editor without backend fallback, verifying the selected primary navigation parent/child nesting through the admin navigation API, verifying saved parent/SEO/keywords/JSON-LD/social/robots meta through the page detail API, verifying the created About template persisted editable header/navigation/footer chrome, title/story/value canvas elements, nested value-card children, canvas sizing, and content-document metadata, and creating landing/storefront/blog-index/contact/registration/blank starter pages through the UI to verify backend-persisted chrome, form blocks, data bindings, canvas sizing, content-document metadata, editor-canvas DOM rendering, non-collapsed required canvas elements, no local fallback, no empty canvas state, no horizontal overflow, and per-template editor screenshot artifacts plus created editor canvas screenshot threshold metrics for visible crop size, sampled pixel count, non-white pixel ratio, dark-detail ratio, and luma range before cleanup. | Current page-create scope is complete; broader cross-browser CI and full baseline-image diffing remain tracked globally. |
| `/pages/$pageId/edit` | Ready | `apps/admin/src/routes/pages.$pageId.edit.tsx` now loads/saves through `GET/PATCH /api/admin/sites/:siteId/pages/:pageId`, exposes backend-backed publish/archive/revision rollback/preview controls, and hydrates site media so uploaded fonts are available in editor font controls. Text styling controls can now open the media picker to upload/select font files for the selected element. Backend preview tokens allow draft hosted page, render, and page API access, and page preview-token creation now records a redacted `previewToken.create` audit event with target, slug, TTL, expiry, and tokenless hosted/render/API path metadata. Editor layers can now select, root-reorder, move up/down in sibling order, move nested layers out to the parent while preserving visual position, move selected layers into child-accepting layers, hide/show, lock/unlock, duplicate, and delete canvas elements through persisted element fields. Canvas drag/resize now uses ref-backed transient transforms with one undo history commit on release, selected elements support arrow-key nudging, multi-layer drag, proportional multi-layer resize/scale from corner handles, toolbar align-left/center/right/top/middle/bottom commands, and multi-layer horizontal/vertical spacing distribution, the design surface has scale-aware zoom/fit controls, dragged elements avoid accidental text selection, and smart guide lines snap dragged elements to canvas/element edges and centers. Canvas elements also support shift/command/control-click multi-select from the design surface for grouping and layer operations. The component library now supports composed nested presets and includes hero, feature-grid, lead-capture form, HTML, and table blocks that remain normal editable canvas trees/elements after drop. The editor library also loads active API-backed reusable sections for the active site, can save the selected element tree as a reusable section, can rename/delete saved sections, can insert saved sections as synced canvas instances with source metadata, can refresh a selected synced instance from the saved source, can detach it into an independent editable copy, and still stores concrete canvas trees for public rendering. The shared canvas editor now supports desktop/tablet/mobile breakpoint editing by merging active breakpoint layout/content/style and layer visibility/lock changes into `element.responsive` overrides while keeping desktop base state intact; the inspector exposes an active breakpoint override card with reset-all plus group-level layout/layer/content/style reset controls that show which groups inherit desktop, and the public renderer applies mobile/tablet/desktop overrides from the rendered container width. `npm run test:editor-drag --workspace @backy-cms/admin` now creates a temporary backend page and smoke-tests authenticated editor load, heading/image/container/nested-child/form dragging, image/form resizing, Shift+Arrow keyboard nudging, Ctrl+Z/Ctrl+Shift+Z drag undo/redo plus non-drag undo/redo for inspector layout edits, keyboard nudges, and layer visibility toggles, selected text inspector font-media picker presence, `Ctrl/Cmd+A` sibling selection, three-layer spacing distribution, multi-layer resize/scale with persisted reload hydration, `Ctrl/Cmd+G` grouping, `Ctrl/Cmd+Shift+G` ungrouping with position-preserving roundtrip, layer-tree nest/outdent controls with position-preserving coordinate conversion, synced reusable-section insertion/source refresh/detach, invalid numeric input warning guards, backend save persistence, mobile and tablet breakpoint layout plus layer visibility/lock override persistence, group-level reset/inheritance controls, desktop/mobile/tablet switch hydration, and fresh editor reload rehydration in headless Chrome. The property panel now loads site collections, can attach normalized collection field bindings to selected elements, can configure type-aware dataset query controls for search, field filters, select/tag/boolean filter values, current-record reference filters, sort direction, limit, and offset, can configure collection-backed repeater elements with dataset ids, field mapping, query filters, sorting, limits, offsets, columns, gaps, and empty states, can configure form-to-collection draft record writes with field mapping, can configure image source, alt, title, object-fit, object-position, loading, decoding, and referrer-policy behavior, can configure icon symbol, size, color, title, and accessibility label behavior, can configure list type, marker style, indent, and item behavior, can configure divider color, thickness, style, and margin behavior, can configure column count and gap behavior, can configure navigation items, direction, gap, and accessibility label behavior, can configure spacer size and background color behavior, can configure quote citation and border behavior, can configure input field label, submission name, required state, placeholder, help text, input type, validation pattern, length limits, and default value behavior, can configure textarea label, submission name, required state, placeholder, help text, rows, length limits, and default value behavior, can configure select field label, submission name, required state, placeholder, help text, option list, and default selected value behavior, can configure checkbox/radio field labels, submission names, required states, placeholders, help text, option lists, and selected values, and can configure link text, href target, rel, title, ARIA label, and underline behavior, can configure button href target, rel, title, ARIA label, and button type behavior, and can configure video source, poster, object-fit, controls, autoplay, loop, mute, and inline-playback behavior plus embed iframe title, allowed feature, sandbox, loading, referrer-policy, and fullscreen behavior plus map address, zoom, title, loading, referrer-policy, and fullscreen behavior that persist into public rendering. Public PageRenderer now renders Slate-backed heading, paragraph, and quote text for custom frontend output instead of dropping array content. Public render payload normalization now preserves binding query and pagination metadata and resolves collection-bound values through search/filter/sort-aware dataset queries. The shared canvas editor now exposes visible dirty, autosave queued, autosaving, manual saving, saved, and failed-save status states, including pending-change count, last save mode/time, and last error metadata while its silent backend autosave runs. Page settings persist scheduled publishing dates. `npm run test:editor-drag --workspace @backy-cms/admin` now also creates a temporary collection, binds a canvas element through the rendered Data inspector query controls, verifies queued autosave metadata after a desktop edit, saves the page, and verifies manual saved metadata plus persisted image behavior, icon behavior, list behavior, divider behavior, columns behavior, nav behavior, spacer behavior, quote behavior, input field behavior, textarea behavior, select field behavior, checkbox/radio behavior, link behavior, button link behavior, video behavior, embed iframe behavior, map iframe behavior, query/pagination binding, type-aware collection filter value options, rich-text public renderer output, reusable-section insert/source-refresh/detach, HTML/table component library exposure, and repeater dataset/layout props through the backend page API. `npm run test:admin-contract --workspace @backy/public` covers page preview-token audit readback and verifies the raw token is not stored in audit metadata. `BACKY_EDITOR_RENDER_PARITY_SMOKE=1 npm --workspace @backy-cms/admin run test:editor-drag` now covers editor-created data binding and repeater controls through autosave and backend persistence, public render payload parity with preview-token access, joined dataset hydration for binding and repeater records, authored dynamic collection list/item template rendering with joined reference values, and editor canvas visual threshold metrics for visible crop size, sampled pixels, non-white ratio, dark-detail ratio, and luma range. | Current page-editor scope is complete; broader cross-browser CI, full baseline-image diffing, production DB rollout, and global auth/RBAC hardening remain tracked globally. |
| `/blog` | Ready | `apps/admin/src/routes/blog.tsx` now loads and deletes through `GET/DELETE /api/admin/sites/:siteId/blog`, with local fallback state. Blog taxonomy and author resources now exist at admin/public endpoints, post list APIs support taxonomy/author filters, and the admin blog list exposes category/tag/author filters plus taxonomy columns. The list workspace has a command center, readiness map, API/frontend handoff snippets, public route snippets, CSV/JSON exports, row preview/edit/delete actions, bulk publish/archive/delete controls, a rendered taxonomy manager for creating/editing/describing/coloring/deleting categories and tags through the backend taxonomy APIs, per-row SEO/comment controls that show SEO title/description readiness, toggle `noIndex`/`noFollow` through the post update API, and summarize total/pending/flagged post comments from the comments API, plus surfaced public search and year/month archive feed URLs for custom frontend blog index pages. `npm run test:blog-list --workspace @backy-cms/admin` creates temporary taxonomy and draft post data through the admin API, submits a temporary blog comment, verifies the rendered command/API/filter/bulk/table/taxonomy/SEO/comment/search-feed/archive-feed regions, runs a visual-state regression guard for visible command/API/table/taxonomy regions, no framework overlay, no horizontal overflow, SEO/comment controls, and search/archive snippets with screenshot artifacts, toggles row `noIndex` with backend readback, creates/edits/deletes a category and tag through the rendered taxonomy manager with backend readback, exercises search/category/tag/author filters, opens a tokenized preview URL, bulk-publishes through the UI, verifies public slug, taxonomy, search, and archive feed delivery, captures a screenshot, and cleans up. | Current blog-list scope is complete; broader platform gaps remain around durable database rollout, audit-event UI, and broader contract tests tracked under Blog admin APIs. |
| `/blog/new` | Ready | `apps/admin/src/routes/blog.new.tsx` now creates through `POST /api/admin/sites/:siteId/blog`, with local fallback state, backend-loaded author/category/tag assignment controls, scheduled publishing input, shared `Panel`/`Notice`/`Button` controls, and the reusable `EditorWorkspaceFrame` around the same page editor canvas used for pages. The creation workspace has been reorganized into an editor-first layout with a wider canvas, editorial draft panel, sticky publishing rail, site-aware taxonomy/author controls, segmented draft/published/scheduled actions, readiness checks for title/slug/summary/SEO/featured-media/design/schedule, and taxonomy chips instead of cramped checkbox stacks. It now supports post-scoped featured image selection through the shared media library, persists `featuredImageId`, includes selected media in the create payload and frontend handoff manifest, and uses the selected image as the Open Graph fallback. It also includes route-level SEO creation controls for search title, canonical path, search description, Open Graph image URL, noindex, and nofollow, and persists those values into post meta. The create surface now has a real `Save draft and preview` workflow: it persists a draft, requests the existing backend preview-token endpoint, opens the hosted/API preview URL, and transitions into the blog edit page for revision/publish work. It also has local autosave/recovery for unsaved title, slug, excerpt, publishing status, SEO fields, featured media, taxonomy, author, canvas elements, and canvas size, with restore/discard actions and autosave status in the command center. The route now supports a canvas focus mode that hides draft/SEO/taxonomy/publish panels, preserves `focus=canvas` in search state, expands the editor frame for page-design work, and keeps compact save-draft-preview, publish, and show-panels controls available above the canvas. A writing-structure panel now adds editable article sections and pull quotes into the canvas while showing word-count and reading-time planning metrics, and those inserted long-form blocks persist through autosave and created post content. Browser QA covers rendering the workspace, the scheduled date control, expanded canvas height, readiness enabling save, and creating then cleaning up a draft post through the UI/API. `npm run test:blog-create --workspace @backy-cms/admin` covers the new create workspace render, desktop and focus-mode visual-state guards for visible command/workspace/canvas/editor side panels, no framework overlay, no horizontal overflow, breakpoint/grouping controls, frontend template handoff, focus-mode panel hiding, and screenshot artifacts, mobile breakpoint authoring for the frontend-template heading with desktop-layout preservation plus autosave/final-post mobile override persistence, writing-structure controls with section/quote insertion plus autosave/final-post persistence, canvas focus/restore behavior, featured media picker, SEO controls, autosave localStorage persistence, recovery restore, and save-draft-and-preview flow against the live admin/public dev servers. The embedded editor toolbar is now grouped into stable control clusters, scrolls instead of clipping in constrained widths, and supports `Cmd/Ctrl+G` plus `Shift+Cmd/Ctrl+G` for grouping/ungrouping selected layers. `npm run typecheck --workspace @backy-cms/admin`, `git diff --check`, and `npm run build --workspace @backy-cms/admin` passed for the featured media, SEO, create-preview, autosave-recovery, browser-smoke, and canvas-focus slices. | Current blog-create scope is complete; broader editor/product gates remain tracked under `/pages/$pageId/edit` and product-level gates. |
| `/blog/$postId` | Ready | `apps/admin/src/routes/blog.$postId.tsx` now loads/saves/deletes through `GET/PATCH/DELETE /api/admin/sites/:siteId/blog/:postId`, persists author/category/tag/scheduled publishing assignments, and exposes backend-backed publish/archive/revision rollback/preview controls. Backend preview tokens allow draft hosted post and post API access. The edit workspace verifies slug availability before save/publish, persists route-level SEO metadata including canonical path, Open Graph image, robots flags, and search title/description, supports featured image selection through the shared post-scoped media library, includes the image and SEO contract in frontend handoff JSON, and surfaces post-specific comment moderation state with pending/approved/flagged counts plus quick approve/reject actions against the comments API. `npm run typecheck --workspace @backy-cms/admin`, `git diff --check`, and `npm run build --workspace @backy-cms/admin` passed for the route, SEO, featured media, and comment moderation slices. | Current page scope is complete; next improvements belong to broader editor/product gates: autosave, responsive breakpoint authoring, richer long-form writing ergonomics, and visual regression coverage shared with the page editor. |
| `/collections` | Ready | `apps/admin/src/routes/collections.tsx` now provides a backend-backed collection builder for the active site. It lists collections, creates/updates/deletes collection schemas, edits field keys/labels/types/required/unique flags, edits select/tag options and reference targets, toggles public-read and visitor-create/update/delete permissions, configures the collection-scoped public write token required by public update/delete routes, saves visitor create/update selected-field allowlists in collection metadata, saves metadata-backed generated list/item template presets with title/summary/image/detail field roles for fallback dynamic routes, can attach a captured frontend collection template to an existing schema for custom list/item route rendering, creates/updates/deletes records, filters/searches/sorts paginated record lists through the admin API, renders backend validation details, runs selected-record bulk status/delete actions, imports CSV rows through backend schema validation/upsert, exports the filtered record set as backend-generated CSV, exports portable JSON collection backups through `GET /api/admin/sites/:siteId/collections/export`, imports/restores those schema-plus-record backups through `POST /api/admin/sites/:siteId/collections/import?upsert=true`, and links published records to generated `/:collectionSlug/:recordSlug` hosted routes. Collection schema mutations, individual record mutations, selected-record bulk status/delete mutations, and backup imports now emit request-id-backed audit logs readable from `/api/admin/audit-logs`. The Collections surface also loads the current admin permission matrix, renders an allowed/blocked permission contract for `collections.view`, `collections.edit`, `collections.export`, and `collections.delete`, gates schema/record/import/export/delete controls by those permissions, shows recent request-id-backed collection schema and record mutation audit events from `/api/admin/audit-logs`, includes a visual relationship browser for outgoing reference fields, incoming reverse references, linked schema counts, unmapped relationship warnings, and route hints, and exposes dataset authoring shortcuts for the active collection that copy a repeater preset, field-binding preset, list-page brief, item-page brief, and list/item page-builder handoff routes into `/pages/new` with `collectionId` and `datasetMode`. The shared editor property panel now lets selected elements bind to collection fields with reusable title/summary/image/link binding presets, site-scoped shared saved preset management backed by `/api/admin/sites/:siteId/editor/collection-binding-presets` with local fallback, reference-field join controls, a selected-record value preview with image thumbnails plus resolved joined values, recent-record picker, manual record id/slug override, target-path selection, search/filter/sort/pagination controls, and persisted binding manifests; repeater elements expose collection, dataset id, join-aware field mapping, joined query filter/sort controls, current-record reference filter controls for reverse relationships, query-backed matching-record previews with joined title/summary/image resolution, and layout controls. `npm run test:collections --workspace @backy-cms/admin` now creates temporary public-readable dynamic collections with outgoing and incoming references, loads `/collections` in authenticated headless Chrome, verifies the command center, templates, API contract, frontend contract, binding contract, dataset authoring shortcuts with list/item builder buttons, permission contract, relationship browser, captured frontend template selector, schema-and-record audit activity panel, schema builder, records table, field controls, route links, record selection controls, visitor update/delete policy controls, public write-token persistence, selected update-field persistence, authored-template compare controls and diff summaries, and New collection reset behavior, exercises the rendered repeater-preset copy action, attaches a captured frontend collection template to the selected schema and verifies persisted frontend template metadata, publishes the selected record through the rendered bulk action, confirms the bulk record audit event appears in the rendered activity panel, confirms admin and public record APIs expose the published record, captures a screenshot, and cleans up the temporary schemas. `BACKY_EDITOR_COMPONENT_SMOKE=data-binding npm run test:editor-drag --workspace @backy-cms/admin` verifies the canvas data panel loads collection field/query options, exposes title/summary/image binding presets, configures a joined reference binding such as `author.company.name`, can save/apply/delete a site-shared binding preset through the backend preset API, shows selected record preview values plus thumbnail image, exposes created records in the picker, applies a selected record to the binding, saves it, and confirms the persisted canvas binding includes dataset id, target path, source collection/field/path/record, query filter/sort, and pagination. `BACKY_EDITOR_COMPONENT_SMOKE=repeater npm run test:editor-drag --workspace @backy-cms/admin` verifies repeater dataset controls expose reference join field paths such as `author.company.name`, can set a `$currentRecord.id` reference filter for reverse relationship repeaters, render current-record preview guidance, show a query-backed matching-record preview with joined title value, summary, total count, and image, persist the joined title mapping, and reload cleanly. `npm run test:admin-contract --workspace @backy/public` verifies collection schema audit readback, individual and bulk collection record mutation audit readback, collection backup export/import with restored-record readback and import audit metadata, editor-session `collections.view` access, editor denial for `collections.export` and `collections.delete`, explicit `collections.edit` deny override enforcement, render payload/hosted page resolution for collection reference joins, joined repeater record hydration through `record.values["author.company.name"]`, dynamic item reverse-relationship repeater hydration by resolving `$currentRecord.id` to the active record id, and public create/update/delete policy enforcement with collection-scoped write tokens and selected update-field allowlists. Database-mode collection record reads now push status, search, field filter, sort, count, limit, and offset into repository queries with composite indexes and repository smoke coverage that verifies full pagination totals while materializing only the requested page. Drizzle schema and Supabase migrations now add JSONB containment, trigram search, and published-feed indexes for field-heavy custom record values. Public generated collection list/item renderers now consume saved dynamic-template metadata when no captured frontend-design template is attached, preserving the full canvas-template path as the higher-priority route model. The Collections surface now also captures Backy-authored page canvases as list/item dynamic templates, and the collection smoke verifies those authored roots and collection/record bindings render through public dynamic routes. It also exposes a visitor-create field policy that stores selected public-writable fields in collection metadata, and the public create API drops disallowed submitted fields before validation/storage. The dynamic template panel now includes a preview-record selector with hosted and render API URLs for list/item routes, bounded authored-template capture history with restore controls, and active-versus-history compare panels that summarize root element, source page, canvas size, and custom CSS changes before restore/save decisions. | Current collections scope is complete; broader dataset/editor parity work continues under `/pages/$pageId/edit` and product-level editor gates. |
| `/products` | Partial | `apps/admin/src/routes/products.tsx` now exposes a backend-backed commerce catalog workspace for the active site: product schema setup/sync, public-read catalog permissions, readiness checks, public storefront/API handoff manifest, CSV export/import/template controls, filters, product CRUD, variants, option-matrix generation, pricing, inventory policy, delivery URL, checkout URL, subscription enabled/interval/trial metadata, gallery/media fields, category/tags/vendor merchandising, SEO title, publish status, and product editor state. Collection record CSV import now preserves typed number, boolean, tags/multi-reference, and JSON field values so imported catalog data is usable by storefront APIs in demo and database-backed modes, including subscription metadata. Public catalog responses now include a non-secret commerce storefront contract derived from Settings so frontends can read catalog/order-intake availability, default currency, payment-provider handoff mode, provider test/live mode, checkout redirect paths, tax/shipping/discount flags, configurable tax/shipping/discount quote rules, inventory reservation settings, webhook endpoint readiness, webhook event allowlist, and reconciliation mode/window without using admin APIs or leaking webhook secret references; product payloads now also expose a normalized `subscription` object for storefront recurring-plan UI. Products also renders product list/detail page-template shortcuts that open `/pages/new` with the products collection id and `datasetMode=list|item`, so editable storefront pages start with the product dataset binding attached. Public order intake now validates whole-number item quantities before pricing/reservation, calculates a checkout quote from the product tax class, shipping profile/weight, product discount code, and Settings tax/shipping/discount toggles/rules, returns a manual or provider-ready checkout-session handoff for connected storefronts, then persists subtotal, discount, shipping, tax, total, checkout session, payment provider/reference, and customer linkage on the private order record while upserting a private customer profile. Products reads those private customer profiles back into the commerce signal panel and now includes inline profile-management controls for name, email, phone, lifecycle status, and internal notes using the existing admin collection-record PATCH path. The commerce provider webhook endpoint now accepts allowlisted provider settlement events, matches private orders by checkout session/order metadata/payment reference, marks orders paid/refunded/failed, stores settlement notes, and exposes commerce-webhook events through `/events`. `npm run test:commerce --workspace @backy-cms/admin` opens the real Products/Orders UI in headless Chrome, forces full product/order schema sync during the test window, creates a published product through the Product editor after generating four Size/Color variant combinations from the option-matrix UI, saves subscription metadata, imports a typed product CSV row through the admin import API, verifies the public `/api/sites/:siteId/commerce/catalog` response includes subscription metadata, verifies fractional public checkout quantities are rejected instead of silently rounded while omitted quantities still default to one item, posts a public checkout order to `/commerce/orders`, verifies the returned and persisted quote totals use custom pricing rules plus inventory reservation/customer linkage/checkout-session handoff, verifies configured order notification delivery events, edits the checkout-created private customer profile from Products to `vip` with phone and note changes, posts a simulated provider webhook to `/commerce/webhook`, verifies order settlement and `/events?kind=commerce-webhook` readback, checks Products layout overflow/regions/import controls including the backend-fed commerce analytics/customer profile management panel plus subscription metadata panel, verifies the product list/detail page-template shortcuts open page creation with the Products collection dataset modes and non-conflicting authoring slugs, deletes the smoke records, captures a screenshot, and restores the original demo collection schemas/settings. `npm run test:settings --workspace @backy-cms/admin` now authenticates with the admin auth API, verifies Settings hydration, saves commerce provider/reconciliation metadata plus pricing rules through the rendered UI, restores the original settings, and a focused manifest verification covers public commerce contract exposure. | Add provider API execution/reconciliation workers, provider-grade tax/shipping/discount integrations, automated provider refunds, provider subscription billing execution, richer storefront template libraries/custom dynamic route promotion, product-specific notification automation, and deeper commerce analytics. |
| `/orders` | Partial | `apps/admin/src/routes/orders.tsx` now exposes a private commerce operations workspace for the active site: order schema setup/sync, private order permissions, public checkout-intake contract visibility, readiness checks, admin API handoff, CSV export/import/template controls, filters, order CRUD, line items, payment state, fulfillment state, tracking, refunds/returns, addresses, customer/support notes, workflow actions, shipment-label handoff metadata, and an inline linked customer profile manager. Order cards now include rendered Prepare Label and Refund/Return workflow actions; Prepare Label calls `POST /api/admin/sites/:siteId/commerce/orders/:orderId/shipping-label`, persists draft label status/provider/id/url/service/cost/created-at fields, and moves eligible orders into processing, while Refund/Return persists refunded order/payment status, cancelled fulfillment status, refund amount/reason, and a timestamped private note through the admin records API. Public checkout intake now upserts a private customer profile by email, tracks order count/total spent/last order metadata with the real order record id, writes the customer record id into the private order, persists generated checkout-session/payment handoff fields and safe shipping-label defaults on the private order, executes configured commerce order email/workflow webhook notifications with queued/succeeded/failed `commerce-order` events, and persists checkout fraud-risk score/level/review reasons. The Orders editor loads private customer profiles, links the selected order by `customerid` or email, and lets admins edit profile name, email, phone, status, and notes without mutating the order record implicitly. Provider webhook settlement now updates matched private orders into paid/refunded/failed payment states and records commerce webhook activity for connected dashboards, `POST /api/admin/sites/:siteId/commerce/reconcile` supports manual admin repair plus scheduled dry-run previews, `GET /api/admin/sites/:siteId/commerce/reconcile?limit=...` provides a bodyless service-key scheduled execution entrypoint for a single site, and `GET /api/admin/commerce/reconcile?siteId=...&limit=...` provides the platform cron-facing batch endpoint with per-site results/skips/errors. Root `vercel.json` schedules that platform endpoint daily and setup docs explain the bearer `CRON_SECRET`/admin-key configuration. `GET /api/admin/sites/:siteId/commerce/orders/analytics` now returns backend order analytics across demo and repository modes, including revenue totals, payment and fulfillment buckets, source/currency breakdowns, operations signals, 14-day trend points, and recent order summaries rendered in the Orders analytics panel and exposed through the handoff manifest. Orders also renders recent order notification delivery activity from `GET /api/sites/:siteId/events?kind=commerce-order`. `npm run test:commerce --workspace @backy-cms/admin` verifies the orders schema can be temporarily synced to the full 40-field order contract and that public checkout intake plus configured order notifications/provider webhook settlement writes a private order record/customer profile/checkout session, shipping-label defaults, and delivery events while keeping direct public order collection access disabled. `npm run test:orders --workspace @backy-cms/admin` snapshots/restores the order and customer schemas/settings, opens the real Orders UI, verifies command/API/metrics/analytics/notification-delivery/shipment-label/risk-controls/queue/editor/profile-manager regions and layout/import controls, verifies the order analytics API reports pending manual orders and later paid revenue/fulfillment backlog after reconciliation, imports a typed order CSV row through the admin import API including shipment-label metadata, creates a manual order with line items, tax/shipping/discount, payment/customer/tracking/address/note data linked to a private customer profile, updates that profile to VIP through the Orders editor, exercises search/payment/fulfillment/source filters, marks the order paid, prepares a shipping label draft through the rendered backend action and verifies the label `GET` endpoint, fulfills it, runs the Refund/Return workflow, edits refund and support notes, simulates a stale pending provider order, verifies a scheduled dry-run detects the eligible repair without mutating the order, verifies the platform scheduled reconciliation endpoint repairs the stale order without a browser click using bearer admin-key auth, runs the rendered Provider reconciliation action, captures a screenshot, deletes the order through the confirmation modal, and restores the original schemas/settings. | Add real provider API execution, production secret configuration for cron, fulfillment integrations, real carrier label purchase/void integrations, provider-grade tax/shipping integrations, provider refund automation. |
| `/forms` | Partial | `apps/admin/src/routes/forms.tsx` now provides a backend-backed forms command center for the active site with form list/detail, frontend API snippets, template cards, submission inbox, approval/rejection/spam moderation, contact-share context, webhook delivery history, email notification delivery history, consent export summary/export, and rendered failed-delivery retry actions for both webhooks and email notifications. Template cards can create standalone backend forms directly instead of only copying schema or starting a page, and standalone forms now expose an inline form-builder panel for title/name/description, active/audience/moderation/success/spam/consent-retention/contact-share controls, field add/remove/reorder, field key/label/type/required/placeholder/help/options editing, per-field validation rule editing for min/max length, pattern, and numeric min/max, collection-write target selection, slug source selection, per-field collection mapping, dirty/reset state, and save through `PATCH /api/admin/sites/:siteId/forms/:formId`. Page/blog canvas-derived forms remain visible but are marked canvas-owned so admins edit them from the source canvas. Captcha-enabled public forms now verify external provider tokens before validation/persistence/delivery, accepting Turnstile, hCaptcha, reCAPTCHA, and generated frontend token aliases while keeping captcha tokens out of stored submission values. Public form notification email delivery now supports local-outbox, generic HTTP handoff, Resend, and SMTP provider modes with delivery events for queued/succeeded/failed outcomes. Selected forms can now be saved as backend reusable embed blocks through `POST /api/admin/sites/:siteId/forms/:formId/embed-block`, which creates a normal reusable section with a form canvas tree, public definition/submit URLs, and captured frontend design tokens/chrome/binding metadata so connected frontends can retain design context while dropping the form into future pages or blogs. Forms analytics are now loaded from `GET /api/admin/sites/:siteId/forms/analytics`, aggregating all form submissions across demo and repository modes into site totals, status counts, collection-route counts, approval/spam rates, 14-day trend data, and per-form breakdowns rendered on the Forms page. The same Forms analytics response now includes lead segmentation and saved-list analytics derived from form contacts and site contact list settings, including lifecycle/quality segment counts, matched saved-list contact IDs/form IDs, and per-form contact/qualified/ready-to-promote counts rendered on `/forms`. Form create/update/delete, submission review, consent retention, and form embed-block creation now emit request-id-backed admin audit logs, and `/forms` renders a Forms activity panel from `/api/admin/audit-logs`. Forms admin APIs now enforce first-class `forms.view`, `forms.create`, `forms.edit`, `forms.manage`, and `forms.delete` permissions instead of relying on broad collection permissions. The database repository now persists form spam/consent policy objects into the durable `settings` JSON, and `supabase/migrations/002_forms_contacts_persistence.sql` creates durable `form_definitions`, `form_submissions`, and `form_contacts` tables with RLS policies and indexes. `npm run test:forms --workspace @backy-cms/admin` verifies a denied `forms.create` permission override blocks form creation with `FORBIDDEN_PERMISSION`, verifies captcha-protected public forms reject missing tokens and accept a valid provider token without storing it as field data, then opens the real Forms UI, creates a temporary public-create collection, creates a temporary Registration form from the template, edits it through the builder into a six-field public definition with an updated title, placeholder, Company field, Company minLength validation rule, collection field mapping, notification email, notification webhook, spam rules, and consent retention/export policy, saves the edited form as a reusable embed block and verifies the reusable-section content/metadata, verifies the analytics API and rendered analytics panel/trend/top-form regions plus lead-segment and saved-list analytics regions, verifies the public definition API exposes the rule and mapping, verifies an invalid public registration is rejected with the configured validation message, submits a valid public registration against the edited schema, verifies the submission creates a draft collection record with mapped values and source submission provenance, verifies configured email delivery events, retries a failed email delivery from the rendered delivery panel, verifies the consent export/retention policy panel, export action, and due-consent anonymization plus site-wide retention automation for consent fields, verifies failed webhook delivery events render in the Forms UI, retries the failed webhook from the rendered delivery panel, verifies retry queued/succeeded events, refreshes the inbox, approves the submission through the UI, verifies form/submission/embed audit-log request IDs and the rendered Forms activity panel, verifies the admin API readback, checks layout overflow/regions, captures a screenshot artifact, and deletes the temporary form/reusable block/collection. `npm run test:repositories --workspace @backy/db` verifies form definition/submission/contact repository persistence, including spam/consent settings merging. | Add a real database-mode end-to-end smoke against a configured Supabase/Postgres service. |
| `/contacts` | Partial | `apps/admin/src/routes/contacts.tsx` now provides a backend-backed lead pipeline for contacts generated by form contact-share mappings. It loads source forms and contacts through admin APIs, aggregates all source forms or a selected form, exposes readiness checks, lifecycle metrics, permission contract/audit activity panels, private contacts/list/create/update/import/promote API snippets, handoff JSON, CSV export/import/template controls, manual contact creation with email upsert, source-value search, form/status/quality/duplicate filters, duplicate-email readiness, internal notes editing, individual lifecycle actions for new/contacted/qualified/archived, selected-contact state, select-visible controls, bulk lifecycle updates, and selected duplicate-email merge actions that consolidate identity/source data into one primary contact while archiving duplicates with trace notes. `GET /api/admin/sites/:siteId/forms/contact-segments` now provides backend contact segment analytics for all forms or a selected form, including lifecycle counts, missing identity segments, source-value retention, ready-to-promote leads, duplicate-email records, contact IDs, and source form IDs across demo and repository modes. `GET/POST/DELETE /api/admin/sites/:siteId/forms/contact-lists` persists saved contact filter lists under site settings in demo and repository modes, returns matched contact counts/IDs, and records create/update/delete audit events. `POST /api/admin/sites/:siteId/forms/:formId/contacts/:contactId/promote` promotes qualified contacts into invited or active Backy users, reuses existing users by email, generates invite links for invited accounts, writes promotion metadata back to the contact, and records contact/user audit events. `POST /api/admin/sites/:siteId/forms/:formId/contacts/:contactId/promote-customer` promotes qualified contacts into private customer collection records, creates or extends the `customers` collection when needed, upserts by email, writes customer promotion metadata back to the contact, and records contact/customer audit events. The contact handoff manifest includes create/import/segment/saved-list/promote-user/promote-customer/sync/consent-retention endpoints, backend segment summaries, saved list summaries, duplicate group metadata, forms/activity permission requirements, recent contact audit actions, and current selection/merge/retention metadata for custom admin frontends. `npm run test:contacts --workspace @backy-cms/admin` creates a temporary contact-share form, creates a contact through the direct admin contact API, imports a contact from CSV, creates duplicate-email leads, verifies the contact-segments API, creates and later deletes a saved qualified-leads list through the contact-lists API, submits a public lead, opens the real Contacts UI on that form, verifies create/import/bulk/merge/segment/saved-list/promote-user/promote-customer/sync/retention/access-audit regions render, promotes an imported contact to a Backy user and private customer record through the UI, syncs that selected contact to a webhook receiver, verifies contact-sync delivery events, exports contact consent evidence, applies due contact consent retention through the UI, verifies permission keys plus promotion/sync/retention audit activity in the Contacts access panel, merges the duplicate leads through the rendered UI, saves internal notes, marks the submitted contact qualified through the UI, selects the rendered contact, archives it through the bulk lifecycle panel, verifies the admin contact API readback, checks layout overflow/regions, captures a screenshot artifact, and deletes the temporary form/contact/customer data. `npm run test:repositories --workspace @backy/db` now verifies the repository-backed contact workflow used by the Contacts surface, including form/status/request/search filters, exact contact ID read/update behavior, duplicate merge metadata, contact/user/customer promotion metadata, consent-retention anonymization, and contact audit-log filters. | No Contacts-specific backend blocker remains; the broader configured Supabase/Postgres integration smoke is tracked under Forms/Contacts APIs. |
| `/comments` | Ready | `apps/admin/src/routes/comments.tsx` now provides a backend-backed moderation command center for page and blog comments. It loads site-wide comments, pages, posts, site comment policy, author blocklist entries, private comment analytics, and comment delivery/activity events through API helpers, shows readiness checks, moderation metrics, workflow links, private list/analytics/bulk/single update/blocklist/event API snippets, handoff JSON, CSV export, search/status/target/triage/thread/sort filters, visible selection, moderation reasons, author blocklist search/type filters, and per-comment approve/reject/spam/block/report-resolution actions. The site policy panel persists default public comment rules through `PATCH /api/admin/sites/:siteId` with `comments.configure` authorization and `commentPolicy.update` audit logging, including open/closed state, manual vs auto-approve, guest/name/email/reply/report toggles, captcha provider enforcement, blocked terms, closed message, and default sort. The admin surface now derives a thread map from `commentThreadId`/`parentId`, exposes thread summary/reply/report/pending counts, lets reviewers filter the queue to a single discussion, and displays parent/reply context on comment cards. Comment cards now include an official reply composer that posts admin-authored replies through the page/blog comment API, attaches them to the selected parent/thread, and lets reviewers publish immediately or send the reply to the manual queue. Reply cards now include a same-target parent selector so reviewers can move a reply under another top-level parent/thread through the single-comment update API. A rendered analytics panel consumes `GET /api/sites/:siteId/comments/analytics`, exposing 30-day totals, report reasons, target hot spots, and threaded queue signals for custom admin dashboards. A rendered delivery activity panel consumes `GET /api/sites/:siteId/events?kind=all`, filters comment events, and shows activity, webhook, and email-channel submission, report, moderation, and thread-change handoffs plus retry controls for failed webhook/email handoffs. Settings now exposes comment email notification enablement, recipient, and workflow webhook URL used by comment delivery. Reported comments can now be resolved from the queue or selected bulk controls, clearing `reportCount`/`reportReasons` while preserving review metadata through the moderation API. Blocking a comment now refreshes the author blocklist panel, and stored email/IP blocklist entries can be reviewed and removed from the Comments UI. Repository mode now stores comment author email/IP blocklist entries in the database, the site comment blocklist API reads/deletes those durable entries, and page/post repository deletes clean attached comments before removing the target. `npm run test:comments --workspace @backy-cms/admin` asserts denied `comments.manage` and denied `comments.configure` overrides return `FORBIDDEN_PERMISSION`, creates a temporary published page, configures a temporary comment webhook plus local-outbox comment email recipient, verifies required comment captcha rejects missing tokens and accepts a mock provider token, submits public pending page comments plus a parent/reply thread and second top-level move target, reports one comment through the public report API, verifies the analytics API reports seeded pending/reported/threaded/reason counts, verifies comment-submitted/comment-reported activity, webhook, and email delivery event readback including failed webhook retry from the rendered panel, opens the real Comments UI, verifies the rendered analytics panel, delivery activity panel, thread map, and parent/reply card context, creates an official admin reply through the rendered composer and verifies API readback with parent/thread linkage plus approved status, moves an existing reply to another rendered parent and verifies parent/thread API readback, saves a stricter comment policy through rendered controls, verifies policy API readback, verifies a public blocked-term comment is rejected, approves one comment, rejects another, resolves the reported comment through rendered controls, blocks one comment with a persisted reason, verifies the author blocklist entry is created, removes it through the rendered blocklist panel, verifies API readback including reviewer/rejection reason/cleared report flags/blocklist cleanup, checks layout overflow/regions, captures a screenshot artifact, deletes the temporary page, restores notification settings and default policy, and verifies attached fallback comments are cleaned up. `npm run test:repositories --workspace @backy/db` covers durable comment moderation update persistence, comment blocklist create/list/delete behavior, and page/post comment cleanup in repository mode. | Current comments scope is complete; broader configured Supabase/Postgres integration smoke remains tracked under platform DB rollout. |
| `/media` | Partial | `apps/admin/src/routes/media.tsx` now loads/uploads/updates/deletes through backend media endpoints, with metadata editing, search/type/visibility filters, folder create/delete/filter/assignment UI, selectable media cards, bulk visibility/folder/delete actions, usage links to referenced pages/posts, explicit page/post bind-unbind controls, and font registration fields for uploaded font family/weight/style/fallback/display. The page now groups uploaded font assets by family, variants, fallback stack, display mode, and delivery visibility. The page exposes usage analytics for the loaded library: storage/type mix, referenced vs unused counts, public/private delivery counts, replacement/version counts, largest assets, and actionable All/Unused/Referenced/Replaced grid focus controls. The media storage panel now shows the active runtime provider, quota, Supabase project/bucket readiness, missing environment fields, a direct Infrastructure settings link, and Media-page "Run check" plus "Provision probe" actions that reuse the Settings infrastructure validator for storage/Supabase diagnostics, create or verify the configured local uploads directory/Supabase Storage bucket/S3-compatible bucket when credentials allow it, and verify the active bucket/path can upload, read, stat, list, and clean up a probe object. The same storage panel can edit and save media.configure-gated non-secret provider metadata from Media itself: provider, bucket, public base URL, path prefix, private file delivery, image transforms, Supabase project URL/ref, and Supabase storage enablement, while keeping secrets in environment variables. It now also renders a provider-specific environment contract for local, Supabase, and S3-compatible storage, including required env vars, optional vars, secret indicators, and current missing/detected status so operators have the runtime credential checklist beside the Media setup form, and the provisioning probe returns provider-specific credential fields plus a no-secrets rotation runbook. The media storage surface now also renders upload-scanner runtime status plus a scanner env contract for `BACKY_MEDIA_SCAN_*`, including provider, HTTP endpoint, ClamAV clamd host/port, optional bearer key, timeout, and fail-open semantics, while admin settings exposes the same non-secret `runtimeMediaScanner` summary for custom admin frontends. The media detail dialog exposes delivery controls for custom frontends: public file endpoint URLs, time-limited signed URL generation with expiry/disposition controls, public image transform URL generation for optimized frontend image delivery, prepared responsive image `srcset` manifests with generated WebP byte/provider metadata, Backy-served delivery analytics, static upload safety scan status, stable-ID file replacement, replacement history, and a per-asset activity trail from admin audit logs. Page/post save, rollback, and delete flows sync media usage references, and `POST /api/admin/sites/:siteId/media/:mediaId/bind` can explicitly bind/unbind media to page/post contexts. Public media catalog hides private items, while public font media is exposed through `/render` `assets.fonts` with `weights`, `styles`, `fallbackStack`, `display`, and `cssFamily`; hosted pages/posts inject matching `@font-face` rules. Admin integrations can now mint temporary signed media file URLs through `POST /api/admin/sites/:siteId/media/:mediaId/signed-url`, and `/api/sites/:siteId/media/:mediaId/file` blocks unsigned private files while serving valid signed private bytes from the storage adapter and recording Backy-served request/byte analytics. Public image assets expose a `responsive` manifest in list/detail responses, `POST /api/admin/sites/:siteId/media/:mediaId/transforms` now generates stored WebP variants through the active storage adapter while accounting generated bytes against quota, and `/api/sites/:siteId/media/:mediaId/transform?width=...&quality=...` remains a validated on-demand optimizer fallback that records transform request analytics, with manifest/OpenAPI/SDK exposure and admin smoke coverage. Uploads and replacements enforce a per-site media quota from `BACKY_SITE_MEDIA_QUOTA_BYTES` with a 500 MB default, run static safety checks before storage, can optionally forward bytes to a fail-closed HTTP scan provider or built-in ClamAV `clamd` INSTREAM adapter configured through `BACKY_MEDIA_SCAN_*` env vars, reject active-content SVG or provider-rejected payloads, return quota usage on upload/list/replace responses, and count retained replacement versions plus generated transform bytes against storage usage. `npm run test:media --workspace @backy-cms/admin` now covers API-backed folder creation, public image and private file upload, rendered media command center/upload/API/storage/env-contract/scanner-contract/folder/bulk/library regions, Media-page media.configure-gated storage metadata editing with API readback, denied media.create/media.edit/media.configure/media.delete permission coverage and restoration, Media-page Supabase env contract rendering, Media-page scanner env contract rendering, Media-page storage/Supabase infrastructure check rendering, Media-page provisioning/rotation probe rendering with bucket automation, detail-dialog stable-ID file replacement with retained replacement history, responsive variant preparation with generated transform metadata, metadata editing with visibility changes and API readback, temporary signed URL generation from the details dialog, delete confirmation, layout checks, screenshot capture, and cleanup. `npm run test:media-safety --workspace @backy/public` verifies optional scanner hooks across default static scanning, clean HTTP provider verdict metadata, HTTP provider rejection, missing HTTP endpoint fail-closed/fail-open behavior, ClamAV clean verdict metadata, ClamAV infected rejection, ClamAV availability fail-open behavior, and unsupported-provider errors. `npm run test:media-quota --workspace @backy/public` verifies low-quota rejection against a configured server. `@backy/storage` provides local/S3/R2/Supabase adapter contracts for upload/read/delete/sign/list/stat, with local path traversal coverage in `npm run test:smoke --workspace @backy/storage`; public runtime media storage now selects local, S3/R2-compatible, or Supabase storage from environment variables and exposes a non-secret runtime summary through admin settings. Upload/replacement metadata stores the provider storage path and safety scan result so private file reads and deletes do not depend on reverse-parsing remote public URLs. Media create/update/delete, replace, transform preparation, provider analytics ingest, and explicit bind/unbind now emit queryable admin audit logs with request-id correlation, existing-media mutation routes require media.edit while upload/folder-create require media.create and destructive routes require media.delete, provider/CDN metrics persist request/byte/conversion/value attribution fields, the library analytics panel renders provider ROI totals for attributed value/conversions/CVR/value per request, retained-version comparisons include media-aware preview fallbacks, and `npm run test:repositories --workspace @backy/db` covers DB-backed media version create/list/delete cleanup. | Add in-provider secret rotation; current Media flow can create or verify the configured local/Supabase/S3 storage container, validates the active bucket/path, and guides credential rotation from environment metadata. |
| Editor media modal | Partial | `apps/admin/src/components/editor/MediaLibraryModal.tsx` now uses backend list/upload and scopes assets to page/post/global contexts; it accepts image/video/file/font uploads, detects font files by MIME or extension, stores configurable font registration metadata on font uploads, and shows font thumbnails in the picker. The picker now exposes insert sizing presets, image fit, draggable crop/focal preview handles with keyboard nudge support, upload progress counters, current-asset replacement while keeping the stable media id, replacement comparison context for already-bound assets, current-vs-candidate binary size/type delta metadata before replacement upload, and direct font weight/style/fallback/display controls for upload and selection flows. `CanvasEditor` hydrates site media so uploaded and registered font files appear in font controls, and the property panel exposes upload/select font actions for text elements plus image media insertion that persists picker fit/focal metadata into canvas image props. `npm run test:editor-media-upload --workspace @backy-cms/admin` verifies image upload defaults, upload progress, folder organization, picker focal/fit controls, draggable focal preview handles/crop box state, current-asset replacement comparison plus binary-diff panel, persisted image `objectFit`/`objectPosition`/`imageFocalPoint`, video/file/font uploads, font default controls, manual saves, and backend page readback. `npm run test:collections --workspace @backy-cms/admin` verifies collection field media pickers expose the library insert/focal-preview/font controls while preserving image-only and mixed media filtering. | Same external provider automation gaps as `/media`; no picker-specific drag-handle or binary-diff blocker remains. |
| `/users` | Partial | `apps/admin/src/routes/users.tsx` now provides a backend-backed users command center. It loads users through authenticated `GET /api/admin/users`, exposes access-readiness checks, role/status metrics, private users API snippets, handoff JSON, CSV export/import/template controls with explicit duplicate-handling policy and dry-run preview, search/role/status/access-review filters, table pagination/sorting, inline role/status updates through authenticated `PATCH /api/admin/users/:userId`, selected-row bulk status/delete actions through `POST /api/admin/users/bulk`, delete confirmation through authenticated `DELETE /api/admin/users/:userId`, role permission summaries, signed-in account self-protection locks for role/status/removal/bulk selection, membership-registration handoff links into Pages, Forms, Contacts, and Settings, and a user access activity panel backed by admin audit logs. Users APIs now enforce the saved permission matrix for `users.view`, `users.create`, `users.manage`, and `users.delete`, including explicit per-user deny overrides. Invited user creation now queues transactional invite delivery through the shared Backy email provider stack while keeping copy-link backup controls. CSV import now records rollback snapshots for created/upserted users and exposes a rollback action that deletes users created by the selected import batch, restores pre-import fields for updated users, blocks repeated rollback, and records `user.import.rollback` audit metadata. `npm run test:users --workspace @backy-cms/admin` verifies the users API rejects missing auth, verifies a denied `users.create` override blocks user creation with `FORBIDDEN_PERMISSION`, creates temporary collaborators through an authenticated users API session, opens the real Users UI, verifies the invite and directory surfaces, previews a CSV import without creating users, imports one collaborator from CSV while skipping a duplicate email, imports again in update-existing mode, verifies import audit metadata for create and upsert, rolls the upsert import back from the rendered import result panel, verifies the user fields are restored and `user.import.rollback` audit metadata is recorded, verifies the signed-in admin row self-protection locks, changes role and status from the directory, runs a rendered bulk status update, verifies bulk API delete and bulk audit-log readback, checks API readback, verifies the user update audit log through `/api/admin/audit-logs`, verifies the rendered Access activity panel, verifies layout/regions, captures a screenshot artifact, removes the user through the rendered confirmation modal, and verifies cleanup. | Broader auth/RBAC enforcement across every non-user admin resource, workspace/team scoping, and persisted granular permission resources beyond the Users surface. |
| `/users/new` | Partial | `apps/admin/src/routes/users.new.tsx` now creates through `POST /api/admin/users`, with duplicate email and role validation at the API layer plus local fallback state. It includes an invite command center, readiness checks, role/status selection, access preview, API payload preview, copy/download handoff controls, and return-to-users routing with site context. Generated invite links now target `/accept-invite?token=...`, where local-demo invited accounts can accept the token, activate the user, receive an admin session, and record `user.invite.accept`. Invite and password-reset token creation now queues transactional email through the shared Backy email provider stack (`local-outbox`, HTTP endpoint, Resend, or SMTP), records provider/status/error metadata in user audit events, persists `deliveryConfigured` with stored auth tokens, and keeps permission-gated copy links as manual backup. `test:users` renders the invite page, verifies the command-center/form surface, fills the controlled invite form by stable field hooks, asserts payload preview JSON, selected role/status, submit readiness, and both submit buttons before submission, verifies invite creation returns a queued delivery result plus an accept-invite URL, verifies the rendered invite panel reports queued delivery or manual fallback, generates and accepts a detail invite token with API/user/audit readback, generates a password reset token with queued delivery metadata, then covers the persisted user workflow through the API and Users directory. | Workspace scoping remains; invite/reset email delivery is now wired through Backy's transactional email providers with local outbox fallback. |
| `/users/$userId` | Partial | `apps/admin/src/routes/users.$userId.tsx` now loads/saves/deletes through authenticated `GET/PATCH/DELETE /api/admin/users/:userId`, with local fallback state. It exposes a user access command center, readiness checks, role/status editing, signed-in account self-protection locks, backend-loaded permission matrix from authenticated `GET /api/admin/users/:userId/permissions`, persisted per-user inherit/allow/deny overrides through authenticated `PATCH /api/admin/users/:userId/permissions`, API update handoff, lifecycle actions for active/invited/inactive/suspended, reset-email helper, invite-link generation through `POST /api/admin/users/:userId/invite-link` with admin-selected expiry, password reset token generation through `POST /api/admin/users/:userId/password-reset` with admin-selected expiry, transactional invite/reset delivery metadata with copy-link backup controls, per-user audit activity from `GET /api/admin/audit-logs?entity=user&entityId=...`, active local-demo admin session history/revocation controls from `GET/DELETE /api/admin/auth/sessions`, and guarded removal. `test:users` opens the real signed-in admin detail page and verifies self-protection locks plus protected current-session rendering, then opens a temporary user detail page, verifies the rendered permission matrix, saves a granular permission override with API readback and audit-log verification, selects invite/reset expiry options, generates and accepts an invite link with activation/session/audit readback, generates and accepts a reset link with local credential update/new-password-login/audit readback, verifies selected expiry values and queued delivery metadata in audit responses, suspends the user through the lifecycle action UI, verifies API readback, verifies the rendered detail activity timeline, and returns to the directory removal flow. | Broader route/API permission enforcement outside Users, ownership transfer flows, and deeper audit-event filtering/detail views. |
| `/teams` | Partial | `apps/admin/src/routes/teams.tsx` now loads teams through authenticated `GET /api/admin/teams`, gates the workspace with `users.view`/`users.manage`, renders loading/empty/error/permission states, and uses the shared admin session token for backend writes. The Teams UI can create teams, switch the current team, edit team name/slug, delete teams with confirmation, invite members with role selection, queue transactional invite email delivery through Backy's shared provider stack with provider/status metadata and a manual copy-link backup, update member roles, block self/final-owner membership mutations, and remove invited members. Newly created teams are inserted into local state before the post-mutation refresh, so a successful create stays visible even if the follow-up list refresh fails. Backend team APIs support authenticated list/create/update/delete plus member invite/list/update/remove through `/api/admin/teams` and `/api/admin/teams/:teamId/members`, emit team/member audit metadata including invite delivery provider/status/error fields, and work in demo fallback or repository mode. The page now renders an `activity.export`-gated Team activity panel for the selected workspace, loading request-id-backed team/member audit events from `GET /api/admin/audit-logs?teamId=...` with refresh, loading, empty, and error states. Team list/detail APIs now include a workspace ownership summary for owned sites, the rendered Teams UI shows a Workspace Sites panel with site counts and owned-site rows, and team deletion is blocked in demo and repository modes while owned sites remain attached. `npm run test:teams --workspace @backy-cms/admin` creates and updates a temporary team through the API, verifies API deletion, opens the real `/teams` UI in authenticated headless Chrome, creates a team through the rendered modal, verifies the Team activity panel shows the create audit event, creates a temporary site owned by that team, verifies `DELETE /api/admin/teams/:teamId` returns `TEAM_HAS_SITES`, verifies the rendered Workspace Sites panel shows the owned site and disables deletion until the site is removed, edits name and slug, verifies the update audit event, invites a temporary viewer, verifies the queued invite delivery panel plus invite audit event, changes the invited member to editor, verifies the role-update audit event, removes that member, verifies the member-removal audit event, deletes the team through confirmation, then creates and accepts a temporary admin invite to verify the rendered Teams UI allows create/edit/invite/delete and audit refresh for a non-owner admin role, creates/accepts a temporary editor invite to verify default editor role access is blocked by `users.view` and disables create/refresh, creates/accepts a temporary default viewer invite to verify the viewer role is blocked by `users.view` and disables create/refresh, and creates/accepts a separate temporary viewer invite with explicit `users.view` allow plus `users.manage`/`activity.export` deny overrides to verify the rendered Teams UI is read-only with audit refresh disabled by that real session. The smoke captures a screenshot and cleans up temporary team/user records. A fresh bounded run passed on 2026-05-15. | Broader workspace/site authorization across every content resource, billing/plan enforcement, and broader production role policy rollout across non-Team resources. |
| `/settings` | Partial | `apps/admin/src/routes/settings.tsx` now loads/saves delivery mode and regenerates API keys through `GET/PATCH/POST /api/admin/settings`, with local fallback state. Settings persist to `data/backy/admin-content.json`, admin reads include active runtime media storage, database, Supabase, media scanner, Vercel, notification email, and commerce webhook-secret diagnostics. `POST /api/admin/settings` with `action: "validate-infrastructure"` returns database/storage/Supabase/media-scanner/Vercel/notification/commerce readiness diagnostics against draft metadata and runtime env, and full Settings editors can record Vercel deployment/domain check history with request IDs, status counts, project/domain metadata, and audit entries. The Delivery tab surfaces API contracts and storage runtime state, the Infrastructure tab persists non-secret Supabase/Vercel connection metadata, shows detected environment readiness, exposes runtime cards and an env validation matrix for storage, Supabase, media scanning, Vercel, notifications, and commerce webhook secrets, exposes a rendered infrastructure check result panel plus recent deployment history, and persists global media policy for max upload size, workspace storage limit, warning threshold, and allowed file types; the Commerce tab now persists storefront mode, currency, payment-provider handoff metadata, checkout redirect paths, guest checkout, taxes, shipping, discounts, pricing rule defaults, inventory reservation, commerce webhook intent, and global platform billing plan/limits/overage/contact policy. This keeps the content/settings/API control plane Backy-owned while treating Supabase, Vercel, notification providers, media scanners, and payment providers as configurable external connectors with secrets kept outside settings. Commerce webhook signing secret fields now accept environment-variable references only; raw secret-like provider values are rejected by UI validation and the Settings PATCH API with `SECRET_REFERENCE_REQUIRED`, and the Settings smoke verifies rejection without mutating persisted settings. The page has moved further onto shared Backy-owned UI primitives (`Button`, `Panel`, `Notice`, `SegmentedTabs`) and includes a settings command center, frontend API capability map, downloadable/copyable handoff manifest, global design/SEO defaults, commerce handoff, notification routing with executable webhook test/retry controls, auth policy controls, API-key rotation, persisted API-key rotation history, named admin service-key issuance with one-time raw-key display, hash-only service-key metadata, service-key last-used/scope metadata, service-key revocation controls, API-key revocation history for rotated-out and manually revoked key fingerprints, and settings audit history. `npm run test:settings --workspace @backy-cms/admin` opens the real admin Settings UI in headless Chrome, verifies tab navigation, changes delivery/general/appearance/SEO/infrastructure/commerce/notification/security controls, verifies broader environment validation surfaces for media scanning, notifications, and commerce secrets, runs the infrastructure check button, verifies Supabase/Vercel/media-scanner/notification/commerce provider diagnostics and persisted deployment history, executes Settings notification webhook test and retry against a local capture server, saves through the backend API, confirms persisted Supabase/Vercel/storage policy/storage provider/commerce/auth/notification metadata including deployment history, commerce pricing rules, and billing limits, confirms planned notification channels and digest choices are normalized out of both the admin UI save path and admin settings API, checks no horizontal overflow, captures a screenshot artifact, asserts `settings.view`, `settings.configure`, `settings.manageKeys`, and `media.configure` denials return `FORBIDDEN_PERMISSION` for Settings read/write/infrastructure/storage/key-management paths, and restores the original settings payload. `npm run test:media-quota --workspace @backy/public` now verifies the media upload API enforces the Settings storage policy for allowed file types and max upload size, while still honoring the env/site media quota and returning quota warning metadata. The Security smoke creates a temporary owner session, drives the real Security tab public-key regeneration confirmation, verifies the public key changes while the admin key is preserved, issues a named admin service key, confirms the raw key is shown only at issue time while persisted/read API responses expose only redacted metadata, verifies the service key authenticates admin reads before revoke, persists a last-used timestamp with non-owner-admin scope, and is rejected after revoke, verifies the API-key audit row renders, verifies the non-secret rotation-history and revocation-history entries persist and render changed/unchanged/revoked/manual fingerprints, and restores the original API keys. Settings changes, infrastructure deployment-check recordings, notification webhook test/retry attempts, API key regeneration, service-key issuance, service-key usage timestamp updates, and service-key revocation emit queryable admin audit logs with redacted API-key snapshots, and the Security tab shows recent settings audit events plus API-key rotation and revocation history with refresh/loading/empty/error states. | Broader auth/RBAC rollout outside Settings, workspace/site scoping, actual Supabase/Vercel/payment-provider connection execution, billing enforcement across workspace/site APIs, and broader site-level webhook execution beyond current form/comment/contact/settings paths. |

## Public/API matrix

| Surface | Status | Current evidence | Required to complete |
| --- | --- | --- | --- |
| `GET /api/sites` | Partial | `apps/public/src/app/api/sites/route.ts` now returns `{ success, requestId, data }` for public site list/detail bootstrap, keeps legacy top-level `site/sites` keys for compatibility, resolves by id/slug/custom domain, and hides draft sites. `npm run test:admin-contract --workspace @backy/public` covers draft-hidden and published-resolved behavior. | Auth/rate limits where needed, richer pagination, locale/domain canonicalization, and durable data source. |
| `GET /api/sites/:siteId/pages` | Partial | Public page retrieval exists from `backyStore`; draft access requires a matching preview token instead of raw `includeUnpublished`, scheduled pages are hidden until their `scheduledAt` time passes, and page list/detail responses now return `{ success, requestId, data }` envelopes while preserving legacy top-level page keys. Page list/detail resources now include normalized SEO metadata (`title`, `description`, `canonical`, `canonicalUrl`, robots, Open Graph, keywords, and JSON-LD) derived from page meta for custom frontends, and that SEO shape is documented in OpenAPI and the JS SDK. Published page reads now emit public contract/cache headers, ETags, conditional 304 responses, and database-mode `content` cache revisions, while preview-token reads are no-store. Page preview-token creation records redacted `previewToken.create` audit logs with tokenless path metadata. `GET /api/sites/:siteId/navigation` now exposes a backend-owned primary/footer navigation tree with page, internal route, external URL, and nested child items while falling back to publishable pages when no custom primary menu exists, and public navigation reads emit contract/cache headers, ETags, conditional 304 responses, and database-mode site-level cache revisions. `GET/PATCH /api/admin/sites/:siteId/navigation` now reads/writes the stored menu and validates page references before saving, and `/sites/$siteId` now provides a connected navigation-management UI. `GET/POST/PATCH /api/admin/sites/:siteId/redirects` now reads/writes stored redirect/410 rules, validates invalid rules, and previews source-route conflicts or missing internal targets without requiring a save; `/sites/$siteId` exposes connected redirect management and conflict diagnostics. `GET /api/sites/:siteId/resolve?path=/...` resolves page/blog/dynamic collection item paths plus site-level redirect and gone rules for external frontends with public contract/cache headers, ETags, conditional 304 responses, and database-mode site-level cache revisions for published reads. `npm run test:admin-contract --workspace @backy/public` covers future-hidden/past-visible scheduled pages, normalized public page SEO on detail/list responses, page preview-token audit readback/redaction, page/post content feed cache headers/304 behavior, configured nested navigation and URL links plus navigation cache headers/304 behavior, invalid navigation references, invalid redirect rules, redirect conflict preview diagnostics, page/post route resolution cache headers/304 behavior, redirect/410 route resolution, and collection dynamic item route resolution. | Broader contract tests. |
| `GET /api/sites/:siteId/render` | Partial | `apps/public/src/app/api/sites/[siteId]/render/route.ts` returns the new AI/custom-frontend render payload for pages, blog posts, and generated collection dynamic list/item pages, and supports token-gated draft page preview. Render payloads now include a `navigation.primary` manifest derived from publishable pages, public media, uploaded public font assets, plus normalized collection dataset manifests, resolved public collection fields/records, server-resolved element prop values, and collection-record editable map entries when canvas elements declare collection `dataBindings`, hydrated `repeater` element datasets/records, or generated dynamic list/item payloads. Published render responses include short public cache headers, ETag/304 revalidation, and Backy contract/schema/request/site metadata headers, while preview/error responses are no-store; page preview-token creation is now audited without storing raw tokens. `npm run test:admin-contract --workspace @backy/public` validates page, blog, collection-bound page, repeater hydration, and dynamic list/item render responses against `specs/ai-frontend-contract/content-payload.schema.json`, checks uploaded font asset manifests, checks bound collection values resolve into element props, covers public post render payloads, checks scheduled post render guards, verifies page preview-token audit readback/redaction, and verifies render cache/contract headers plus conditional 304 responses; `npm run test:preview-contract --workspace @backy/public` covers draft blocking and tokenized page render/page/hosted preview. | Version negotiation, custom dynamic template routes, cache invalidation events, stable SDK examples, and CI wiring. |
| `GET /api/sites/:siteId/seo` | Partial | Public SEO discovery now lists publishable page, blog post, and dynamic collection item routes with canonical paths, absolute `canonicalUrl` values, title/description, robots flags, Open Graph basics, route-specific JSON-LD objects, keywords, sitemap priority/change frequency, and updated timestamps. Site SEO defaults now persist via `GET/PATCH /api/admin/sites/:siteId/seo`, are editable from `/sites/$siteId`, and are applied to route titles, fallback descriptions, Open Graph images, site-level JSON-LD defaults, discovery defaults, sitemap enablement/default priority/default frequency/dynamic-route inclusion, and robots index/follow/custom directives. Admin SEO responses and the site UI now show supported title variables plus sample dynamic collection list/item route previews using real collection and record values. Page settings can edit route-specific JSON-LD arrays, and page/post render payload SEO merges site-level and route-level JSON-LD for custom/default frontend head rendering. `format=sitemap` emits XML from indexable routes according to stored sitemap settings and `format=robots` emits robots text from stored robots settings; custom-domain sites expose `https://custom-domain/...` canonical URLs and custom-domain sitemap/robots public URLs while keeping path canonicals for API compatibility. Cacheable public contract JSON responses emit `x-backy-cache-revision`, SEO sitemap/robots responses carry the same SEO revision, hosted and API RSS feeds now emit RSS schema/site/cache-revision headers, and SEO JSON supports ETag/304 revalidation against that revision seed. `@backy/db` now includes a durable `cache_invalidation_events` table and repository/factory contract for scoped invalidation revisions, and database-mode admin SEO, navigation, redirect, page, blog post, collection, collection record, media, and media folder mutations record scoped invalidation events. Hosted Backy sites expose crawlable `/sites/:siteSlug/sitemap.xml` and `/sites/:siteSlug/robots.txt` from the same route index/settings. Site readiness now flags duplicate page/post canonical paths as publish-blocking SEO errors. Site settings now persist redirect/410 route rules consumed by public route resolution; admin redirect read/save/preview responses include route-shadowing and missing-target warnings for safer SEO routing. Manifest/OpenAPI/SDK surfaces advertise and consume the endpoint, and `test:admin-contract` verifies JSON, API sitemap/robots, hosted sitemap/robots, hosted/API RSS cache revision headers, SEO/manifest/OpenAPI/render cache revision and 304 behavior, manifest, OpenAPI, SEO defaults, dynamic SEO previews, custom-domain canonical URLs, JSON-LD defaults, route JSON-LD, sitemap/robots settings, canonical conflict readiness errors, redirect conflict previews, and redirect/410 resolver coverage; `test:repositories` verifies the invalidation repository contract. | Broader non-JSON feed cache coverage as new public feed types are added. |
| `GET /api/sites/:siteId/media` | Partial | Public media list exists, only returns public catalog entries, supports type/scope/page/post/search/tag/folder pagination filters for custom frontends, and now returns a `{ success, requestId, data }` envelope while preserving legacy top-level `media/pagination`. Public image media includes responsive `srcset` manifests that prefer stored WebP variants generated by the admin transform-prep endpoint. `GET /api/sites/:siteId/media/:mediaId/file` serves public files directly and private files only with a valid temporary signed token from the admin signing endpoint while recording Backy-served file request/byte analytics; `GET /api/sites/:siteId/media/:mediaId/transform` validates public image transforms, records transform request analytics, and redirects to the optimizer as an on-demand fallback; `test:admin-contract` covers unsigned private blocking, signed private delivery, generated image variants, image transform redirects, delivery analytics, and unsupported transform errors. | Editable metadata/reference browsing from custom frontend workflows where needed. |
| `GET/POST/PATCH/DELETE /api/admin/sites/:siteId/media` | Partial | Admin media upload/list/update/delete/folder endpoints store files through the environment-selected `@backy/storage` adapter (`local`, `s3`, or `supabase`) and metadata under `data/backy/media-library.json`; list filters support type, visibility, search, tag, folder, and quarantine state; upload and folder creation require `media.create`, existing-media metadata updates, replacements, retained-version restore, transform preparation, folder updates, bind/unbind, and provider analytics ingestion require `media.edit`, storage metadata changes require `media.configure`, and destructive media/folder/version operations require `media.delete`; `POST /api/admin/sites/:siteId/media/:mediaId` replaces an asset file while keeping the media id stable and retaining prior file metadata in `metadata.replacementVersions`; uploads/replacements store SHA-256 binary fingerprints, and retained-version comparison renders name, size, MIME, provider, storage path, checksum, and timeline deltas before restore/delete decisions; `POST /api/admin/sites/:siteId/media/provider-analytics` ingests provider/CDN counters plus conversion/value attribution fields in batch, matches rows by media id, storage path, or URL, supports replace/increment modes, returns matched/unmatched rows, persists provider delivery metadata, records audit activity, and invalidates media caches in repository mode; `GET /api/admin/sites/:siteId/media/:mediaId/versions`, `POST /api/admin/sites/:siteId/media/:mediaId/versions/:versionId`, and `DELETE /api/admin/sites/:siteId/media/:mediaId/versions/:versionId` expose DB-backed retained version review, restore, and deletion with stored-file cleanup when a storage path is recorded; `POST /api/admin/sites/:siteId/media/:mediaId/transforms` generates stored WebP responsive image variants and records byte/provider metadata; content saves sync page/post usage references; explicit media bind/unbind route updates page/post references and binding metadata; uploads/replacements/transform generation enforce a per-site quota, run static upload safety checks, reject active-content SVG payloads, support optional HTTP provider scan hooks and a built-in ClamAV clamd adapter, and return quota usage metadata; the admin Media page now exposes scanner runtime/env contract, quarantine readiness/count/filter controls, detail-level quarantine, retained-version compare/restore/deletion, provider analytics ingest endpoint handoff, bulk quarantine/release that forces blocked assets private and clears quarantine metadata with previous-status context, and a library-wide activity panel backed by `/api/admin/audit-logs` with action filtering, pagination, activity-export permission context, CSV export, and open-asset shortcuts; media create/update/delete/replace/version-restore/version-delete, transform preparation, folder create/update/delete, and bind/unbind emit admin audit logs covered by `test:admin-contract`, while `test:media` covers rendered media activity, browser-driven quarantine blocking, provider analytics ingest/merge by storage path, retained-version compare/restore/deletion with checksum/path/name diff output and media previews, denied permission gates across media.create/media.edit/media.configure/media.delete, and bulk release from the quarantined filter. | Automated provider-account bucket creation and in-provider secret rotation; current runtime probe validates active storage operations. |
| Collection APIs | Partial | `GET/POST/PATCH/DELETE /api/admin/sites/:siteId/collections` and nested `/records` endpoints now persist collection schemas and records to `data/backy/admin-content.json`. Public endpoints under `GET /api/sites/:siteId/collections` expose only published/public-readable collections and publishable records, include select/tag option metadata plus reference targets in field schemas, return `{ success, requestId, data }` envelopes while preserving legacy top-level collection/record keys, emit public contract/cache headers with ETags, support conditional 304 responses, and attach database-mode `content` cache revisions from invalidation events. Public/admin record lists support basic search, field-value filtering, sorting, limit, offset, and pagination metadata. Public `POST /api/sites/:siteId/collections/:collectionId/records` respects `permissions.publicCreate`, applies collection metadata visitor-create field allowlists before validation/storage, validates submitted values, stores visitor-created records as draft, and returns private/no-store contract responses with ignored-field metadata. Public `PATCH/DELETE /api/sites/:siteId/collections/:collectionId/records/:recordId` now respect `permissions.publicUpdate`/`permissions.publicDelete`, require a collection-scoped public write token from `visitorWritePolicy`, enforce selected public-update field allowlists, and return private/no-store envelopes for successful mutations. The admin record list endpoint also serves the same filtered result set as CSV via `format=csv`, `/records/bulk` handles selected-record status updates and deletes, and `/records/import` accepts CSV for schema-validated create/upsert, including select/tag option validation. Collection-bound canvas elements, repeater elements, and generated collection list/item pages now surface dataset, field, field option/reference, record, and binding metadata through `/render`; public `/:collectionSlug` and `/:collectionSlug/:recordSlug` routes resolve after normal page/blog routes, and admin writes reject page/collection route conflicts before ambiguous URLs are saved. Collection schema and admin record create/update/delete mutations emit queryable admin audit events with request IDs plus compact before/after or deletion metadata. The admin `/collections` page now exposes schema CRUD, public read/create/update/delete controls, collection-scoped public write-token configuration, selected visitor create/update field allowlists, field option/reference editors, record CRUD, record search/filter/sort/pagination/bulk controls, CSV import/export, JSON backup import/export, and permission-aware mutation/export/delete controls against these endpoints, and the editor property panel can attach collection field bindings to selected canvas elements. `npm run test:admin-contract --workspace @backy/public` covers collection CRUD, collection schema and record create/update/delete audit readback, required/unique/option field validation, public collection feed cache headers/304 behavior, public visibility guards, public record reads/search/filter/sort, public create disabled/allowed behavior, draft moderation for visitor-created records, public update/delete disabled guards, write-token enforcement, selected update-field filtering, admin CSV export/import, admin record pagination metadata, bulk record status/delete actions, partial record updates, collection backup export/import, collection RBAC denies, collection-bound render payload hydration, repeater dataset hydration, dynamic list/item route/render payloads, hosted collection route rendering, OpenAPI public collection update/delete operations, and cleanup. `npm run test:collections --workspace @backy-cms/admin` verifies Backy-authored list/item page canvases can be captured into collection metadata and rendered by public dynamic collection routes with collection and record bindings applied, verifies public update/delete policy controls persist the write token and selected update field allowlist through the admin UI, and verifies authored-template compare controls render active-versus-history diff summaries. | Continue deeper in-editor dataset UX and broader page/editor parity work. |
| Reusable section APIs | Partial | `GET/POST/PATCH/DELETE /api/admin/sites/:siteId/reusable-sections` now persists reusable canvas sections to `data/backy/admin-content.json`, and public `GET /api/sites/:siteId/reusable-sections` plus detail reads expose active saved sections for published sites with public contract/cache headers, ETags, conditional 304 responses, and database-mode `content` cache revisions. Database-mode reusable-section mutations record scoped invalidation events. Saved sections keep normal `content.elements`/`canvasSize`, support category/tag/search/status filters where appropriate, validate required non-empty content, reject duplicate slugs, support optimistic conflict guards and bounded version history, restore older versions, export/import portable JSON with upsert history preservation, structured library metadata editing, and discover/bulk-refresh synced page/blog instances while preserving instance placement. Admin reusable-section list/detail/version/export/instance reads enforce `pages.view`, create/update/import/restore/metadata/instance-refresh mutations enforce `pages.edit`, and deletes enforce `pages.delete`. Reusable-section create/update/metadata-update/delete/restore/import/instance-propagation emits queryable admin audit logs covered by `test:admin-contract`. The editor library lists active saved sections, saves the selected element tree through `POST`, renames/deletes saved entries, refreshes the saved-section list, inserts saved sections as synced canvas instances with source metadata, refreshes a selected synced instance from its saved source, and detaches instances into independent editable copies while still storing concrete canvas trees for public rendering. Manifest/OpenAPI/SDK surfaces advertise and consume reusable section reads. `npm run test:admin-contract --workspace @backy/public` covers missing-auth rejection, create/list/detail/update/archive/delete plus duplicate and invalid-content errors, structured metadata read/update, version conflict/history/restore, export/import/upsert, synced instance discovery/refresh, reusable-section audit logs, public list/detail cache headers/304 behavior, and manifest/OpenAPI exposure; SDK smoke covers public reusable-section reads; editor drag smoke now covers synced reusable-section insert/source-refresh/detach. | Add full template registry contracts, move persistence to DB, and extend RBAC enforcement to the rest of the admin surface. |
| Sites/pages admin APIs | Partial | `GET/POST/PATCH/DELETE /api/admin/sites` and `/api/admin/sites/:siteId/pages` now persist to `data/backy/admin-content.json`. Page publish/archive/rollback/revisions, `scheduledAt`, and preview token endpoints exist. Page create/update/delete mutations now emit queryable admin audit events with request IDs plus compact before/after or deletion metadata, and page preview-token creation records redacted tokenless audit metadata. The public API proxy now allows the local admin dev origin so the Vite admin can call the Next backend without CORS failures and stamps admin API responses with `Cache-Control: no-store`, `x-backy-cache-scope: admin`, request ids, and `x-backy-admin-contract-version: backy.admin.v1`. `npm run test:admin-contract --workspace @backy/public` covers temporary site CRUD, page create/list/detail/update/delete audit readback, scheduled page visibility, the admin-origin CORS preflight/read path, and global admin response headers for every admin JSON call exercised by the smoke. `npm run test:sites --workspace @backy-cms/admin` now covers the site creation command surface, API-backed temporary site creation, rendered multi-site command center/API/feature-contract regions, UI status publishing with API readback, delete confirmation, layout checks, screenshot capture, and cleanup. | Full session auth/RBAC, durable database, production origin policy, and broader contract tests. |
| Blog admin APIs | Partial | `GET/POST/PATCH/DELETE /api/admin/sites/:siteId/blog` now persists posts to `data/backy/admin-content.json`. Post publish/archive/rollback/revisions, `scheduledAt`, and preview token endpoints exist. Post create/update/delete mutations emit queryable admin audit events with request IDs plus compact before/after or deletion metadata, and preview-token creation records redacted tokenless audit metadata. Category and tag CRUD endpoints exist under `/api/admin/sites/:siteId/blog/categories` and `/api/admin/sites/:siteId/blog/tags`; database-mode category/tag mutations record scoped invalidation events. Author list endpoints exist under `/api/admin/sites/:siteId/blog/authors`; public taxonomy/author list endpoints and post filters also exist, public search/archive filters accept `q`/`search` plus `year`/`month`, and public category/tag/author/search/archive feeds emit contract/cache headers with ETags and conditional 304 responses. Admin blog list/detail/readiness/revision/author/taxonomy reads enforce `pages.view`, create/update/archive/rollback enforce `pages.edit`, publish/preview-token creation enforce `pages.publish`, and deletes enforce `pages.delete`. `npm run test:admin-contract --workspace @backy/public` covers missing-auth rejection, temporary blog category/tag CRUD, taxonomy feed cache headers/304 behavior, author resources, post assignment/filtering, post create/update/delete audit readback, and scheduled post visibility; `test:blog-list` covers public search/archive feed delivery. | Durable database, audit event UI, and broader contract tests. |
| Users admin APIs | Partial | `GET/POST/PATCH/DELETE /api/admin/users` now persists team users to `data/backy/admin-content.json`; `POST /api/admin/users/import` imports CSV rows with full-name/email/role/status validation, duplicate-email skip or explicit upsert mode, dry-run preview counts with no writes/audit mutation, `users.create` plus `users.manage` permission enforcement for upserts, current-user protection, last-active owner/admin safeguards, and queryable create/upsert import audit events. `POST /api/admin/users/bulk` supports selected-user status updates and deletes with current-user protection, last active `owner`/`admin` safeguards, `users.manage`/`users.delete` permission enforcement, and queryable bulk audit events. User role/status normalization now preserves `owner` and `suspended`, the detail endpoint prevents deleting, demoting, or deactivating the last active `owner`/`admin`, and create/update/delete mutations emit queryable admin audit logs with before/after snapshots, request IDs, role/status/email metadata, and changed-field hints. Local-demo admin sessions can now be listed and non-current sessions can be revoked through `GET/DELETE /api/admin/auth/sessions` with bearer-session authorization and current-session protection. Local-demo invite tokens can be generated through `POST /api/admin/users/:userId/invite-link` with validated expiry minutes, accepted through public `POST /api/admin/auth/accept-invite`, consumed on success, and audited with selected expiry metadata. Local-demo password reset tokens can be generated through `POST /api/admin/users/:userId/password-reset` with validated expiry minutes and accepted through public `POST /api/admin/auth/reset-password`, which consumes the token, updates a hashed local credential, starts a session, and records `user.password_reset.accept`. `npm run test:admin-contract --workspace @backy/public` covers list/create/duplicate-email/update/delete plus the last-admin safeguard. `npm run test:users --workspace @backy-cms/admin` now covers API user creation, API search/role/status filters, API `sortBy`/`sortDirection` ordering before pagination, API limit/offset pagination metadata, CSV import dry-run preview, duplicate skips, update-existing mode, permission-denial enforcement, invite generation and acceptance, reset generation and acceptance, selected invite/reset expiry audit metadata, new-password login, UI self-protection locks, active session rendering, UI directory role/status updates, bulk status updates, bulk API deletion, user update/import/bulk audit-log API readback, rendered activity panel, detail lifecycle status updates, delete confirmation, API readback, layout checks, and cleanup. | Full session auth/RBAC on every admin resource, workspace scoping, and broader contract tests. |
| Settings admin APIs | Partial | `GET/PATCH/POST /api/admin/settings` now persists delivery mode, API keys, API-key rotation history metadata, API-key revocation history metadata for rotated-out and manually revoked key fingerprints, hash-only named admin service-key grants with permission scope and last-used timestamps, auth policy, global design/SEO/notification settings, commerce behavior and billing-limit settings, global storage policy limits, and non-secret Supabase/Vercel/storage integration metadata including Vercel deployment/domain check history to `data/backy/admin-content.json`; it returns non-secret `runtimeStorage`, `runtimeDatabase`, `runtimeSupabase`, `runtimeMediaScanner`, `runtimeVercel`, `runtimeNotifications`, and `runtimeCommerce` diagnostics for the active runtime without exposing stored secrets. Settings update, Settings infrastructure deployment-check recording, Settings notification webhook test/retry execution, API-key regeneration, admin service-key issuance, and admin service-key revocation emit redacted admin audit logs covered by `test:admin-contract` and `test:settings`. Commerce webhook signing secret updates are restricted to environment-variable references, with raw secret-like values rejected before persistence by the Settings PATCH API. `POST /api/admin/settings` can issue a named admin service key once, stores only its hash/fingerprint/prefix/scope metadata, excludes hashes from settings responses and audit snapshots, accepts active service keys for non-owner admin API access, updates last-used metadata after successful service-key auth, rejects revoked service keys, records deployment-check history for full Settings infrastructure checks, and executes Settings notification webhook test/retry requests with request-id/status/error metadata. Media upload, replacement, and responsive-transform APIs now resolve the Settings storage policy for quota limits, allowed MIME/extension checks, and max upload size enforcement, while env quota remains an override for deployment-level caps. `npm run test:admin-contract --workspace @backy/public` covers read/update validation/key regeneration, runtime diagnostics, integration metadata persistence, and no secret exposure in runtime summaries. `npm run test:admin-auth --workspace @backy/public` verifies that a key-enforced server rejects missing/wrong admin API keys and accepts `X-Backy-Admin-Key` plus bearer auth. `npm run test:settings --workspace @backy-cms/admin` covers the admin UI save path and readback for delivery/general/appearance/SEO/infrastructure/commerce/notification/security settings, including Settings broader environment validation, deployment-history readback, billing-limit readback, Settings notification webhook test/retry execution, persisted/rendered API-key rotation and revocation history fingerprints, service-key issue/use/revoke behavior, service-key usage timestamp/scope readback, and explicit `FORBIDDEN_PERMISSION` denials for `settings.view`, `settings.configure`, `settings.manageKeys`, and `media.configure` across Settings read/write/infrastructure/storage/key-management paths. `npm run test:media-quota --workspace @backy/public` covers Settings-based file-type and max-size enforcement plus quota details. The broader admin contract smoke can also target a key-enforced server and will send the configured key on admin requests. | Workspace/site scoping, executable provider configuration beyond non-secret metadata, and broader contract tests. |
| Forms/Contacts APIs | Partial | Form definitions, submissions, contacts, spam checks, CSV-like admin exports, and audit events exist in `apps/public/src/lib/backyStore.ts`. Public form list/detail, definition, submission list/detail/create/review, and contact list/status routes now return `{ success, requestId, data }` envelopes while preserving legacy top-level fields where applicable. Public form list reads and `GET /api/sites/:siteId/forms/:formId/definition` emit contract/cache headers with ETags and conditional 304 responses; form detail, submission list/detail/create/review, and contact list/update routes use contract headers but remain private/no-store because they currently include submission/contact visitor data. Admin forms now support direct `POST /api/admin/sites/:siteId/forms` creation plus `GET/PATCH/DELETE /api/admin/sites/:siteId/forms/:formId` detail/update/deletion in both repository-backed and demo fallback modes, with fallback form definitions persisted into `admin-content.json`; deletion also removes related fallback submissions and contacts. Required checkbox consent fields and authored validation rules validate correctly for public submissions. Canvas-derived forms and standalone admin-created forms can define collection write targets and field maps; accepted public form submissions can create draft collection records after public-create permission and collection schema validation, including normalized source-submission provenance fields when present, and contact-share submissions create/update private contacts. Admin contact list/create/update/import endpoints are exercised from `/contacts`, including direct JSON create, CSV import with email upsert, full contact field updates for merge operations, and rendered duplicate merge UI contracts. `GET /api/admin/sites/:siteId/forms/contact-segments` now returns backend contact segment analytics for all site forms or a selected form across demo and repository modes. `GET/POST/DELETE /api/admin/sites/:siteId/forms/contact-lists` now persists saved contact filter lists under site settings, returns matched contact counts and IDs, and records audit events in demo and repository modes. `POST /api/admin/sites/:siteId/forms/:formId/contacts/:contactId/promote` now promotes qualified contacts into Backy users with optional invite-link generation and persisted contact promotion metadata. `POST /api/admin/sites/:siteId/forms/:formId/contacts/:contactId/promote-customer` now promotes qualified contacts into private customer collection records with persisted contact metadata. `POST /api/admin/sites/:siteId/forms/:formId/contacts/sync` now sends selected contacts to an admin-provided webhook and records `contact-sync` delivery events. `POST /api/admin/sites/:siteId/forms/:formId/contacts/consent-retention` now exports selected contact consent evidence and applies due consent/source-IP anonymization. `test:admin-contract` covers the temporary page/form/collection/contact flow plus form list and form-definition cache headers/304 behavior and private submission/contact cache headers. The frontend manifest/OpenAPI and SDK now expose the cacheable form definition URL and `formDefinitionCached()` helper. Form delivery events can be queried through `GET /api/sites/:siteId/events?kind=form-submission&formId=:formId`; notification email sends support local-outbox, generic HTTP handoff, Resend, and SMTP provider modes with queued/succeeded/failed events. Failed submission webhooks can be replayed through `POST /api/admin/sites/:siteId/forms/:formId/submissions/:submissionId/webhook-retry`, and `/forms` now renders delivery history plus retry controls. `/forms` also detects consent-like checkbox fields and exports a dedicated consent CSV with status, request, timestamp, source, policy label, retention/delete due dates, privacy request email, and optional IP hash/user-agent provenance. `/forms` now persists per-form spam timing, rate-limit, duplicate-window, blocked-term controls, and consent retention/export policy controls; admins can apply due-consent anonymization for consent values plus IP/user-agent provenance, and admin automation can run the same policy site-wide; public submissions enforce spam rules before persistence or delivery. Captcha-enabled public forms verify Turnstile, hCaptcha, reCAPTCHA, and generated frontend token aliases before persistence or delivery. Form admin mutations and review/retention workflows now emit request-id audit events rendered on `/forms`, and form admin routes enforce dedicated `forms.*` permission keys with override coverage. `supabase/migrations/002_forms_contacts_persistence.sql` adds durable Supabase/Postgres tables, policies, and indexes for forms/submissions/contacts, while `npm run test:repositories --workspace @backy/db` covers repository create/update/list/detail/delete behavior for form definitions, submissions, contacts, and spam/consent settings JSON persistence. `npm run test:forms --workspace @backy-cms/admin` covers denied `forms.create` override enforcement, admin template creation, form-builder update persistence, authored validation rule persistence, standalone form collection mapping persistence, per-form spam and consent settings persistence, consent retention anonymization including the site-wide automation endpoint, public blocked-term spam rejection, public definition readback, invalid public registration rejection, valid public registration submit, collection record creation, configured email delivery events, consent export UI, webhook failure/retry event history through the rendered Forms UI, UI moderation, rendered audit activity, API readback, layout checks, and cleanup. `npm run test:contacts --workspace @backy-cms/admin` covers contact direct creation, CSV import, contact-share creation, backend contact segment analytics, saved contact-list API create/render/delete, contact-to-user promotion, contact-to-customer promotion, selected contact webhook sync, contact consent-retention export/anonymization, permission/audit panel rendering, duplicate-email merge, private contact listing, notes/status update through the Contacts UI, API readback, layout checks, and cleanup. | Add a database-mode integration smoke against a configured Supabase/Postgres service. |
| Comments APIs | Partial | Page/blog comments, reports, moderation filters, site policy, analytics, and report reasons exist. Public page/blog comment submit/list/detail/update endpoints, site-wide comment list/detail/update/report routes, comment analytics, event listing, and report-reason discovery now return `{ success, requestId, data }` envelopes while preserving legacy top-level comment/reason/event keys. Report-reason discovery emits public contract/cache headers, ETags, conditional 304 responses, and cache revisions; comment list/detail/moderation/report submission, analytics, and interaction-event routes emit contract headers with `private`/`no-store` cache scope because they can include visitor identity, actor, and moderation state. Page/blog comment submission now respects persisted site comment policy for open/closed state, default moderation, guest/name/email/reply rules, blocked terms, and captcha provider enforcement; report submission respects the policy report toggle. The frontend manifest exposes normalized `site.commentPolicy` for custom frontend builders, including captcha requirement/provider/site-key hints for rendering public comment forms. Comment-policy-only admin site patches require `comments.configure`, moderation/update/retry/blocklist routes require `comments.manage`, private comment reads require `comments.view`, comment policy/moderation/thread/retry/blocklist actions record admin audit logs, `/comments` renders the filtered comment audit trail, and the comments smoke asserts denied `comments.manage` and `comments.configure` overrides plus rendered audit activity. The site-wide moderation PATCH route now supports `action: "clearReports"` / `clearReports: true` in demo and repository modes so report triage can resolve flags without changing the comment status, and the admin API helper now forwards `commentThreadId` filters for thread-specific moderation handoffs. Admin-authored official replies are submitted through the page/blog comment API with parent/thread IDs, admin request IDs, and reviewer-selected moderation mode. Single-comment PATCH now also supports moving replies by updating `parentId`/`commentThreadId` with same-target parent validation in demo and repository modes. `GET /api/sites/:siteId/comments/analytics` provides authenticated 30-day/all-time totals, status counts, report reasons, thread metrics, top targets, and daily submitted/reviewed/reported trends in demo and repository modes. Comment submission, report, moderation, and thread-update paths now execute configured Settings workflow webhooks and comment notification emails, recording queued/succeeded/failed channel events through `GET /api/sites/:siteId/events?kind=all`; `/comments` consumes those events so admins can see activity, webhook, and email handoffs beside the moderation queue. Failed comment webhook/email events can be replayed through `POST /api/sites/:siteId/comments/:commentId/delivery-retry`, and `/comments` renders retry controls for failed webhook/email handoffs. Repository mode now includes a durable `comment_blocklist` table/repository/API path, DB-backed blocklist deletion, and page/post target delete cleanup for attached comments; `test:repositories` covers moderation persistence, durable blocklist create/list/delete, and target cleanup. `test:admin-contract` submits an approved page comment and verifies target comments, site-wide comments, detail/update/report routes, report reasons, report-reason cache headers/304 behavior, private comment/event route cache headers, and audit events through the public envelopes. `npm run test:comments --workspace @backy-cms/admin` now covers permission-denied overrides, rendered comment audit activity, policy UI persistence, required comment captcha rejection/acceptance, public blocked-term rejection, public pending page comment submission, parent/reply thread creation and UI context, analytics API counts, site-wide comment listing, public report submission, configured webhook and local-outbox email delivery events, rendered comment delivery activity from the events API, rendered comment audit activity, a failed report webhook replayed from the rendered delivery panel, UI-created official admin reply readback, UI reply reparenting readback, UI-driven approve/reject/report-resolution actions, API readback, layout checks, and fallback cleanup after deleting the temporary page. | No Comments-specific API blocker remains; the broader configured Supabase/Postgres integration smoke is tracked under platform DB rollout. |
| Blog APIs | Partial | Public blog routes exist; draft detail access requires a matching preview token instead of raw draft status access, scheduled posts are hidden until their `scheduledAt` time passes, and blog post list/detail plus taxonomy/author endpoints now return `{ success, requestId, data }` envelopes while preserving legacy top-level keys. Public category/tag/author list endpoints and `categoryId/categorySlug/tagId/tagSlug/authorId/authorSlug`, `q`/`search`, and `year`/`month` blog filters now exist. `GET /api/sites/:siteId/render?path=/blog/:slug` returns a post-shaped AI/custom-frontend render payload with post comment interactions. Public RSS is now available at `GET /api/sites/:siteId/blog/rss` and hosted `/sites/:subdomain/blog/rss.xml`, uses the same visible published/past-scheduled post rules as the blog API, includes post links, descriptions, categories/tags, authors, and RSS contract headers, and is advertised through the frontend manifest, OpenAPI, and JS SDK. The frontend manifest now exposes structured `modules.blog.feeds` discovery metadata for RSS, including endpoint, hosted path, content type, schema version, visibility rules, cache revision header, and `limit` bounds; OpenAPI carries the same feed metadata through an `x-backy-feed` operation extension plus `BlogFeedDiscovery` schema, and the JS SDK exposes `blogFeeds()` with smoke coverage. `npm run test:blog-api --workspace @backy/public` is now the public blog/API contract entrypoint chaining preview, admin-contract, and public-security smokes. `npm run ci:blog-api-contract` runs `npm run test:blog-types --workspace @backy/sdk-js`, starts the public app, waits for manifest discovery, runs that blog/API smoke, shuts the server down, and `.github/workflows/blog-api-contract.yml` wires the gate into pull requests and pushes to `main`. Blog preview-token creation now records a `previewToken.create` post audit event with target, slug, TTL, expiry, and tokenless preview path metadata, while keeping the raw token out of audit storage. `packages/sdk-js/scripts/blog-contract-types.ts` verifies generated-client/type contract coverage for typed blog list filters, `blog`/`blogCached` response types, taxonomy response types, manifest feed discovery typing, and rejected unsupported generated-client filters; `npm run test:blog-types --workspace @backy/sdk-js` and the CI wrapper exercise it. `npm run test:preview-contract --workspace @backy/public` covers draft post blocking and tokenized post API preview; `npm run test:admin-contract --workspace @backy/public` covers public taxonomy/author list/filter smoke, preview-token audit readback/redaction, RSS future-scheduled hiding/past-scheduled inclusion, hosted RSS, manifest/OpenAPI RSS discovery, structured feed discovery, and post render payloads; `npm run test:smoke --workspace @backy/sdk-js` covers the SDK `blogRss`/`blogRssUrl`/`blogFeeds` helpers; `test:blog-list` covers public search/archive feeds. | No Blog-specific API blocker remains; continue broader generated SDK-from-OpenAPI rollout under Frontend manifest/OpenAPI/SDK APIs. |
| Public hosted pages | Partial | `apps/public/src/app/sites/[subdomain]/[[...path]]/page.tsx` renders page content, supports token-gated draft page previews, resolves collection-bound page element values before rendering, renders generated collection item pages for published `/:collectionSlug/:recordSlug` records, and applies site-level redirect/gone settings before normal page resolution. Demo hosted page, redirect/gone, and dynamic collection route selection now reuses `resolveSiteRoute` from `apps/public/src/lib/routeResolver.ts` before fetching the resolved page or collection content for HTML rendering, reducing drift with `GET /api/sites/:siteId/resolve`. Repository-backed hosted page, blog, redirect/gone, and dynamic collection route selection now reuses `resolveRepositorySiteRoute` from `apps/public/src/lib/repositoryRouteResolver.ts` in both hosted HTML rendering and `GET /api/sites/:siteId/resolve`; `npm run test:public-security --workspace @backy/public` includes static regression coverage that blocks duplicated database collection route matching from returning to either surface. `apps/public/src/app/sites/[subdomain]/blog/[slug]/page.tsx` renders published posts, resolves collection-bound post element values, and supports token-gated draft post previews with preview metadata marked noindex. `apps/public/src/app/sites/[subdomain]/blog/page.tsx` now renders a theme-aware hosted blog archive with published-post cards, featured-image delivery, category/tag/author filters, search, empty states, and pagination links scoped to `/sites/:subdomain/blog`; when the site frontend-design contract includes a `/blog` page template or `blog-archive` template, the archive now renders that authored canvas through `PageRenderer`, hydrates archive title/description/filter tokens, and injects published posts into bound repeaters with post href/image/author/category/tag fields. Hosted sitemap/robots route handlers now publish crawlable SEO files for each published Backy site. `GET /api/sites/:siteId/resolve` exposes page/blog/dynamic item/redirect/gone publish guards for external frontends. Hosted HTML now receives explicit `Cache-Control: no-store`, `x-backy-cache-scope`, `x-backy-hosted-cache-policy`, and `x-backy-render-surface` headers from `apps/public/src/proxy.ts`, keeping draft preview and mutable public HTML out of shared caches while API/render endpoints retain their cache/ETag contracts. `PageRenderer` now applies inherited site theme tokens to hosted output, including root/body/heading fonts, text/background colors, unit-normalized spacing CSS variables, site custom CSS, theme custom font URLs, uploaded font assets, hidden-element suppression, responsive breakpoint markers, scale markers, and mobile/tablet/desktop responsive overrides for layout, style, props, visibility, and nested children; `npm run test:page-renderer --workspace @backy/public` covers that inherited theme/font contract plus responsive breakpoint thresholds/overrides, custom blog archive template hydration, collection-authored dynamic item template precedence, record binding, canvas sizing, and captured custom CSS preservation. `npm run test:preview-contract --workspace @backy/public` covers hosted page/post preview smoke, resolver-backed dynamic collection list/item HTML, hosted HTML cache headers, and blog archive/filter/empty-state rendering. `npm run test:hosted-responsive --workspace @backy/public` creates a temporary published demo page, opens hosted HTML in headless Chrome at desktop/tablet/mobile sizes, asserts live breakpoint and scale markers, checks responsive text/layout/visibility overrides from real DOM geometry, rejects horizontal overflow/error overlays, captures non-empty screenshots, and cleans up the temporary page. Public rendering now skips elements marked `visible: false` by the editor layer controls. | No current hosted-pages-specific blocker remains; continue broader DB-backed/service and CI rollout. |
| Frontend manifest/OpenAPI/SDK APIs | Partial | `GET /api/sites?identifier=:identifier` and `GET /api/sites` provide contract/cache-aware public site discovery with ETags, conditional 304 responses, and site metadata headers for identifier lookups. `GET /api/sites/:siteId/manifest` exposes a schema-validated site discovery document for external frontends, including site identity, theme tokens, schema references, capability flags, auth-aware admin capability flags, route patterns, redirect/gone route metadata, configured navigation, public endpoint URLs, admin endpoint hints, SEO/sitemap/robots URLs, canonical/custom/managed delivery domain discovery, explicit single-locale discovery, pages, blog taxonomy/author summaries/feed discovery, collection schemas/routes, reusable section summaries, form submit/detail/submission/contact URLs, page/blog comment URL templates, comment report URLs, interaction event URLs, collection targets, media/font counts, and a Settings-derived commerce storefront contract for catalog/order-intake/checkout UI decisions. Anonymous manifest requests now expose `data.admin.auth.mode=anonymous` with every known admin permission false, while valid admin session requests expose the session role/status plus allowed `sites.*`, content, media, commerce, comments, users, settings, and activity flags; admin API keys are treated as non-owner admin capability contexts so owner-only permissions such as key management are not advertised. Site frontend design contracts can be read/saved/captured through `GET/PATCH/POST /api/admin/sites/:siteId/frontend-design` with `sites.view` for reads and `sites.configure` for writes; create-page/create-blog/forms/collections/products/reusable-section workflows can preserve frontend design template provenance, and frontend-design contract saves/captures now emit queryable `frontendDesign.update`/`frontendDesign.capture` audit logs with database-mode settings cache invalidations. `GET /api/sites/:siteId/openapi` exports a site-scoped OpenAPI 3.1 document for discovery, resolve, render, SEO discovery, navigation, media, collection, reusable-section, form detail/submission/contact, page/blog/site comment, comment report, report reason, and interaction event operations, including typed redirect/gone route schemas, `ROUTE_GONE` 410 responses, RSS feed discovery extensions, and `x-backy.delivery` domain/locale metadata. Published discovery responses include short public cache headers, ETag/304 revalidation, and Backy contract/schema/request/site metadata headers, while hidden/error discovery responses are no-store. `packages/sdk-js` now provides a TypeScript client and exported public contract shapes for render payloads, typed route results including redirect/gone, content documents/elements, navigation items, SEO routes, media/font assets, collection schemas/records, reusable sections, forms/submissions/contacts, comments, events, commerce storefront settings, manifest admin discovery, manifest delivery discovery, response metadata, and conditional 304 results. The SDK also exposes `manifestCached`, `openapiCached`, and `renderCached` helpers so generated/custom frontends can use Backy's ETags without raw `fetch`, and `resolve()` returns non-throwing gone envelopes for 410 routes while ordinary errors still throw. `npm run test:smoke --workspace @backy/sdk-js` validates site discovery, manifest admin discovery, manifest delivery discovery, OpenAPI delivery extensions, cached manifest/OpenAPI/render 304 revalidation, resolve, redirect/gone route handling, render, SEO discovery, configured navigation, media, reusable sections, forms, site comments, events, public collection record creation, form submission/review/contact updates, page comment submit/detail/update, comment reports, and report event reads against the local public app. `npm run ci:sdk-smoke` now typechecks/builds the SDK, starts the public app, waits for manifest discovery, runs the SDK smoke, shuts the server down, and `.github/workflows/sdk-smoke.yml` wires that gate into pull requests and pushes to `main`. `test:admin-contract` validates the manifest against `frontend-manifest.schema.json` and verifies anonymous/authenticated manifest admin capability states, delivery domain/locale metadata, key OpenAPI paths/schemas/extensions, routing metadata, navigation payloads, SEO/reusable-section/commerce discovery, cache/contract headers, conditional 304 responses, plus draft-site visibility guards; `npm run test:frontend-design --workspace @backy-cms/admin` validates missing-auth rejection, frontend-design persistence, manifest exposure, template-seeded page/blog content, capture, and audit readback. | Generate SDK types directly from OpenAPI/schema instead of maintaining handwritten starter types, multi-locale routing variants, fuller component schemas, and DB-backed service data. |

## Immediate implementation order

1. **Real admin persistence API**: add authenticated admin endpoints for sites/pages/posts/users/settings and move admin store writes behind API helpers.
2. **Editor completion pass**: layers panel, snapping/alignment, undo/redo hardening, responsive canvas modes, composed presets, saved reusable sections, preview/publish, revision autosave, and conflict-safe save/reload recovery.
3. **Media management completion**: public media feeds now expose cache revision headers/ETags and admin asset details expose queryable activity, replacement history, retained-version compare/restore/deletion with checksum/path/name/timeline diff review and media previews, library usage analytics, generated responsive image manifests, Backy-served delivery counts, manual provider/CDN metric recording with conversions/value attribution, batch provider/CDN metric ingestion, provider ROI rollups, scanner runtime contract, HTTP/ClamAV provider scan behavior, and detail/bulk quarantine workflows; remaining work is automated provider-account bucket creation and in-provider secret rotation.
4. **Publishing model**: add cache invalidation, preview token audit events, and broaden hosted preview tests into the main CI path.
5. **Backend validation/test suite**: broaden endpoint contract tests (`test:preview-contract`, `test:admin-contract`), editor serialization tests, render payload schema validation, and smoke tests for every admin page.

## Product backlog notes

- **Reusable admin UI system**: continue moving every page onto shared Backy-owned primitives for buttons, panels, pills, tabs, notices, empty states, tables, and form controls before adding page-local styling.
- **Blog/page editor organization**: the blog editor now uses a shared workspace frame and better grouped toolbar controls, and the shared page editor can persist desktop/tablet/mobile responsive overrides, but the canvas, component rail, layers/properties panels, page size controls, autosave, preview, and post settings still need a deeper organization pass so users can freely design complex pages without the surface feeling cramped or scattered.
- **Page editor conflict safety**: page editor saves now send `expectedUpdatedAt`, stale backend PATCH requests return `PAGE_VERSION_CONFLICT`, the editor renders a save-conflict banner with editor/backend timestamps plus a reload-latest action, and `BACKY_EDITOR_CONFLICT_SMOKE=1 npm run test:editor-drag --workspace @backy-cms/admin` verifies a stale save is blocked without overwriting the newer backend copy.
- **Page editor rich text persistence**: selected-range rich-text marks now survive the canvas history/save path instead of being collapsed to plain text; `BACKY_EDITOR_RICH_TEXT_SMOKE=1 npm run test:editor-drag --workspace @backy-cms/admin` verifies persisted font size, font family, underline, strikethrough, and color leaf marks through the backend page API.
- **Registration/form backend**: registration templates can now create standalone backend forms, standalone forms can be edited from `/forms`, authored validation rules persist into public definitions, standalone form collection binding is configurable from the admin form surface, per-form notification email/webhook targets, spam timing/rate/duplicate/blocked-term controls, captcha provider verification, consent retention/export policy controls can be configured in the form builder, due consent evidence can be anonymized from the Forms panel, and the same retention policy can run site-wide from admin automation, form notification emails create provider-ready/local-outbox/generic HTTP/Resend/SMTP delivery events, failed form submission webhooks and failed email notifications can be retried from the rendered Forms delivery panel, rendered consent export CSV is available for consent-like fields with policy/retention/deletion metadata, reusable embed blocks preserve frontend design context, request-id audit activity is rendered on `/forms`, form admin APIs enforce first-class `forms.*` permissions, backend contact segment analytics are available for site-wide or form-scoped lead lists, saved/custom lead lists are backed by `/forms/contact-lists`, qualified contacts can be promoted to Backy users and private customer records, and the public registration validation/spam/captcha/submit/email/webhook-delivery/retry/collection-write/moderation loop is covered by `test:forms`; form definitions/submissions/contacts have durable Supabase/Postgres migration coverage and repository persistence smoke coverage. Remaining work is a real database-mode integration smoke against a configured Supabase/Postgres service.
- **Contacts/lead backend**: contact-share submissions now have a dedicated `/contacts` smoke covering private contact list, direct contact creation, CSV import with email upsert, backend contact segment analytics, saved contact-list API create/render/delete, contact-to-user promotion, contact-to-customer promotion, selected contact webhook sync, contact consent-retention export/anonymization, permission/audit panel rendering, duplicate-email merge/dedupe UI, notes, individual lifecycle status, rendered bulk lifecycle actions, API readback, and cleanup; `test:repositories` now covers the repository-backed contact workflow for filtering, exact-ID updates, merge/promotion metadata, retention anonymization, and contact audit logs. No Contacts-specific backend blocker remains; broader Supabase/Postgres service smoke remains tracked under Forms/Contacts APIs.
- **Comments/moderation backend**: `/comments` now has a dedicated smoke covering denied comments permission overrides, site policy save/readback, required comment captcha rejection/acceptance, public blocked-term rejection, public pending page comments, parent/reply thread creation and rendered thread context, analytics endpoint/panel counts, public report submission, configured webhook and local-outbox email delivery events, rendered comment delivery activity from the events API, rendered comment audit activity, failed webhook replay from the rendered delivery panel, official admin reply authoring with parent/thread API readback, reply reparenting with parent/thread API readback, UI approval/rejection/report-resolution/blocking, author blocklist creation/removal, API readback, layout checks, notification settings restoration, default policy restoration, and fallback cleanup. Repository mode now has durable blocklist persistence plus page/post comment cleanup coverage in `test:repositories`; no Comments-specific backend blocker remains.
- **Users/access backend**: `/users`, `/users/new`, and `/users/$userId` now have smoke coverage for missing-auth rejection on the users API, authenticated API creation, CSV import dry-run preview, duplicate skips and update-existing mode, invite/directory/detail rendering, signed-in admin self-protection locks, active local-demo session history, backend-loaded permission matrix rendering, invite-link and password reset token generation with selected expiry and audit-log readback, role/status updates, rendered selected-user bulk status updates, bulk delete API behavior, user/import/bulk audit-log readback, rendered directory and detail access activity, lifecycle suspension, delete confirmation, API readback, layout checks, and cleanup; remaining work is real auth/RBAC across non-user admin resources and workspace scoping beyond the Users surface.
- **Sites/multi-site backend**: `/sites` is now Ready after fresh smoke coverage for the site list command surface, UI-driven blueprint creation, starter page API readback, UI status publishing, domain verification state, Vercel deployment handoff state, per-site billing/quota state, search/status/domain/page-coverage filters, duplicate-as-draft, quick archive, frontend API/feature-contract regions, typed delete confirmation, layout checks, screenshot capture, cleanup, and DB repository persistence for domain/deployment/billing site settings. `/sites/new` remains Partial for workspace/team ownership, template marketplace/imports, and its own production-hardening gaps.
- **Media/file backend**: `/media` now has smoke coverage for folder organization, public image/private file upload, rendered central upload/API/storage/scanner/bulk/library/activity surfaces, stable-ID replacement with retained version history plus retained-version compare/restore/deletion, retained-version checksum/path/name/timeline diff review with media previews, responsive variant preparation, rendered library and asset audit activity, metadata save, visibility changes, signed URL generation, manual provider/CDN metric recording and conversion persistence, batch provider/CDN metric ingestion by storage path with conversions/value attribution, provider ROI dashboard rendering, storage provisioning/rotation probe rendering, media.create/media.edit/media.configure/media.delete denied-permission gates, detail quarantine blocking, quarantined-filter bulk release, delete confirmation, API readback, layout checks, and cleanup; remaining work is automated provider-account bucket creation and in-provider secret rotation.
- **Commerce/products**: product and private order primitives, schema sync, public catalog API, checkout-intake API, inventory reservation, admin product/order workspaces, product and order CSV export/import/template controls, typed collection CSV import, smoke coverage, and Settings-level commerce behavior/provider handoff metadata now exist. Products now carry first-class tax class, shipping profile, discount code, return-policy metadata, subscription enabled/interval/trial metadata, and generated variant option matrices through the admin editor, schema sync, CSV import/export, frontend-design product seeding, demo seed catalog, public commerce catalog delivery/checkout/subscription payloads, and commerce smoke coverage. Products also exposes list and detail page-template shortcuts that seed `/pages/new` with the Products collection dataset binding for storefront authoring. Public order intake now calculates quote totals for enabled tax, shipping, and discount settings/rules, returns those totals plus a manual/provider-ready checkout-session handoff in the public order response, persists subtotal/discount/shipping/tax/total/session/payment fields on the private order, and upserts private customer profiles with order count/total spent/last order metadata using the real order record id. Products now renders an inline private customer profile manager for name, email, phone, status, and notes, with commerce smoke coverage proving the checkout-created profile can be updated to `vip`. Commerce webhook settlement now accepts allowlisted provider events, maps checkout-session/payment metadata to private orders, persists paid/refunded/failed settlement state, and exposes commerce-webhook events through `/events`. Orders now includes a rendered provider reconciliation action that compares recent commerce webhook activity with private order payment state and repairs stale records through the admin reconciliation API; the same endpoint supports scheduled dry-run previews plus service-key single-site and platform batch GET entrypoints for cron-style reconciliation execution, and root `vercel.json` wires the platform endpoint to a daily Vercel cron. Settings persists payment provider test/live mode, webhook endpoint/secret-reference metadata, webhook event allowlists, configurable pricing-rule defaults, and reconciliation mode/window, while public contracts expose only non-secret readiness and policy fields. Remaining parity work is provider API execution, production secret configuration for cron, provider-grade tax/discount/shipping integrations, automated provider refunds, provider subscription billing execution, richer product template libraries/custom dynamic route promotion, product-specific notification automation, and deeper commerce analytics.

## Current conclusion

Backy has meaningful scaffolding for a CMS/editor and now has stronger media and render-contract foundations, but most admin pages are still **Prototype** or **Partial**. The next milestone should be replacing local admin CRUD with real backend APIs before adding more visual features, otherwise new Wix/Canva-style UI controls will keep saving into a non-production data model.
